# Sub2API 美西、东京账号池使用评估与整改方案

> 评估时间：2026-08-20 13:44，Asia/Shanghai  
> 数据窗口：最近 30 天为 2026-07-21 13:44 至 2026-08-20 13:44；趋势与故障重点看最近 7 天  
> 数据来源：美西、东京 Sub2API PostgreSQL 只读聚合，未读取或落盘 API Key、OAuth Token、代理密码  
> 隐私处理：人员只使用 Key ID 和部门做分析，文档不记录姓名、邮箱、原始 IP  
> 文档状态：本文保留 2026-08-20 的容量、故障和成本评估；当前分组以《sub2api-东京美西API-Key资源分组方案-2026-08-20.md》为准。最近一次分组验收为 2026-08-24，后文已将原始建议与已实施拓扑分开标注。
> 本次同步：2026-09-11；已通过 VPN 对美西、东京 PostgreSQL 做只读复核，分组表以实时结果为准

## 1. 结论先行

2026-08-20 原始评估的整改优先级是账号池容量、Key 与账号的双向全共享、限额保护，出口代理排在后面。分组隔离已按后述实施结果更新，历史故障数据不代表整改后的表现。

最近 7 天，美西总错误率约 22.3%，东京约 38.6%。其中，美西有 18,028 次、东京有 22,257 次错误属于网关侧 503，且没有选中任何上游账号。这类错误分别占两节点全部错误的约 93.9% 和 92.6%，说明账号池在部分时段已无可调度容量。此时先降低账号并发会扩大故障，不应作为第一步。

容量和隔离分两条线推进。原评估中的“美西 6 池、东京 4 池、每池 2 至 3 个账号”是采购前方案，未作为当前线上拓扑实施。已实施方案改为每个 Codex 组只绑定一个 OAuth 账号；后续扩容应新增账号并创建独立组。

- 已实施：美西 `codex-A` 至 `codex-J`，东京 `codex-A` 至 `codex-D`。
- 已实施：无成功用量 Key 放入 `codex-unused` 并保留 active；实时状态为美西 0 个、东京 19 个，且共享 API 账号关联尚未清理。
- 待决策：是否继续采购账号，应以新的 7 天/30 天额度、503 和池级余量复核，不沿用本文件 2026-08-20 的固定采购数量。
- 代理：暂不要求立刻一号一代理。先解决容量、分池、Key 限额和访问控制。代理只用于可信、固定、合规的出口隔离，不用于规避平台规则。
- 中转：可以做 5% 至 10% 灰度和灾备，但按当前用量全量迁移并不比自建账号池便宜。APIKEY.FUN 外接版仅已定价的 GPT 文本模型约为 47,977 元/月；若客户端直接满足“仅限 Codex”组资格，约为 39,981 元/月。两者均未包含当前全部模型流量。

原评估目标方案的账号固定成本约为 4,800 美元/月，按 1 美元兑 7 元折算约 33,600 元/月；该数值是采购情景，不是当前账单。当前已核验的 OAuth 账号规模较小，实际成本应按现有订阅发票重新核算。

## 2. 现状快照

### 2.1 原始账号、人员和用量快照（2026-08-20）

本表保留原始评估窗口，不与后续账号变更混算。最近一次已核验的账号关系和 Key 数见第 2.3 节。

| 指标 | 美西 | 东京 |
| --- | ---: | ---: |
| 当时上游账号 | 13 | 5 |
| OpenAI OAuth | 10：6 个 `pro`，4 个 `prolite` | 4：均为 `pro` |
| OpenAI API Key 账号 | 2 | 1 |
| 其他账号 | Anthropic API Key 1 | 0 |
| 已配置代理 | 0 | 0 |
| 账号并发配置 | 10 至 20 | 10 至 20 |
| 活跃 API Key | 76 | 130 |
| 最近 30 天当前活跃且有成功用量的 Key | 73 | 108 |
| 最近 30 天去重 Key 名称，用作人数近似 | 72 | 108 |
| 最近 7 天去重 Key 名称，用作人数近似 | 68 | 99 |
| 最近 30 天成功请求 | 570,122 | 326,682 |
| 最近 30 天 Token，含缓存读取 | 684.40 亿 | 321.81 亿 |
| 缓存读取占输入侧 Token | 93.2% | 92.1% |
| 最近 30 天内部标价成本 | $54,506.84 | $31,540.55 |

`usage_logs.actual_cost` 是 Sub2API 按模型价表计算的内部标价，不是实际订阅账单。账号订阅是固定月费，API Key 账号和第三方渠道的实际采购价需要以发票或渠道账单为准。

数据库只有 1 个活跃用户，所有 Key 都挂在管理员名下。因此，数据库中的 `user_id` 不能代表真实人员。本文使用去重 Key 名称作为人数近似，最终分组前仍需由负责人确认“一人多 Key”和离职、测试 Key。

### 2.2 原始评估窗口的趋势与故障

| 指标 | 美西 | 东京 |
| --- | ---: | ---: |
| 最近 7 天请求数 | 92,086 | 50,018 |
| 相比前 7 天 | -36.8% | -42.1% |
| 最近 7 天 Token | 142.20 亿 | 50.73 亿 |
| 相比前 7 天 | -13.0% | -37.0% |
| 最近 7 天成功数，运维口径 | 66,894 | 38,181 |
| 最近 7 天错误数 | 19,196 | 24,028 |
| 错误率 | 22.3% | 38.6% |
| 网关无可用账号 503 | 18,028 | 22,257 |
| 最近 24 小时无可用账号 503 | 1,897 | 396 |

流量下降但容量错误陡增，说明问题与账号额度窗口、账号可调度状态和模型资格有关，不是单纯由请求量增长造成。2026-08-20 13:44 的瞬时快照中，美西 10 个 OAuth 账号有 5 个 7 天额度为 100%，东京 4 个均刚进入 0% 状态。东京的低瞬时占用不能推翻过去 7 天已发生的大面积耗尽。

### 2.3 当前分组拓扑（实时核验：2026-09-11）

当前拓扑已不再使用部门组全交叉。每个生产组只绑定一个 OpenAI OAuth；共享 API 账号只承载已映射的非 GPT/Codex 模型。

美西（范围行中的分组与账号按列出顺序一一对应，不是多个 OAuth 共用一个组）：

| 分组 | OAuth 账号 | 套餐 | 活跃 Key |
| --- | ---: | --- | ---: |
| `codex-A` | 42 | `pro` | 8 |
| `codex-B` | 47 | `pro` | 8 |
| `codex-C` | 49 | `pro` | 9 |
| `codex-D` | 51 | `pro` | 9 |
| `codex-E` | 53 | `pro` | 9 |
| `codex-F` | 54 | `pro` | 8 |
| `codex-G` | 67 | `pro` | 8 |
| `codex-H` | 68 | `pro` | 8 |
| `codex-I` | 32 | `prolite` | 4 |
| `codex-J` | 33 | `prolite` | 2 |
| `codex-unused` | 60、70（共享 API） | — | 0 |
| `研发Claude` | Anthropic 账号 36 | API Key | 1 |

美西共享 API 账号 60、70 挂入生产组作为共享模型通道，同时仍关联 `codex-unused`；当前均为 `schedulable=true`，不承载 GPT/Codex OAuth 请求。应清理其在 `codex-unused` 的关联。

东京：

| 分组 | OAuth 账号 | 套餐 | 活跃 Key |
| --- | ---: | --- | ---: |
| `codex-A` | 8 | `pro` | 26 |
| `codex-B` | 16 | `pro` | 31 |
| `codex-C` | 19 | `pro` | 34 |
| `codex-D` | 20 | `pro` | 26 |
| `codex-unused` | 22、23（共享 API） | — | 19 |

东京共享 API 账号 22、23 挂入生产组及 `codex-unused`，主要承载共享模型，不作为 OAuth 故障兜底；应清理其在 `codex-unused` 的关联。原 8 个部门组已 inactive，账号关联已清理；美西旧 `研发Codex` inactive、旧 `研发Grok` 已软删除。

## 3. 需要整改的核心问题

### 3.1 一个人使用多个上游账号

这是调度拓扑造成的系统性现象，不应简单归因于个人主动切号。

- 美西最近 7 天有 68 个 Key 产生成功请求，其中 15 个 Key 使用了 10 个及以上当前账号；Key ID 56 使用了全部 12 个当前 OpenAI 账号。
- 东京最近 7 天有约 90 个 Key 产生成功请求，其中 40 个使用了 4 至 5 个当前账号，26 个使用了全部 5 个账号。
- 最近 30 天，部分 Key 的账号数高于当前账号数，是因为窗口内包含已替换或已删除的历史账号，账号变动也扩大了关联面。

已实施规则是更强的隔离：同一 Key 的 GPT/Codex 请求固定在所属组的唯一 OAuth 账号，不在组内做 OAuth 轮换。账号故障时迁移 Key 或做一对一账号替换，不恢复全池共享；共享 API 账号只处理其模型映射允许的非 GPT/Codex 请求。

### 3.2 一个上游账号被过多人共享

原始评估窗口内存在两个方向的全共享：一个人会打多个账号，一个账号也被大量人员使用。以下是整改前数据，不代表当前组内路由。

- 美西每个主力 OAuth 账号最近 30 天服务了 63 至 71 个不同 Key。
- 东京每个 OAuth 账号最近 30 天服务了 90 至 102 个不同 Key。
- 美西单个 OAuth 账号请求量约为 2.05 万至 7.69 万，相差约 3.7 倍。
- 东京单个 OAuth 账号请求量约为 3.54 万至 9.51 万，相差约 2.7 倍。

全池随机调度没有形成隔离，账号负载也不均衡。

已实施分配采用“容量加权 + 重轻混排 + 低扰动迁移”：

- 规划负载取 `max(最近 7 天 OAuth Token, 最近 30 天 OAuth Token × 7 ÷ 30)`，共享 API 模型用量不参与 OAuth 平衡。
- 按规划负载划分重度前 20%、中度中间 40%、轻度后 40%；以实际规划 Token 优化分配，不沿用原评估的固定人头权重。
- 每个生产组只对应一个 OAuth 账号；实时 Key 数为美西 `8/8/9/9/9/8/8/8/4/2`、东京 `26/31/34/26`，共享 API 账号不计入 OAuth 容量。
- 负载重排只迁移必要 Key，新增 OAuth 时创建新组，不把新账号挂到多个现有组。

人数仍可作为采购和负载复核的二级约束，但不改变“一号一组”：

| 使用形态 | 建议人数/账号 | 适用条件 |
| --- | ---: | --- |
| 重度 Agent 编程 | 2 至 4 人 | 长上下文、高频工具调用、经常使用 Sol |
| 中等研发使用 | 4 至 6 人 | 日常编码，重度与轻度混排 |
| 轻度使用 | 6 至 10 人 | 低频问答、短任务、主要使用 Terra/Luna |
| 上限 | 10 人左右 | 超过后应再拆池，不以继续提高并发代替扩容 |

这是内部容量与故障域建议，不是 OpenAI 官方公布的多人共享额度。实际使用还应确认订阅条款和组织授权。

### 3.3 Key、用户和分组均缺少保护

原始评估中发现的保护缺口如下；本次只同步分组状态，没有重新核验限额和访问控制配置：

- 所有 Key 由 1 个管理员用户持有，无法按真实人员审计、冻结和结算。
- 0 个 Key 配置了 5 小时、1 天、7 天限额或总配额。
- 0 个部门组配置 RPM、日限额、周限额或月限额。
- 0 个 Key 配置 IP 白名单。
- 实时复核中，美西 `codex-unused` 为 0 个 active Key，东京为 19 个；东京这些 Key 仍需与共享 API 账号隔离，恢复前需确认责任人并迁回生产组。
- 美西旧“研发Grok”已软删除，当前不存在仍参与路由的空 Grok 组。

建议：

1. 为每个真实使用者建立独立用户，或至少为 Key 增加明确的责任人、部门和用途标签。
2. 给 Key 配置 5 小时和 7 天额度，先以当前 P95 用量的 1.2 倍为上限，再根据两周数据调整。
3. 给组配置 RPM 和周预算。重度 Key 的突发流量不能把整个账号池吃空。
4. 对固定 CI、办公出口、前置网关配置 IP 白名单。移动办公场景使用可信网关，不要维护大范围放行名单。
5. 连续 30 天无成功用量的 Key 先通知责任人，确认后禁用，不直接删除。

## 4. 推荐分池方案

### 4.1 美西

原评估建议将美西拆成 6 个、每组 2 至 3 个 OAuth；该方案已被 2026-08-24 的实施结果取代。当前按“一号一组”执行：

| 资源组 | OAuth 账号 | 套餐 | 活跃 Key |
| --- | ---: | --- | ---: |
| `codex-A` | 42 | `pro` | 8 |
| `codex-B` | 47 | `pro` | 8 |
| `codex-C` | 49 | `pro` | 9 |
| `codex-D` | 51 | `pro` | 9 |
| `codex-E` | 53 | `pro` | 9 |
| `codex-F` | 54 | `pro` | 8 |
| `codex-G` | 67 | `pro` | 8 |
| `codex-H` | 68 | `pro` | 8 |
| `codex-I` | 32 | `prolite` | 4 |
| `codex-J` | 33 | `prolite` | 2 |
| `codex-unused` | 60、70（共享 API） | — | 0 |

共享 API 账号 60、70 保留在生产组的共享模型通道，当前 `schedulable=true`，不作为 GPT/Codex 兜底；两者仍误挂在 `codex-unused`，应清理；`研发Claude` 单独保留。

### 4.2 东京

原评估建议增加至 12 个 OAuth、形成 4 个多账号池；当前实施采用 4 个单 OAuth 资源组，先解决全交叉和账号隔离：

| 资源组 | OAuth 账号 | 套餐 | 活跃 Key |
| --- | ---: | --- | ---: |
| `codex-A` | 8 | `pro` | 26 |
| `codex-B` | 16 | `pro` | 31 |
| `codex-C` | 19 | `pro` | 34 |
| `codex-D` | 20 | `pro` | 26 |
| `codex-unused` | 22、23（共享 API） | — | 19 |

账号 22、23 作为共享模型通道挂入生产组，但不作为 GPT/Codex 账号；同时仍关联 `codex-unused`，应清理。原 8 个部门组已 inactive，关联已清理。

东京的 API Key 账号只做已映射共享模型通道；若未来承担按量兜底，必须新建独立兜底组并设置日预算，不能与 OAuth 组混成无差别池。

## 5. 是否需要每个账号配置代理

结论：现在不建议为了“防关联”立刻采购廉价住宅代理，也不建议使用轮换代理。当前数据没有显示大规模上游 403 或认证封禁，最严重的错误是 Sub2API 自身没有可调度账号。

代理可以解决的是出口故障域和地域一致性，不能替代容量、权限和合规治理。

可选方案：

| 方案 | 出口数量 | 月成本 | 建议 |
| --- | ---: | ---: | --- |
| 保持节点固定出口 | 2 | 0 增量 | 先执行，优先修容量和分池 |
| 每个现有 Codex 组一个固定出口 | 美西 10 + 东京 4，共 14 | `14 × P` | 现有组与 OAuth 一一对应，P 为单个固定出口月价 |
| 原采购情景：每个 OAuth 一个固定出口 | 历史推荐线共 26 | `26 × P` | 非当前规模，仅保留为历史成本情景 |

出口要求：

- 固定 IP、固定国家或地区、长期不轮换。
- 使用可信供应商和加密隧道，不允许 TLS 中间人解密。
- 代理凭证独立存储和轮换，不写入仓库或报表。
- 先对一个非关键池灰度 7 天，观察延迟、连接失败和账号状态。
- 不使用代理绕过平台限制、地区限制或账号风控。

最近 7 天有一个匿名化来源 IP 同时访问美西和东京，且分别贡献约 4.99 万和 1.50 万请求。同一调用方在跨节点使用，但该地址是 Sub2API 的客户端来源，不等于 OpenAI 看到的上游出口。除非确认客户端 IP 被转发到上游，否则不能据此认定上游账号已因该 IP 关联。

## 6. 需要增加多少账号

### 6.1 容量估算

> 以下是 2026-08-20 的采购规划测算，不代表已经新增的账号数量。2026-08-24 已先通过现有账号重分组完成“一号一组”；是否继续加号，应使用新的线上窗口重新计算。

以最近 7 天网关侧容量失败近似未满足需求，并再保留 20% 余量：

`目标容量系数 = (成功数 + 网关容量失败数) ÷ 成功数 × 1.2`

- 美西目标系数约 1.51。原快照按 4 个 Pro 5x 和 6 个 Pro 20x 估算；当前最近一次核验为 8 个 `pro`、2 个 `prolite`，采购前必须按实际账单和额度重算，不能直接沿用“增加 4 个”的结论。
- 东京目标系数约 1.89。现有 4 个 Pro 20x，约 80 单位，目标约 151 单位，缺口约 71 单位，同样对应增加 4 个 Pro 20x。

该公式是假设额度消耗与套餐倍率近似线性，只适合采购规划，不是 OpenAI 官方容量承诺。模型、上下文、推理强度、工具和缓存都会改变实际额度消耗。

### 6.2 分阶段采购建议

| 阶段 | 美西新增 | 东京新增 | 目的 |
| --- | ---: | ---: | --- |
| 第一批 | 2 个 Pro 20x | 2 个 Pro 20x | 先降低 503，验证模型资格和调度 |
| 容量最低线 | 再加 2，共加 4 | 再加 2，共加 4 | 达到当前需求加 20% 余量的估算线 |
| 隔离推荐线（历史情景） | 保持共加 4 | 再加 4，共加 8 | 原评估的多账号池方案，当前未采用 |
| 东京人数理想线 | 不变 | 再加 2，共加 10 | 东京约 7.7 个活跃人员/账号 |

采购检查点：每批账号上线后至少观察 3 至 7 天；只有网关 503 仍高于 1%、账号池余量低于 20% 或单池人数和负载超标时，才进入下一批。

## 7. 成本

### 7.1 原始账号成本与采购情景（2026-08-20）

截至 2026-08-20，OpenAI 官方 Codex 定价页显示：Plus 为 20 美元/月；Pro 5x 从 100 美元/月起，Pro 20x 为 200 美元/月。本文暂按 `prolite = Pro 5x`、`pro = Pro 20x` 估算；这两个 Sub2API 字段不是 OpenAI 官方套餐名，必须用实际订阅账单复核。[OpenAI 官方 Codex 定价](https://developers.openai.com/codex/pricing)

这套扩容测算只回答当前架构需要多少容量。采购前应确认订阅套餐是否允许当前组织共享方式；不允许时应改用 Business、官方 API 或合规中转。

按上述映射和 1 美元兑 7 元估算。下表保留原账号基线及采购情景，不是当前账单；最新已核验资源是美西 8 个 `pro`、2 个 `prolite`，东京 4 个 `pro`，采购前需重新核账。

| 项目 | 美西 | 东京 | 合计 |
| --- | ---: | ---: | ---: |
| 原评估 OAuth 基线 | 4 × $100 + 6 × $200 = $1,600 | 4 × $200 = $800 | $2,400，约 16,800 元/月 |
| 容量最低线新增 | 4 × $200 = $800 | 4 × $200 = $800 | 新增 $1,600 |
| 容量最低线总额 | $2,400 | $1,600 | $4,000，约 28,000 元/月 |
| 隔离推荐线新增 | 4 × $200 = $800 | 8 × $200 = $1,600 | 新增 $2,400 |
| 隔离推荐线总额 | $2,400 | $2,400 | $4,800，约 33,600 元/月 |
| 东京人数理想线总额 | $2,400 | $2,800 | $5,200，约 36,400 元/月 |

未计入：

- 原评估窗口内美西 2 个、东京 1 个 OpenAI API Key 账号的真实按量账单；最近一次分组核验中，美西仅保留共享 API 账号 60。
- 美西 Anthropic API Key 账号的真实按量账单。
- 两台服务器、数据库备份、监控和运维人工。
- 代理成本。现有 14 个独立 Codex 组若各配固定出口为 `14 × P`；历史 26 账号采购情景为 `26 × P`。
- 税费、汇率和订阅地区差异。

### 7.2 APIKEY.FUN 中转成本

2026-08-20 的公开价目中，ChatGPT 有两档：

- Codex Pro，仅限 Codex：0.5 倍率。Sol/5.5 输入 2.50 元、输出 15.00 元、缓存读取 0.25 元，每百万 Token。
- Codex Pro 外接版：0.6 倍率。Sol/5.5 输入 3.00 元、输出 18.00 元、缓存读取 0.30 元，每百万 Token。
- 外接版 Terra 为 1.20/7.20/0.12 元，Luna 为 0.60/3.60/0.06 元，5.4 为 1.50/9.00/0.15 元，5.4 mini 为 0.45/2.70/0.05 元。

来源：[APIKEY.FUN 模型价格](https://apikey.fun/pricing)

按最近 30 天当前 GPT 文本模型的输入、输出和缓存读取 Token 估算：

| 方案 | 美西 | 东京 | 合计 |
| --- | ---: | ---: | ---: |
| 外接版 0.6 倍率 | 31,997 元 | 15,981 元 | 47,977 元/月 |
| 仅限 Codex 0.5 倍率 | 26,664 元 | 13,317 元 | 39,981 元/月 |
| 自建隔离推荐线 | 16,800 元 | 16,800 元 | 33,600 元/月 |

估算只覆盖价目页能直接映射的 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`、`gpt-5.5`、`gpt-5.4` 和 `gpt-5.4-mini`。Sub2API 的 `gpt-5.6` 别名暂按 Sol 价格计算，缓存创建按普通输入价格计算，正式采购前需要供应商确认。以下当前流量尚未计入中转总价或兼容性未确认：

- `codex-auto-review`
- DeepSeek 系列
- Claude 系列
- GPT Image 系列
- 其他第三方模型和模型别名

“仅限 Codex”组的页面说明限制为 Codex App 和 Codex CLI。Sub2API 服务器到中转的调用是否满足该组资格不能自行假设。服务器侧通用接入应按外接版 0.6 倍率预算，除非供应商书面确认。

### 7.3 自建与中转怎么选

| 维度 | 自建账号池 | APIKEY.FUN 中转 |
| --- | --- | --- |
| 直接成本 | 推荐线约 33,600 元/月，另加 API 账单和运维 | 已定价 GPT 文本约 39,981 至 47,977 元/月 |
| 运维复杂度 | 高，需要账号、额度、分池、出口和恢复 | 低，一个上游 Key 即可 |
| 数据链路 | 自有节点后到模型提供方 | 多一个第三方中转，需评估代码和数据合规 |
| 模型兼容 | 当前已经覆盖多供应商和自定义模型 | 需逐项验证模型、端点、图片、流式和工具调用 |
| 故障域 | 自己管理，可多节点和多供应商 | 增加供应商集中故障和价格变化风险 |
| 扩容速度 | 采购和养护账号较慢 | 充值后按量扩容较快 |

建议采用混合方案：

1. 自建池继续承担稳定主流量，并先完成容量与隔离整改。
2. APIKEY.FUN 作为 5% 至 10% 灰度和灾备，只迁移已验证的 GPT 文本模型。
3. 连续验证 7 至 14 天，比较成功率、网关 503、上游 429/5xx、首 Token 延迟、总延迟、模型输出一致性和实际账单。
4. 如果中转节省的运维人工和故障损失高于每月约 6,400 至 14,400 元的直接成本差，再考虑扩大比例。
5. 不在一次切换中迁移 DeepSeek、Claude、图片和自动审查等未验证流量。

## 8. 其他需要补充的整改项

### 8.1 不要先降账号并发

账号当前配置为 10 至 20 并发，但本次数据没有请求级在途并发证据。最近 7 天已经大量出现无可用账号，直接把并发降到 2 至 4 会先扩大 503。正确顺序是：

1. 增加容量和建立分池。
2. 给 Key 和组设置 RPM、5 小时和 7 天限额。
3. 采集每账号实际并发、排队深度和额度消耗。
4. 再把并发逐步下调到实测 P95 加 20% 余量，而不是使用统一固定值。

### 8.2 共享 API 通道与 OAuth 分开路由、核算

OpenAI API Key 账号是按量成本，OAuth 订阅账号是固定成本和额度窗口。两者混在同一普通池会隐藏真实成本，并在订阅耗尽时产生不可控账单。

- 每个生产组的唯一 OAuth 用于 GPT/Codex 流量。
- 美西账号 60、70 与东京账号 22、23 只服务明确映射的共享模型，不计入 GPT/Codex 备用容量；当前没有已验证可用的 GPT/Codex API 兜底。
- 若后续单独接入 GPT/Codex 按量兜底，再配置明确的路由、日预算、周预算和成本告警。
- 账单按 `account_id + model` 监控，不能只看全站 `actual_cost`。

### 8.3 建立容量与故障看板

最少监控：

- 每节点、每账号池的成功数、总错误数、网关无可用账号 503。
- 每账号 5 小时和 7 天额度、下次重置时间、可调度状态。
- 每 Key 最近 7 天 Token、账号扇出数、RPM 和失败率。
- 每池当前账号数、可用账号数、实际并发和队列深度。
- API Key 兜底账号的实际美元成本。
- 中转灰度的实际人民币成本、首 Token 延迟和模型兼容性。

### 8.4 数据库查询和报表安全

本次最初的一条错误联表查询把账号、Key 和用量明细直接相乘，产生了大临时文件，并在美西短暂返回 `No space left on device`。查询结束后，美西磁盘仍有约 25 GB 可用，东京约 18 GB 可用，说明不是长期磁盘占满，但报表查询仍可能影响生产。

后续报表应：

- 先分别聚合账号、Key、组和用量，再按组 ID 连接。
- 优先使用现有日汇总表和复合索引。
- 为只读报表设置语句超时和临时文件上限。
- 避免在生产库直接执行多张大表的 `COUNT(DISTINCT)` 全交叉查询。
- 长期将报表迁到只读副本或离线数仓。

## 9. 执行顺序与验收标准

### 第一阶段：1 至 3 天

- 建立 Key 责任人清单，确认 30 天无成功用量的 Key。
- 给 Key、组和 API Key 兜底账号配置限额与告警方案。
- 复核现有 `codex-A` 至 `codex-J`（美西）和 `codex-A` 至 `codex-D`（东京）的额度、503 和可调度状态。
- 验证每个生产组只有一个 OAuth，Key 不跨组扇出；新增账号时创建独立资源组。
- 建立网关无可用账号 503 看板。

### 第二阶段：3 至 14 天

- 依据新窗口数据决定是否加号；每增加一个 OAuth，配套创建一个 `codex-*` 独立组。
- 复核美西 `8/8/9/9/9/8/8/8/4/2`、东京 `26/31/34/26` 的负载偏差，必要时只迁移 Key，不恢复多账号全交叉。
- 清理两节点 `codex-unused` 的共享 API 账号关联；恢复使用的 Key 先确认责任人，再迁回生产组。
- 开始 APIKEY.FUN 5% 至 10% 的 GPT 文本灰度。

### 验收标准

- 网关无可用账号 503 小于总请求的 1%，连续 7 天达标。
- 总错误率小于 3%，上游 5xx 小于 1%。
- 任一普通 Key 的 GPT/Codex 请求只命中所属组唯一 OAuth；共享模型请求不得反向计入 OAuth 账号。
- 任一账号池额度余量不低于 20%，按套餐容量归一化后的各池 Token 负载偏差不超过 20%，不直接要求 Pro 与 Pro Lite 的原始 Token 相等。
- 所有 Key 有责任人、用途、过期策略和限额。
- 隔离 Key 保留在 `codex-unused`；当前东京仍有 19 个 active Key，恢复调用前必须迁回生产组，并先清理共享 API 账号关联。
- 若启用 API Key 兜底池，必须具备日预算和成本告警。
- 中转灰度完成 Responses、流式、工具调用、推理强度、缓存、图片和模型别名兼容性记录。

## 10. 最终建议

1. 当前拓扑以“一号一组”为准：美西 10 个 Codex 生产组，东京 4 个 Codex 生产组；不要恢复旧部门组或多 OAuth 全交叉。
2. 美西当前生产组 Key 配额为 `8/8/9/9/9/8/8/8/4/2`，东京为 `26/31/34/26`；东京 19 个无用 Key 留在 `codex-unused`，美西当前无隔离 Key。
3. 是否加号暂不按旧报告固定采购数量决定；先看新窗口的额度余量、503、账号可调度状态和组级负载，再按“一账号一新组”扩容。
4. 共享 API 账号只服务明确映射的非 GPT/Codex 模型；先清理其在 `codex-unused` 的关联，不作为 OAuth 故障兜底。
5. 代理仍不是首要整改项。先完成限额、责任人、过期策略、IP 白名单和容量看板，再评估池级固定出口。
6. 自建与中转的价格比较仍按第 7 节执行；中转适合 5% 至 10% 灰度和灾备，不建议未经兼容性与账单验证直接全量替换。
