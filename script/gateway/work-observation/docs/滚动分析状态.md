# 滚动分析状态

更新：2026-09-22。4000/4001 已正式接入 candidate-r3 持续采集；GPU 已安装分析专用 v12.1 release 和人工复核页。当前 state 目录为 `/data/work-observation/cutover-live-candidate-r3`；8095/8096 只服务离线语义预览，不承接正式流量；4004/4005 仍分别是 Tokyo/US 的 Auto+Guard 灰度入口；页面不占用任何入口端口。复核页将 Guard 明确拦截、Auto 离线返回、技术失败和待分析积压分开，离线 Auto 结果不会被标成线上已执行。按当前采集隐私边界，`work-observation-analysis.timer` 暂停，v12.1 尚未形成新的真实样本训练批次。

2026-09-22 v12 部署后的采集复核：Tokyo/US 均 `capture_enabled=true`；15 秒内 captured/written 增量分别为 `3/3`、`17/120`，dropped、write_errors、loss_persist_errors 均为 0。4000/4001 及其命名空间内 18400/18401 探针均 HTTP 200；US watchdog 曾回退后已恢复 active。分析队列中的 20 个中断 claim 已回到 pending，达到 3 次上限的 8 个 pending 已转 deferred；当前没有 running claim。详细无正文证据见 `docs/evidence/2026-09-22-convergence-v12/README.md`。

2026-09-21 最新运行态：分析 worker 已切换到 `observation-review-20260921-v9`，每批最多 80 条、4 个并发、阶段结果 TTL 1 小时；4000/4001 路由仍为 `4000→18400`、`4001→18401`。最近批次处理 80 条，其中 35 条完成、45 条因 Auto `classifier_unavailable` 保留 pending；该错误来自候选分类器兼容性，不能归因采集器。阶段复用已用真实元数据样本做不写库诊断，确认只调用失败阶段；批次计数会在旧版本结果升级完成后出现。

| 范围 | 当前证据 | 可得结论 |
| --- | --- | --- |
| 正式入口持续采集 | candidate-r3 state `/data/work-observation/cutover-live-candidate-r3`；4000→18400、4001→18401；两地 watchdog `--seconds 0 --interval 5 --max-5xx -1` 无期限运行，容器/宿主配置SHA一致，正式 GET 均200。2026-09-20复核时重启US sidecar重新附着Nginx namespace后，US watchdog恢复active | 采集已应用到正式4000/4001；watchdog负责异常或停止时关闭采集并恢复原路由，单条projection/truncated只作为质量告警 |
| 正式用户请求及回复 | 2026-09-20 02:07 UTC 复核：Tokyo `captured/written=259/255`、US `736/23208`（Tokyo于01:59:50 UTC重启后重新计数；HTTP/WS一请求可产生多条记录）；两地 `capture_enabled=true`，`dropped/truncated/write_errors/projection_errors/loss_persist_errors/unavailable_bypassed_requests=0`，队列持续被写盘，数据写入统一200GiB卷 | 可以开始积累真实工作过程；当前样本窗口不足以报告长期任务分布、误拦率、模型适用比例或实际节费 |
| GPU真实入口与sidecar | 已确认4000→`106.14.254.110:9881`、4001→`106.14.254.110:9880`；candidate-r3由受限systemd托管，18400/18401回环探针均200，store已迁移到统一200GiB卷，当前capture开启 | 证明正式旁路可达并正在采集；线上配置仍由可验证manifest和回退副本管理 |
| 路径恢复与持久化 | Tokyo在2026-09-20重启后使用固定本地image ID恢复，sidecar与Nginx netns inode一致；四个candidate unit均`enabled/active`，ExecStop无权限错误。一次被取消的坏JSON请求产生`projection_errors=1`后，watchdog已改为将projection/truncated作为告警并保持采集，重启后两地持续active | 已覆盖Nginx重启导致的旧netns、镜像引用错误、宿主Docker NNP限制和单条坏请求误回退；这些是部署可靠性证据，不等于性能门槛已签收 |
| Tokyo/US短窗口（历史） | 两地各完成60秒、12次watchdog采样；新增Nginx 5xx均为0，自动disable与回退成功，最终原配置SHA恢复，candidate systemd active、capture=false | 回退链路通过；窗口只有采集器自检事件，未取得真实员工p95或任务质量样本 |
| Tokyo/US 600秒观察（历史） | 两地各完成118次watchdog采样；`nginx_5xx=0`、`failure=null`、`rollback_error=null`，manifest均`rolled_back`，最终capture=false且错误/丢失为0 | 有界运行和自动回退通过；当时的3/2条事件不能证明真实员工任务覆盖，p95与Guard/Auto质量未签收 |
| 四客户端原生流程 | dev.8共33项、69条HTTP交换；WS另6项、31条记录 | 工具/压缩/取消恢复/图片传输与采集通过；不代表模型任务质量 |
| 完整合成压测 | dev.9共8,820次、14,364条记录；优化后的多轮报告见`docs/evidence/2026-09-18-collector-r2/` | 响应和完整性通过；单轮p95仍受GPU共享负载波动影响，5%性能门槛暂不签收 |
| 候选采集器回归 | GPU候选二进制完成Go测试、race、vet和静态构建；独立生命周期smoke为6条事件（5完成、1中断），覆盖四客户端协议形状 | 正文完整、鉴权头未落盘、启停/重启/导出/分析通过；真实Codex复测因GPU安装的0.135.0-alpha.1不接受历史`--stdio`参数而阻断，不能归因采集器 |
| 实际采集到滚动索引 | GPU 分析库持续增量导入 Tokyo/US 正式入口的 `http_exchange`；2026-09-20 09:50 UTC 快照为 `total_jobs=10100`、`pending_jobs=9987`，WebSocket连接/消息另存为证据；滚动 timer 每5分钟运行 | 幂等、无新增正文副本；当前任务队列和每次运行元数据可在页面查看，费用/质量未知值保持 |
| 本地Guard/Auto语义分析 | 独立 `semantic.py` worker 已启用；GPU 8095/8096 候选端点健康200，使用两地0600专用 semantic Key；正式8093/8094未改动。v9 阶段复用 release 已切换 | 2026-09-21 01:30 UTC 批次为80条，35条终态、45条不可用；分析队列约9968条，分类器 `classifier_unavailable` 仍需单独修复；不可用任务继续 pending，结果是离线观察，不是线上拦截或训练结论 |
| 独立人工复核页 | `tools/review_dashboard.py`、`review/`静态页和独立标签库已完成本地回归；GPU 服务绑定 `192.168.64.16:8765`，无登录，仅受控内网；2026-09-22 dashboard-only release 为 `observation-review-20260922-v11-dashboard`，证据见 `docs/evidence/2026-09-22-dashboard-v11/` | 默认 Guard 队列只展示明确拦截且尚未人工处理的任务；保存后从待复核计数移出，仍可按人工状态查看历史。Auto 视图保留离线已返回结果；分页支持每页50/100条、首页/末页/页码跳转，详情列固定高度并支持上一条/下一条、`J/K`连续浏览、保存并下一条和跳过并下一条。正式入口到灰度入口固定为 Tokyo `4000 → 4004`、US West `4001 → 4005`；页面不监听或代理四个入口端口 |
| 大规模元数据报告 | 100万条合成事件，报告约6.8秒 | 统计不再把全部行装入Python；语义分析吞吐未知 |
| 存储故障 | 256MiB实验卷真实ENOSPC、释放和恢复；GPU统一200GiB卷已创建、挂载和`volume.py status`校验；新增body-free loss ledger | 业务响应不变、损失可见并可按事件定位；导出副本和分析临时文件仍需纳入同卷生命周期 |

运行后`health.json`记录当前健康，`hourly.json`记录增量窗口，`daily.json`同时包含近期与30天滚动趋势，`tick.json`记录积压、缺失、失效来源和待复核数量。正文保留在原采集记录，报告不复制正文。HTTP成功、被旧入口放行、候选选择DeepSeek均不能独立证明任务成功、安全或节费。

下一步是持续积累并离线生成训练候选数据集；正式分析/训练仍需独立的标注、验证集、灰度和回退流程。当前已批准的 worker 会把经过校验的采集正文发送到 GPU 私有 8013/8014 与 8095/8096 做离线复核，绝不访问正式8093/8094或公网端点；采集器不自动更新模型。指标必须同时标明样本数、客户端/任务覆盖及未知项。当前 v5 证据见[bounded replay v5](evidence/2026-09-20-collector-r4/README.md)。
