# 滚动分析状态

更新：2026-09-20。4000/4001 已正式接入 candidate-r3 持续采集，正式定时分析仍未启用。当前 state 目录为 `/data/work-observation/cutover-live-candidate-r3`；GPU上的Auto离线预览服务只服务语义分析实验，不承接正式流量；员工正文不会逐条发送给Guard/Auto。

| 范围 | 当前证据 | 可得结论 |
| --- | --- | --- |
| 正式入口持续采集 | candidate-r3 state `/data/work-observation/cutover-live-candidate-r3`；4000→18400、4001→18401；两地 watchdog `--seconds 0 --interval 5 --max-5xx -1` 无期限运行，容器/宿主配置SHA一致，正式 GET 均200 | 采集已应用到正式4000/4001；watchdog负责异常或停止时关闭采集并恢复原路由 |
| 正式用户请求及回复 | 2026-09-20 02:07 UTC 复核：Tokyo `captured/written=259/255`、US `736/23208`（Tokyo于01:59:50 UTC重启后重新计数；HTTP/WS一请求可产生多条记录）；两地 `capture_enabled=true`，`dropped/truncated/write_errors/projection_errors/loss_persist_errors/unavailable_bypassed_requests=0`，队列持续被写盘，数据写入统一200GiB卷 | 可以开始积累真实工作过程；当前样本窗口不足以报告长期任务分布、误拦率、模型适用比例或实际节费 |
| GPU真实入口与sidecar | 已确认4000→`106.14.254.110:9881`、4001→`106.14.254.110:9880`；candidate-r3由受限systemd托管，18400/18401回环探针均200，store已迁移到统一200GiB卷，当前capture开启 | 证明正式旁路可达并正在采集；线上配置仍由可验证manifest和回退副本管理 |
| 路径恢复与持久化 | Tokyo在2026-09-20重启后使用固定本地image ID恢复，sidecar与Nginx netns inode一致；四个candidate unit均`enabled/active`，ExecStop无权限错误。一次被取消的坏JSON请求产生`projection_errors=1`后，watchdog已改为将projection/truncated作为告警并保持采集，重启后两地持续active | 已覆盖Nginx重启导致的旧netns、镜像引用错误、宿主Docker NNP限制和单条坏请求误回退；这些是部署可靠性证据，不等于性能门槛已签收 |
| Tokyo/US短窗口（历史） | 两地各完成60秒、12次watchdog采样；新增Nginx 5xx均为0，自动disable与回退成功，最终原配置SHA恢复，candidate systemd active、capture=false | 回退链路通过；窗口只有采集器自检事件，未取得真实员工p95或任务质量样本 |
| Tokyo/US 600秒观察（历史） | 两地各完成118次watchdog采样；`nginx_5xx=0`、`failure=null`、`rollback_error=null`，manifest均`rolled_back`，最终capture=false且错误/丢失为0 | 有界运行和自动回退通过；当时的3/2条事件不能证明真实员工任务覆盖，p95与Guard/Auto质量未签收 |
| 四客户端原生流程 | dev.8共33项、69条HTTP交换；WS另6项、31条记录 | 工具/压缩/取消恢复/图片传输与采集通过；不代表模型任务质量 |
| 完整合成压测 | dev.9共8,820次、14,364条记录；优化后的多轮报告见`docs/evidence/2026-09-18-collector-r2/` | 响应和完整性通过；单轮p95仍受GPU共享负载波动影响，5%性能门槛暂不签收 |
| 候选采集器回归 | GPU候选二进制完成Go测试、race、vet和静态构建；独立生命周期smoke为6条事件（5完成、1中断），覆盖四客户端协议形状 | 正文完整、鉴权头未落盘、启停/重启/导出/分析通过；真实Codex复测因GPU安装的0.135.0-alpha.1不接受历史`--stdio`参数而阻断，不能归因采集器 |
| 实际采集到滚动索引 | 6条合成事件重复导入 | 幂等、无新增正文副本、费用/质量未知值保持 |
| 本地Guard/Auto语义分析 | `semantic.py` worker已完成隔离合成回归；GPU持久8095/8096运行候选二进制，默认关闭，尚无正式事件 | 正式8093/8094未改动；8095/8096现在使用仅限route-preview的0600专用 semantic Key 文件调用两地分类器，候选二进制已完成`go test`、`-race`、`vet`和静态构建，两个端口健康200、合成路由200并选出`deepseek-flash`；候选预览服务可独立回退，没有真实员工样本，不称正式工作已分析 |
| 大规模元数据报告 | 100万条合成事件，报告约6.8秒 | 统计不再把全部行装入Python；语义分析吞吐未知 |
| 存储故障 | 256MiB实验卷真实ENOSPC、释放和恢复；GPU统一200GiB卷已创建、挂载和`volume.py status`校验；新增body-free loss ledger | 业务响应不变、损失可见并可按事件定位；导出副本和分析临时文件仍需纳入同卷生命周期 |

运行后`health.json`记录当前健康，`hourly.json`记录增量窗口，`daily.json`同时包含近期与30天滚动趋势，`tick.json`记录积压、缺失、失效来源和待复核数量。正文保留在原采集记录，报告不复制正文。HTTP成功、被旧入口放行、候选选择DeepSeek均不能独立证明任务成功、安全或节费。

下一步是持续积累并离线生成训练候选数据集；正式分析/训练仍需独立的标注、验证集、灰度和回退流程。采集器本身不会把员工正文自动送往Guard/Auto。指标必须同时标明样本数、客户端/任务覆盖及未知项。历史失败和所有测量范围见[阶段证据](evidence/2026-09-17-collector-r1/README.md)与[候选预览部署证据](evidence/2026-09-18-collector-r2/semantic-preview-candidate-deployed.json)。
