# 独立采集器阶段验收 / 2026-09-17

范围：Go独立代理、原文采集、附件元数据、受限明文记录与本地分析。全部流量使用合成内容，未修改Sub2API、4000/4001或Auto/Guard测试入口。

## dev.8基准证据

2026-09-18的dev.9增加body-free缺失账本，并在GPU隔离目录完成gofmt、race、vet和`CGO_ENABLED=0` Linux构建；精确命令、退出标记和源码SHA见[`dev9-go-validation.json`](dev9-go-validation.json)与[`dev9-go-validation.log`](dev9-go-validation.log)。以下性能、原生和生命周期数据仍明确属于dev.8，不得把历史性能结果冒充为dev.9重新通过。

语义worker的隔离合成结果见[`semantic-dev9.json`](semantic-dev9.json)；它只记录决策元数据、不可用重试和正文不落分析库断言，不包含员工事件。GPU实际私网端点 smoke 见[`semantic-gpu-dev9.json`](semantic-gpu-dev9.json)：Guard 8011/8013/8014的合成探针均为200 allow。正式regional Auto 8093/8094在认证后返回404，因为它们明确关闭了`/v1/route`预览；随后用同一release启动临时回环预览8095/8096和独立状态/审计文件，分别使用东京、美西专用0600 Key，Guard 200/allow、Auto 200/complete，均选出`deepseek-flash`且队列清为complete。临时进程已停止，没有发送员工数据或启用正式入口。

36项Go顶层测试、race、vet及Linux/macOS构建通过；52项Python测试通过。四原生客户端33项流程通过，69条HTTP交换完整；独立Codex WS另6项通过、29条消息加2条连接记录无缺口。macOS制品SHA256：`83e70f87329f1e5d0ba31271e3fa0cbb1322c0d04dfc8ccb30c6434a9b8aa1a3`。源码取回SHA见`download-manifest-dev8-source.json`。

`native-dev8/`包括原生压缩、普通文件/多个工具读取、子任务、取消恢复、四端图片原生输入；图片请求在上游与采集摘要字节一致，附件仅元数据、图片负载未落盘，文本和工具标记保留。Codex/OpenCode含fork；WS另证原生工具续接、取消及恢复后的答案标记。合成模型回复不证明真实模型质量、Windows故障或所有文件格式已通过。`native-dev8-first/`保存新增验证器解析空行失败的记录，修正后重新运行四端，未覆盖旧失败。

`native-dev4/`补充历史Codex原生压缩与WS、Claude/Hermes复测；Hermes取消错误归因在该版已消除。dev.5起持久化采集开关在监听前加载；dev.8生命周期和滚动索引再验6条事件、5正常/1强杀中断、6条待语义复核引用，未创建额外正文副本。

## 长输入与WebSocket优化及失败

| 制品/数据目录 | 范围 | 完整性 | 性能结论 |
| --- | --- | --- | --- |
| dev.5 / 20260917-191241 | 63组、8,820次；128MiB副本预算 | 响应一致；25条截断、13次投影错误 | 失败，不能只凭记录数量称完整 |
| dev.6 / 20260917-192315 | 63组、8,820次；512MiB副本预算 | 14,364条记录，全部缺失计数0 | 64KiB WS合并总耗时+14.29%，未达标 |
| dev.7 / 20260917-193728 | 仅WS，18组、1,440次 | 11,424条记录，无缺失 | 64KiB WS合并总耗时+14.66%，未达标 |
| dev.8 / 20260917-194349 | 63组、8,820次；512MiB副本预算 | 14,364条记录，无缺失 | 下表多项或单轮超5%，未达标 |

全程各容器仍限2CPU/2GiB。dev.6减少外层JSON重复扫描，并提高正文副本预算；不能把容量问题消失全归因编码优化。8MiB记录封装微基准6.406ms→1.454ms只说明该函数，不能替代端到端验收。dev.7复用WS缓冲、按容量计预算并修复复用后的帧所有权；dev.8对经验证无附件且无转义的JSON保留原始字节，含转义/附件判别字段继续完整投影，反例已回归。

最新dev.8三轮样本合并的nearest-rank p95如下；全部轮次结果及CPU/RSS保存在各目录`summary.json`和`results.jsonl`，使用`tools/bench_report.py`可复算。

| 场景 | 首段开关增幅 | 总耗时开关增幅 | 新增整条路径总耗时增幅 |
| --- | ---: | ---: | ---: |
| 约1KiB JSON | 2.58% | 2.57% | 10.07% |
| 约1MiB JSON | 0.67% | 0.68% | 1.33% |
| 约8MiB JSON | 5.58% | 5.60% | 6.48% |
| 约1MiB SSE | 0.73% | 0.51% | 1.12% |
| 约8MiB SSE | 1.91% | 1.35% | 2.02% |
| 约4KiB WS，8往返 | 4.42% | 2.60% | 30.72% |
| 约64KiB WS，8往返 | 14.35% | 15.52% | 22.70% |

因此目前不接入4000/4001；不降低5%门槛、不用平均值或模型长等待掩盖采集开销，也不宣称Go已证明最快。固定容量数据卷尚未加入该压测。

## 滚动报告与容量故障

`report-scale-dev8.json`保存100万条合成元数据验证：30天报告约6.786秒，Python tracemalloc峰值24,640字节，整个测试进程maxRSS约102MB；后者含建库过程，不能说整个程序只用25KB。统计SQL聚合、SQLite文件排序，分布限制100项并报告省略数量，p95采用nearest-rank。扫描游标跨预算批次持久化并周期重扫，已有记录前缀不再阻止新记录导入。`semantic.py`本地worker的代码和合成回归已补齐，但默认关闭，未产生真实员工任务质量结论。

`volume-smoke-dev8.json`验证新建256MiB独立ext4文件卷的真实ENOSPC：磁盘满时三次业务响应保持一致，两次采集写入故障有计数和ID；释放本实验填充文件后不重启即可恢复采集。实验后卸载，未修改宿主XFS选项、其他卷或业务入口。首次losetup缺少`--list`导致挂载核验失败，按UUID和镜像路径核验后卸载，失败证据保留；工具已修正并补后置验证失败卸载回归。

固定容量卷只完成独立工具及缩小故障演练；正式200GiB卷、专用用户、所有正文副本/分析临时文件同卷、服务挂载依赖和最终性能尚未集成，不能计为全局容量验收完成。

## 完整采集性能 dev.2

`20260917-182814/`保存三轮36组、7,020次测量请求的逐请求延迟、CPU/RSS及最终状态；SHA校验取回清单见`download-manifest.json`。计入预热的2,724个HTTP交换/WS连接，产生8,100条记录；其中WS消息单独保存，不能将written与captured直接相等比较。零转发内容错误，零采集丢失/截断/投影错误/写错误，结束队列和活动请求为零，记录约1.49GB。

| 场景 | 首段p95采集增幅 | 总耗时p95采集增幅 | 新增整条代理路径总耗时增幅 |
| --- | ---: | ---: | ---: |
| 约1KiB JSON | 3.08% | 2.99% | 10.96% |
| 约1MiB JSON | 0.95% | 0.95% | 0.61% |
| 约1MiB SSE | 1.53% | 0.81% | 0.36% |
| 约4KiB WS、8往返 | 1.96% | 4.69% | 36.70% |

表中是三轮逐请求样本合并后、nearest-rank `ceil(N×0.95)`分位数。开关增幅比较Nginx→同一代理的on/off；整条路径比较Nginx→开启采集的代理与原Nginx→上游。绝不混用这两个分母。

**还未通过最终性能验收。** 单轮小JSON首段/总耗时分别+5.57%/+5.81%，单轮WS总耗时+9.64%；未隐藏这些失败。当前完整实验也未覆盖8MiB、长时间并发和所有附件。不能只以合并结果低于5%宣布可上线或“Go最快”。dev.3/dev.4后续修改未复用为同一制品性能通过。

`20260917-181156/`保留首次探索数据：请求内容通过，但逐组status可能读取到一秒前的值。后续等待预期累计captured、活动请求与队列一起排空后复测；该首次结果不作逐组完整性证明。

## 原生客户端 dev.3

`native-dev3/`保存原生客户端检查与采集计数，无正文。Codex0.154.0、OpenCode1.18.4、Claude Code2.1.233及Hermes0.21.0共27项流程检查通过。内容和模型回复由本地确定性服务提供，真实客户端和真实采集器运行；这不验证真实业务模型质量，也不能解释Windows同事的历史超时。

- Codex：6项，9条交换；两次读取工具、子任务、fork、取消、恢复。此驱动尚未包含原生Codex压缩和WS。
- OpenCode：7项，12条交换；含原生压缩、工具、子任务、fork、取消及恢复。
- Claude：7项，14条交换；含原生压缩、读取、Agent、取消与恢复。额外/api/hello探测由合成上游返回501，原样记录，不能称所有HTTP请求均200。
- Hermes：7项，28条交换；含原生压缩、读取、delegate、取消及恢复。多数业务调用UA是通用OpenAI/Python，按原UA保留并标unknown，不伪装成Hermes。

四端共63条交换，四次主动取消，采集计数63/63，丢失/截断/写入/投影错误均0。Hermes取消发生在响应头前，dev.3误附`upstream_transport_failure`且状态502；失败原始指标保留在hermes.json，dev.4修复及回归另记，不能覆盖旧证据。Claude实际头显示X-Claude-Code-Session-Id/Agent-Id，dev.4新增按Key隔离的关联，仍标client_claimed。

## 开关与服务生命周期

`disable-regression-before.log`保存旧实现关闭采集后仍复制旧会话正文的失败。远端SSH在Go测试失败时仍返回外层0，后续构建明确回传并验证`__WO_EXIT__`，不再以SSH进程状态独立判断成功。

dev.2、dev.3服务进程已分别验证init、默认关闭、开关、强杀恢复、校验导出及SQLite分析：每次6条事件，5正常、1process_interrupted。dev.3进一步通过滚动分析重复导入：6条事件不重复、6条待本地复核引用、不创建正文副本，费用与任务质量均unknown；这些历史dev.8滚动证据中的pending值保持原样，不回写历史结果。当前worker已在GPU临时回环8095/8096用两地专用Key完成认证合成复核，结果见`semantic-gpu-dev9.json`。

## 待完成

原生WS远端压缩/opaque状态组合、剩余故障矩阵、全局200GiB部署与导出生命周期、可验证的完整会话关联、正式调度与端点配置、性能稳定性及发布回退。worker代码、GPU临时端点认证和合成回归已完成，但默认关闭；当前无正式入口采集或真实员工分析结果。
