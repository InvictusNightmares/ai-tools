# CHANGELOG

## 2026-09-20：work-observation 旁路正式接入 4000/4001

- 按用户单独授权，将 GPU 上的 candidate-r3 采集旁路接到现有 4000/4001 内部路径：4000→18400、4001→18401；Sub2API 源码、镜像、鉴权、模型选型、Auto/Guard 业务路由均未改动。两地 sidecar 与无期限 watchdog 均由 enabled systemd 实例托管。
- 采集路径在 Nginx 只读 bind mount、容器重启和 Docker network namespace 重建后会重新探测并绑定；员工正文只写受限采集卷，`semantic.py`、Guard/Auto 正式调用和训练调度保持关闭。
- 一条被取消的畸形正文请求曾产生单次 `projection_errors` 并触发旧watchdog回退；事件已保留缺失标记，确认不是sidecar或存储故障。watchdog现将 `projection_errors`/`truncated`作为质量告警，仅采集完整性故障回退，并已通过57项本地回归。
- 2026-09-20 02:14 UTC 复核：两地 live/sidecar active，18400/18401 与 4000/4001 均200，Tokyo `captured/written=490/485`、US `1067/27663`，采集错误/丢失和warning计数为0。该旁路接入不等于P17的Auto/Guard业务切流，也不等于性能或训练验收通过。

## 2026-09-18：独立采集性能优化与持久语义预览

- `work-observation` dev.9增加采集副本缓冲复用、WebSocket完整帧就地解掩码和分段落盘，GPU隔离完整基准保持内容正确、8,820次转发和14,364条记录零缺失；共享GPU下单轮p95仍有超过5%的样本，正式4000/4001接入继续阻止。
- 独立语义分析预览已由同一Auto release部署为GPU回环systemd模板：东京8095、美西8096；专用Key只由0600 `auth_file`读取，状态/usage/audit/natives均与正式服务分离。两地健康、调用方认证和重启通过；早期临时合成进程的Guard 200/allow与Auto 200/complete结果保留，但持久服务新会话仍受分类器调用方凭据透传阻塞。8093/8094仍返回route-preview关闭的404，未发送员工正文。
- 持久服务复核发现新会话分类器认证/可用性不稳定；两地专用semantic Key直连分类器均200，但Auto regional模式拒绝共享`AUTO_CLASSIFIER_TOKEN`注入。候选代码增加仅route-preview可用的0600 `AUTO_CLASSIFIER_TOKEN_FILE`，启动器按东京/美西指定对应分类器端点；生产regional服务仍只透传调用方凭据。远端最新审计需待跳板恢复后复核。旧Python 3.6 `text=`启动失败已修复并保留日志证据。
- 远端审计进一步显示失败发生在东京复核模型的单次传输阶段：前置 Luna 200 成功，Sol 记录为无HTTP状态的 `classifier_invalid_or_unavailable`，随后同一请求重试可恢复。候选 classifier 现在只对无HTTP状态的 transport error 做一次75ms重试；HTTP错误、非法模型输出、取消仍失败关闭，并新增回归覆盖。
- 候选二进制已在GPU隔离目录完成gofmt、全量测试、race、vet和静态构建；仅8095/8096预览服务切换到候选路径并完成健康、专用Key认证和合成`/v1/route`验证，两地均选择`deepseek-flash`。正式regional入口仍关闭route-preview，4000/4001和Sub2API未改动。

## 2026-09-17：独立工作采集开发与性能选型

- 新功能独立位于`../../work-observation/`。按用户后续确认，采用GPU独立采集代理，不修改Sub2API源码/镜像/升级流程；取消原两地缓冲与正文转存设计。先隔离验证，通过后才接入4000/4001内部转发，正式Auto/Guard切流仍不执行。
- 用户最新确认取消正文脱敏和落盘加密，普通文本/工具内容原样存GPU受限目录，鉴权头不采集；正文30天、附件仅元数据、200GiB边界保持。目标为Guard/Auto完整分析请求、回复和工作过程；元数据统计仅作辅助。当前开发中，尚未部署。
- 原文采集草稿已移除正文脱敏，新增完整内容证据导出；19项Python、6项Go及vet通过。性能对照120组/37,560次内容校验成功，但仍有采集开关p95增幅超过5%的场景，未选择正式制品；完整采集、真实离线判断与四端验收未完成。

AI Gateway 的统一变更账本。当前状态和待办见[实施总计划](智能网关与Guard分阶段实施总计划.md)，维护方法见[维护指南](维护指南.md)。新版本/日期放在前面；末尾迁移的历史执行表保持时间正序，不重写历史测试结果。

## Unreleased

### 2026-09-18 / 独立采集 dev.9 GPU Go 回归

- 缺失账本改动已同步到 GPU 隔离目录；`golang:1.27.1-bookworm` 中的 gofmt、`go test -race ./source/... ./cmd/...`、`go vet ./source/... ./cmd/...` 和 `CGO_ENABLED=0 go build` 全部通过，远端退出标记为 `__WO_EXIT__=0`。证据见 [`dev9-go-validation.json`](../../work-observation/docs/evidence/2026-09-17-collector-r1/dev9-go-validation.json)，未接入4000/4001、4004/4005或Sub2API。

### 2026-09-18 / 独立采集本地语义复核worker

- `work-observation/tools/semantic.py`已接入滚动tick：读取完整校验事件的请求和可见回复，在`offline_analysis`平面分别调用配置的私网Guard `/v1/preflight` 与 Auto `/v1/route`预览，保存allow/block、风险/原因、分类、有效模型及不可用重试状态；regional Auto认证通过权限为0600的专用`auth_file`注入请求头，密钥不落库、不写日志；不复制正文到分析SQLite，不改变线上路由、隔离、缓存或业务工具。正式regional Auto 8093/8094在认证后返回404（服务关闭route-preview）；GPU随后用同一release的临时回环8095/8096、独立状态/审计/usage文件和两地专用Key完成认证离线复核，两地均Guard 200/allow、Auto 200/complete并选出`deepseek-flash`，临时进程已停止。52项Python回归通过。
- 默认仍关闭，配置加载时拒绝公共端点；Guard/Auto block是可记录的离线观察结果，不冒充线上决策。52项Python回归覆盖放行、block终态、不可用、陈旧任务恢复、端点限制、认证文件边界和正文不落库。GPU隔离认证已通过但未发送员工数据；正式调度、可靠完整上下文、全局容量和4000/4001接入仍未执行。

### 2026-09-17 / 独立采集dev.8四端及磁盘满补验，尚未接入

- 独立模块新增WS缓冲/正文编码优化、启动前恢复采集开关、元数据查询和有界滚动报告；36项Go/race/vet、44项Python及Linux/macOS构建通过。四原生客户端33项流程/69条交换、Codex WS6项/31条记录通过；附件原生透传、仅元数据落盘，文本及工具内容保留。未验证真实模型任务质量或Windows历史故障。
- 最新完整63组/8,820次内容一致、14,364条记录零缺失；8MiB JSON和64KiB WS的p95开销仍超5%，不执行4000/4001接入。保留dev.5截断和各版性能失败。百万条合成报告和256MiB文件卷真实ENOSPC演练通过，容量卷工具尚未集成正式200GiB部署。
- 本轮只修改work-observation及文档、运行GPU独立实验；Sub2API和4004/4005策略/缓存保持。现有入口无需回滚；实验卷已卸载，失败证据保留。仍欠语义worker/可靠关联、全局副本生命周期、持久缺失定位、最终性能及发布回退。详见[采集变更账本](../../work-observation/CHANGELOG.md)。

### 2026-09-17 / 独立采集器dev.4与滚动分析

- work-observation独立Go采集服务已实现HTTP/SSE/WS原文旁路、附件元数据、明文限权保存、校验导出、30天清理及故障计数；Sub2API源码/镜像和正式入口保持。启动存储异常停采但继续业务转发，默认关闭。
- 四原生客户端dev.3流程27项通过，采集63/63交换；发现并修复响应头前主动取消误记上游失败，补Claude会话/子任务关联。当前30项Go/race/vet、34项Python及Linux/macOS构建通过；dev.4原生补验进行中。
- 完整采集36组/7,020次响应一致、8,100条记录零丢失，但单轮尾延迟仍超5%且新增代理本身有开销，不声明性能/发布完成。独立增量与滚动分析模板已通过合成闭环，语义worker未完成、正式调度未启用。详见[独立采集阶段证据](../../work-observation/docs/evidence/2026-09-17-collector-r1/README.md)。
- 待完成：全局存储/副本生命周期、持久缺失定位、上下文语义分析、最终性能/原生WS附件/回退验收。未接入4000/4001，未改4004/4005或Guard/Auto线上策略。

### 2026-09-17 / R05客户端超时与R06 Guard语义误判，已诊断未修复

- R06按Key5确认14:43首次medium/Political语义拒绝，随后23次会话隔离拒绝；正常安装包分析原脱敏请求在真实Qwen完整六块复现，第2块误判。凭据硬规则不是本次根因；所有block统一隔离24小时放大了影响。多项正文保持的呈现对照仍失败，没有发布未经验证的提示词调整。
- 已提出争议结果自动本地独立复核；用户正询问工作方式，未确认策略。另一已运行本地模型承担GPU0业务，尚未调用或接入，需验证准确率、完整覆盖、失败关闭及负载。通知不等同于自动复核。详见[R06证据](evidence/2026-09-17-guard-r06/README.md)。
- R05夏斯建Key77的Windows CLI0.146.0超时仍未定位；用户确认health可达。本机同版macOS原生WebSocket/HTTP均成功且同样出现auto元数据警告，证明警告不能单独解释超时。当前终端NO_PROXY对照等待结果，详见[R05证据](evidence/2026-09-17-codex-r05/README.md)。
- 本轮只读运行状态、隔离诊断并补文档；未改代码、配置、策略或缓存，未解除会话隔离，未部署。两地保持release v31/Guard v29-r19，无回滚动作；4000/4001/4003及原Guard/vLLM不变。用户正文只留服务器受限位置，未送外部模型或进入仓库。

### 2026-09-17 / R04 多人选型核对及格式修复v31已两地采用

- 最终14:58快照按Key/网关request ID核对，真实用户Key5/60共47业务：DeepSeek4、Luna2、Terra41；22次分类/复核另计，内部验收Key4排除。首份14:40的37次全Terra保留；付晓镇新增10次中4次DeepSeek发生于v30、早于本轮部署。共69/69调用精确匹配本人账单，新增15/15的Token差异0。
- 产爱军28次中25次工具续接，27条multi_step debugging和1条工具回合中的simple summary；旧日志不保存原文/输出格式，不能拿评估标签证明任务难度，也不能归因全部由格式问题引起。付晓镇新增两次简单任务仍受structured限制，但未知实际是否要求schema。
- 修复Chat response_format=text、Responses verbosity/空值误算structured并排除DeepSeek；补Messages output_config.format遗漏。三协议共用格式判定、保留原参数；审计仅加text/json_object/json_schema/unknown枚举。分类提示词/schema及主分类器不变，策略r4/业务v31失效旧缓存/工具评估；状态兼容，回退v30恢复已知缺陷。
- 旧实现7种格式回归失败；当前736 Go/42 Python、race/vet/六构建通过。两地普通文本4场景Luna→DeepSeek，实际JSON Schema4场景仍Luna。初验两次Responses因验收脚本把reasoning_text混入答案而断言失败，原结果保留；修正只读output_text后两地补验通过。两地入口各17/17；真实验收46/46精确账单和Token匹配，UA保持。
- candidate-20260917-r19-native-v31已采用4004/4005，仅Auto更新，Guard复用v29/r19；源码SHA e8bab6ab957acdb787f74461c84ef2e11362b06a189f494b808774a8d7496efc。美西备份release-20260917-145257-439490、东京release-20260917-145324-862409；Key/状态/受保护服务不变。未重放用户项目正文、未新增四端开发画像或Guard30分钟负载，4000/4001/4003未切流；分类成本仍有优化空间。见[R04完整证据](evidence/2026-09-17-routing-r04/README.md)。

### 2026-09-17 / R03 v30已更新两地测试入口

- 4004/4005按用户确认是可随时修改的多人测试入口，部分人员使用不视为正式切流。美西Key5有43条Guard前模型名404，17条精确关联HTTP /responses；旧日志缺模型名和操作类型，不能将43条都认定为压缩。9条业务全Terra，简单summary/quick_qa受sticky_quality_floor保留高档。
- 根因与修改：已知GPT/DeepSeek目录模型名现在接受为Auto输入别名，与Claude兼容一致；继续由Guard和Auto决定实际模型，未知名称仍404。普通信息阶段在置信度/不确定性/失败/混合目标条件达标后允许用户边界降档，工具回合、流式和原生能力限制保持。审计补协议、操作类型、会话及拒绝模型摘要、结构化任务评估；无原始正文。
- 分类提示词/schema和Luna/Sol选型不变；策略r3、业务修订v30使旧缓存/工具评估保守失效。会话格式兼容，回退v29会恢复原缺陷。改动涉及gateway/http、route_state/task_routing、route_audit、语义版本、release元数据及回归，完整清单见证据。
- 旧源码10个模型名/压缩组合、5类降档及诊断回归失败；首轮完整测试发现旧“拒绝Astra”契约断言，改为拒绝未知模型后完整719 Go/42 Python、race/vet/六构建通过。两地真实压缩404 4/4变为200 4/4，原生Codex旧式/v2各5项共20/20；两地三语12请求成功、6次同任务总结均Sol→DeepSeek，原因routine_phase_downshift。入口各17/17。
- 两地release candidate-20260917-r19-native-v30，只重建Auto，Guard复用v29/r19；Key/状态及受保护服务不变。美西备份release-20260917-142643-754268、东京release-20260917-142702-251698。源码SHA 013b8d821aba7b0aa9904a88f64b85ee79eb06c8a85750f332418b961811aafd，与本地冻结清单一致。
- 独立账单78/79精确匹配、Token差异0、已知成功漏账0；未匹配项是东京Luna主分类502、用量未知且无账单，Sol复核及业务随后成功，不声明零费用或取消。真实Codex/合成客户端UA保持。用户原17/17账单也已关联：业务0.2489548 USD，分类/复核0.54741 USD，长上下文分类成本仍有优化空间。
- 补验两地真实上游Sol的加密compaction状态进入网关，再压缩并以auto续接，均200且精确回忆原标记/流程；首次普通回复无加密状态、东京一次召回断言失败、直连非流式502均保留，最终使用原生SSE取到真实状态。附加探针不混入前述79次账单统计。
- 用户Desktop alpha原会话未复测，不能把CLI实测当成本人旧43次错误逐条闭环。Guard30分钟负载未重跑；正式4000/4001/4003未切流。全部结果、失败和限制见[R03证据](evidence/2026-09-17-codex-r03/README.md)。

### 2026-09-17 14:08 / R02误拦截修复v29/r19已两地采用

- r18原请求回放88/88与两地入口各17/17通过，但原生OpenCode合成开发流程在两地读取函数参数类型时仍403；后续被会话隔离。五种函数签名在r18回归全部失败，八种凭据反例保持阻断，失败证据保留。
- r19只识别可见Kotlin函数签名内String/String?参数类型；扫描从类型结束位置继续，防止无空格相邻凭据被宽匹配吞掉。单等号默认字面值、裸配置、类型后缀伪装均仍检查。补camelCase字段名与正文完整性回归，完整691项Go/42项Python、race/vet/六构建通过。同发布二进制真实Guard103/103，包括用户原请求三次和两地新失败请求；队列/审计/tokenizer故障均失败关闭。两地仅更新Guard到v29/r19，Auto保持v27；东京备份release-20260917-140219-091056、美西release-20260917-140318-953044，密钥/状态/受保护服务保持。回退旧制品兼容状态但会恢复已知误拦，正式入口不切流。

- 同一合成Kotlin/Markdown项目，两地原生OpenCode1.18.4均完成读取和续问，工具错误0、403为0；业务5/6轮均DeepSeek none，逐轮Guard5/5与6/6匹配。东京入口17/17，美西首轮health瞬时503、其余16项通过；后续两地health各3/3为200，不抹去首次失败。
- 最新33/33调用精确匹配两地Sub2API账单，Token差异0，真实OpenCode UA保持。103项真实矩阵、故障、首次失败及版本边界见[R02证据](evidence/2026-09-17-guard-r02/README.md)。r19未重跑30分钟负载，r17结果仍仅为历史制品范围。

### 2026-09-17 / R02首次Markdown修复r18及原生后续失败

- 用户原生 OpenCode 两轮工具读取后触发 `secret_input_not_allowed`；受限审计确认 CHANGELOG 中反引号包裹的空值比较被误识别为凭据赋值。旧 r17 四种 Markdown 回归全部复现；六种凭据反例仍阻断。
- 仅补充 null/undefined/nil/布尔等值比较的 Markdown 结束边界，保留字符串比较、单等号赋值、相邻凭据检查，不豁免文档/工具/角色。安全规则升 r18 使旧 Guard 决策缓存失效，正文契约、Auto 分类策略及业务缓存修订保持。
- 完整676项Go/42项Python、race/vet及六构建已通过；同制品真实Guard88/88（含原请求三次正文不变放行），队列/审计/tokenizer故障均失败关闭。两地已采用，仅Guard重建，入口各17/17。原生合成Kotlin流程随后在两地都发现函数参数类型被误当凭据，续问因会话隔离继续403；保留失败，后续r19已补充修复和原生回归。范围限 4004/4005，可回退 v27 配套制品，4000/4001/4003 不切流。

### 2026-09-17 12:18 / R01选型修订v27已两地采用

- 用户真实 OpenCode 只读总结暴露难度膨胀与重复分类：五轮业务 0.0170032 USD，十次分类 1.2082364 USD。旧回归复现 routine 被选 Luna、三协议同步/流式规划均重复分类；原生业务全程 Luna low，UA 正确，不能归因 DeepSeek 能力不足。
- 候选 semantic-v10 增加 summary/extraction 语义，质量/能力门槛后按候选档位选择；独立复核可纠正两方向偏差。已验证供应商工具绑定仅在目标/历史摘要与策略匹配、纯工具结果且无原生媒体时复用评估，逐轮 Guard 与身份/能力检查保持。
- 分类策略版本失效旧分类缓存；工具状态只新增可选摘要/评估字段，旧绑定保守重分类，回退旧制品可忽略新字段。新增审计 task_type、selection_policy、target_model_tier、tool_binding_reused；不记录原始任务或工具正文。
- r2 冻结源码 `16a4d40e5c7dc11de4f006d631eed838f68c370e3530255d2574b30c4ba69678` 完整 664 Go/42 Python、race/vet/六构建通过。第一候选旧保留题 45/48=93.75% 未部署；r2 旧保留题47/48=97.92%、开发12/12、前一新题24/24、后冻结12组新边界题12/12。旧题已用于诊断，不能称为未见过的盲测；一致率不等于能力正确率。
- 两地原生 OpenCode1.18.4 对完全合成5个已跟踪改动+751新增文件完成多轮读取，业务为DeepSeek none，工具续轮复用分类。保留首次权限/输出问题、诊断shell及普通shell的不同范围；普通shell东京覆盖全部，美西一轮漏tests/evidence目录，不能宣称每次总结完整。两项架构复核问题（升级受冷却阻挡、summary混合audit标签走低档）已修复并回归。
- v27已按备份/排空流程采用4004/4005，仅替换Auto；Guard复用v26/r17原二进制，密钥/状态及受保护服务保持。两地入口各17/17；隔离59/59、部署后33/33账单精确匹配，Token差异0，原生UA正确。备份东京`release-20260917-120326-647537`、美西`release-20260917-120519-665391`，可回退v26兼容制品，不覆盖新增状态。
- 最终本机原生OpenCode直连两地分别17.285/23.134秒、工具错误0；业务5/7回合全部DeepSeek none，Luna各2次（正文/标题），工具续轮3/5次复用且Guard逐次有决策。美西内容覆盖正确；东京文件数量、类型和输出语言存在质量问题，未伪称每次正确。原生工具与实际回答范围详见[R01证据](evidence/2026-09-17-routing-r01/README.md)。
- F01同一r2规则的DeepSeek主分类对照：原180/180结构成功，开发12/12、保留46/48=95.83%，前一新题24/24；本轮达单次门槛但不声称连续稳定性，两个语言边界差异保留。线上按用户选择保持Luna。
- 隔离候选容器和六个临时客户端凭据副本已退役，日志/状态/账单保留。正式4000/4001/4003不切流；DeepSeek主分类优化仍与业务选型分开。

### 2026-09-17 10:59 / 本期开发验收收尾（不切正式入口）

- r17同制品、同GPU真实模型与生产参数的独立18017负载：10RPS连续30分钟18,000/18,000，p95 93.61ms，预检队列max 0ms，窗口内OOM 0，分钟采样最低显存余量20.38%；结束后独立进程退出。制品SHA与两地运行Guard一致；与线上4004/4005各17/17入口验收分列。新源码644 Go/42 Python及真实Guard67项/故障矩阵已通过，旧混合负载p95 483ms限制保留。
- P01–P16除用户排除的第二故障域外完成对应开发/验收范围；模型画像如实保留复杂启动题全部有漏项、仅Terra自行结束及设备未验证，未代修模型产物。Luna/v9达到95%一致率门槛；DeepSeek实验未达标，不切主分类器。
- 最新165次独立账单全部准确匹配，临时身份及10个固定模型服务已退役，运维四timer active、导出剩余0；默认保留日志/账单，不自动删除。文档和脱敏证据同步，4000/4001/4003与原Guard/vLLM保持，正式切流不执行。


### 2026-09-17 10:42 / 模型画像与本轮对账收尾

- 三项目×五固定候选画像已完成：小源五份通过HAP/行为；提醒四者自行结束，Terra轮数上限；启动五份APK/21项既有JVM均通过，但全部有需求漏项，仅Terra自行结束。Astra的原403请求在r17续跑6回合，总计仍24轮，无额外提示或评审者修复；不能把画像完成写成模型全部通过。
- 最新两地入口与五候选7范围共165/165精确匹配独立账单，Token差异0、已知账单缺失0；历史取消中途usage保持原值。只归档元数据/SHA，不归档源码、原始提问、工具正文或响应。
- 两地升级后入口各17/17；健康/用量四个timer active，分钟导出成功、剩余0、告警空。10个临时固定模型进程完成后已停止，业务容器不变，日志/状态/账单保留；测试组/用户/Key先前已退役。
- 当前仅等待P07 r17制品连续30分钟负载；前版普通负载通过和混合长输入延迟限制分列。代码组件SHA与已部署制品一致，文档142个本地链接检查通过。正式4000/4001切流不执行。

### 2026-09-17 10:31 / release v26、Guard r17 已两地采用

- 比较运算修复完整644项Go/42项Python、race/vet及六构建通过；真实Guard67/67，队列1/100ms下15/16正确503，审计磁盘满与tokenizer异常均失败关闭，零业务上游。
- 两地仅重建Guard，Auto v24二进制相同，密钥/状态/受保护服务保持。制品`candidate-20260917-r17-native-v26`，源码SHA `c99fd5ea6382c0b9fd711691516061df828b04a30606edb11ce7c88688af110b`；东京备份`release-20260917-102929-293311`、美西`release-20260917-103007-502405`。可回退v25 Guard；原4000/4001/4003不变。
- 首次部署校验因Python生成未列清单pyc而拒绝，尚未激活；仅移除明确归属本次的两个缓存文件，采用`python3 -B`后升级成功，制品清单保护未绕过。
- Astra从已明确403且未调用业务上游的原请求继续，共享原24轮及30分钟预算，不添加修复提示、不代改产物。新版30分钟负载使用相同真实GPU/模型和生产参数的独立18017端口，制品SHA一致，入口两地另验；所有范围明确区分。

### 2026-09-17 10:22 / 凭据比较运算误判修复候选

- 第四轮 Astra 在第18个成功回合后新增 `accessToken == null`，下一请求被硬规则当成凭据赋值拦截；五项比较回归在旧实现全部复现。候选仅识别与 null/undefined/nil/布尔关键字的等值比较，字符串比较、真实赋值及相邻凭据继续检查，不按文件或角色豁免。
- 安全规则升r17使决策缓存失效，正文契约、Auto及业务缓存策略不变；候选待完整/真实验证及两地采用，可回退前一Guard制品。原4000/4001/4003保持。
- r16已完成30分钟普通10RPS，18,000/18,000，p95 98.32ms；旧混合长输入延迟失败保留。Astra此次属于基础设施中断，不计模型能力失败；其他候选产物按冻结要求单独评分。

### 2026-09-17 09:58 / 模型画像工具 SSH 传输修复

- 第三轮五模型均因逐回合新建 SSH 后连接关闭而中断，无模型产物改动；完整请求单独复现200，合成16KB/160KB传输均通过，不能归因模型或正文大小。失败保留，模型质量不评分。
- `tools/model_profile.py`改为每个候选复用一个受控 SSH exec 会话，经stdin发送结构化数据、Key仍留远端；设置建连/保活/响应超时，无端口转发、无配置变更。任何投递不确定均停止，不自动重连或重放。补多请求原字节、HTTP错误、丢失响应不重试测试。该变更只影响验收工具，不改变已部署Auto/Guard二进制。

### 2026-09-17 09:49 / release v25、Guard r16 已两地采用

- Guard Kotlin声明误判修复完整642项Go/40项Python、race/vet和六构建通过；真实55/55，队列1/100ms下15/16正确503，审计磁盘满与tokenizer异常均失败关闭，零业务上游。
- 东京/美西各仅重建Guard，Auto v24二进制不变，密钥/状态/受保护服务一致，升级后入口各17/17。新安全规则r16使旧Guard决策缓存失效；业务分类仍Luna/v9，正文契约不变。
- 制品`candidate-20260917-r16-native-v25`，源码SHA `15820d57cf61218702150f5a78444abc3a5bfc0aef25a5904624617bf470222a`。两地备份分别`release-20260917-094720-133306`、`release-20260917-094811-833247`，可用前一v24制品回退Guard。
- 第三轮五模型从相同149文件历史副本启动，仍24轮/30分钟相同预算；前两轮失败及产物保留。r16的30分钟10RPS负载与固定模型开发验收并行运行，完成前不借用r15成绩。

### 2026-09-17 09:39 / v24 已采用；Guard r16 修复候选

- 两地 Auto v24 已版本化采用，入口各 17/17；仅 Auto 重建，密钥、状态和受保护服务保持。原生审核真实允许/拒绝均已有两地证据，省略 effort 时保留发送 unknown，不把上游默认 medium 误报为不一致。普通负载 30 分钟 18,000/18,000，p95 94.25ms；混合长输入窗口的 483ms 失败仍保留。
- 用户已明确批准五个模型经东京接收启动任务的脱敏历史副本。第二轮 Sol/Terra 读取 Kotlin 登录代码触发凭据硬规则；六项新回归在旧实现失败，确认普通 val/var 运行时引用与类型声明被误认为真实密码。候选仅识别带声明上下文的成员引用/无参调用，带类型的真实赋值仍检查；安全规则升 r16 使旧决策缓存失效，正文契约不变。
- r16 候选已完成642项Go/40项Python、race/vet/六构建及真实Guard55/55，冻结新制品后复验故障与入口；尚未部署；4000/4001/4003不变。两个首请求 SSH 传输失败无业务调用记录，受控复测均 200，失败不计模型能力结论。

### 2026-09-17 09:18 / 原生审核拒绝通过东京，未知effort审计边界修正候选

- 原拒绝夹具使用多层shell引号的Python写文件提议，Guard先于审核拒绝。改为与实际Codex常见输出一致的简单printf同效写入后，东京完整Guard放行→真实审核HTTP 200/deny→客户端不执行通过，美西同场景复验中；旧失败保留，没有降低Guard策略或跳过审核输入。
- 审计边界检查发现未指定effort、上游回报默认low时被错误标为effort不一致。候选只在发送值与回报值都已知时判断不一致，保留未知原值；与action_review usage已有规则对齐。不改变请求、选型或缓存。新增默认/缺回报/相同/不同四种审计回归，完整验证及采用待完成。

### 2026-09-17 09:09 / v23两地采用与临时验收身份退役

- 两地仅升级Auto到v23（semantic-v9），Guard保持v20/preflight-r15。东京备份`release-20260917-090227-619832`，美西`release-20260917-090253-082074`；两地入口各17/17，密钥/状态/保护服务不变，4000/4001/4003未切换。
- v23两地隔离27次调用全部精确匹配账单；采用后29次新调用再次全部匹配，含两次原生Codex真实动作审核允许及文件写入，发送/账单effort均low，真实原生UA保留。P11账单项闭环，P03审核模型真实deny仍未取得：Guard提前403不能冒充审核模型拒绝。
- 东京测试group729/users2,3/keys142,143和美西group704/users3,4/keys88,89已停用，并仅失效这些Key的认证缓存。操作前验证精确身份、每组仅两个Key和成员；不删除使用历史，不停用所关联的既有上游账户，不修改原业务分组。
- 新增分类/多项目产物报告，同步过时的客户端、高级流程、压缩、当前策略和续接文档。P04启动项目外发仍等待具体授权；普通负载独立30分钟继续，DeepSeek高推理强度只在独立冻结题集实验中，不改变线上Luna分类器。

### 2026-09-17 08:54 / 分类稳定性复验、混合负载结果与审核候选

- semantic-v9 非简单任务复核策略：Luna 原48组三语保留题连续三轮一致率95.83%、95.83%、97.92%，新24组100%；第三轮开发组11/12，单列保留。DeepSeek 对照原保留组93.75%，仍不满足95%门槛，不切主分类器。结果只证明冻结题集的语言一致性，不代替开发任务质量。
- r15 混合负载18000/18000普通请求与96/96长请求完成；普通请求p95 482.85ms超过300ms要求，不能标为通过。此窗口同时运行原生客户端和长输入诊断，补独立普通负载30分钟；旧结果保留。
- v23 审核effort候选完整635项Go/40项Python、race/vet和六构建通过，冻结源码2b34b86f2232dfd3c3d7bce33716d51e780004753eb14590866b8f7f71e57657。Guard复用v20制品；当前4004/4005仍为Auto v16，先独立验收，不切4000/4001。
- Linux官方Codex 0.154.0在v22隔离入口以普通创建临时文件请求完成原生审核允许和实际写入；显式要求require_escalated的另一夹具被Guard判Controversial/Jailbreak，保留失败。原生拒绝必须有审核HTTP 200及真实usage，不能把Guard 403解释成审核模型拒绝。
- 东京临时分组729为独占验收组，两个测试用户/Key复用原有3个账户关联，仅自身清空effort映射/上限；原业务组不变。相关验收收尾后停用专用组、用户和Key，账单保留。

### 2026-09-17 08:44 / 动作审核effort计量补齐候选

- 实际action_review账单核对发现原始reasoning.low已透传，但网关审核usage与audit未记录该值。候选只读取原生reviewer已提供的合法effort枚举，补请求/发送/回报字段和独立每日桶；不让它参与Auto选型，不映射或重写原始审核请求。缺失/未知值保持未知，不把任意输入写入日志。SSE回报值也从已有观测器保留。
- 新增原生审核计量和合法/未知effort回归，完整验证待完成；旧账单与旧usage原值不改。当前线上仍Auto v16+Guard r15，后续候选与前述预热修复一起隔离验收。

### 2026-09-17 08:32 / 原生动作审核预热顺序修复候选

- 两地真实允许路径已到达codex-auto-review并落计量，但拒绝夹具仅检查客户端denied产生误报：Guard提前拒绝也会被Codex记为denied。验收工具增加审核接口200要求，旧结果保留并撤销其模型拒绝通过结论。
- 真实全链路定位为审核WebSocket预热先进入正式action_review分支，只有developer策略而尚无用户操作。候选先识别预热；仅codex-auto-review、纯system/developer文字的设置包允许在认证后的当前连接暂存为未检查上下文，审计明确deferred而非allow，不调用分类或审核模型。下一生成回合恢复完整设置与操作，再走正常Guard；Guard失败仍零上游调用。含user/assistant/未知类型/非文本的预热保持原检查，HTTP传generate=false不能触发该路径；无白名单或strict阈值变化。
- 增加真实WebSocket跨回合的完整上下文、Guard失败、零预热费用及HTTP伪造反例；候选尚未验证/采用，4004/4005仍Auto v16+Guard r15。状态格式和持久缓存不变，未检查暂存只随当前连接存活并受原容量/会话隔离限制。

### 2026-09-17 08:21 / 非简单任务统一复核候选

- 分类器主模型在相邻难度间的语言差异会决定是否进入复核，导致相同任务走不同判定流程。独立实验将所有非simple结果交Sol复核，保留主分类较高难度的保护和失败关闭；问候等高置信simple仍一次主分类。Luna原保留组95.83%、新题100%，DeepSeek原组仍93.75%，不能切换DeepSeek。
- 将已验证的通用复核范围写入候选semantic-v9-nontrivial-review，分类缓存按策略版本自然失效；补五档覆盖、独立usage和bounded复核故障回归。可能增加bounded/multi_step分类延迟及费用；线上仍v8/Luna，完整验证、重复评估和真实区域验收后才采用。
- Guard r15及附件契约不变。P04私有源码外发等待明确批准；不以新分类候选绕过该限制。

### 2026-09-17 08:16 / r15真实安全及故障验证通过，两地采用；验收对账更新

- v20完整614 Go/40 Python、race/vet和六构建通过；42项实际Qwen正反例全部符合预期，包含7类原生误拦记录、三角色恶意反例和超长文本。队列15/16正确503，审计磁盘满及tokenizer不可用均失败关闭，tokenizer故障时模型调用0；未放宽标签阈值或豁免策略。
- 仅升级4004/4005的Guard到r15，Auto复用v16；两地备份、密钥/状态保留、受保护服务不变。东京入口17/17，美西复验中；30分钟负载和原生高级/审核矩阵继续，不能写成全能力已验收。回滚使用各自发布前备份，旧规则缓存命名空间隔离。
- P02、P05、P12、P15完成各自真实版本补验及对账，总计划逐项关闭；新增最高档与缓存报告，保留两条max账单归一化差异和延迟未改善样本，不覆盖原始失败。P11仅在唯一审计证明时承认取消部分用量，动作审核账单仍待实测。
- 分类实验将所有非simple结果加入Sol复核，Luna原保留组46/48=95.83%、新题24/24=100%；DeepSeek对照仍在运行，未修改线上分类策略。私有启动项目第二轮仍等待用户明确外发授权。

### 2026-09-17 08:05 / 原生长输入误判定位；真实token预算候选

- Qwen实际vLLM上下文为32768 token，已验证/tokenize消息模板计数。原20K字符切块提前拆开公开策略/工具说明；七条原生误拦记录使用全量解码字段且完整上下文检查均Safe，完整原始JSON仍有一条动作审核误判。仅可读字段小块也仍失败，证明不能只靠修改提示词或凭据字段边界解决。
- 新候选将JSON字段按原始顺序解码成路径与原文字串，保留未知字段、重复键、数字精度、工具和元数据；不信任或跳过任何角色。通过本地vLLM实际token计数保留可容纳的完整上下文，超预算按字段边界递归切分，单个大字符串按Unicode分割并保留最多256字符重叠，完整覆盖且每段均审核；留256输出token和64模板余量。tokenizer故障/无效结果严格不可用，不降级放行。规则升r15失效旧安全缓存；模型权重和标签阈值不变。
- 新增完整字段/超长中部风险/Unicode/token预算/分词器故障回归；首次测试抓到风险短语恰好跨切分点的漏判，已增加双侧重叠并保留失败证据，正在运行完整验证与真实恶意反例；尚未部署。Auto不变、附件原字节和审核范围不变，原Guard/vLLM端口保持。
- P02两地当前v18 Guard均完成原生v2各5/5，旧compact两地也各5/5。P12共72次精确账单和token核对全部通过；P15当前终止审计完整响应误记上游失败为0，原美西无可见输出耗尽额度失败保留、改预算后真正取消通过。

### 2026-09-17 07:50 / 缓存与最高档验收；取消部分用量对账

- P12两地五个固定模型共60次、自然Auto切换共12次全部完整返回；固定候选首轮cache=0，其余50次均命中，上游缓存合计409600 token。Auto两地均DeepSeek none→Sol high→DeepSeek none，6次分类缓存命中、业务响应缓存0，另有77312上游缓存token；逐请求保留模型/effort/正文SHA和首可见时间。美西Luna/Astra重复延迟未改善，不能将命中等同于因果加速；账单成本对照继续。
- F01静态low实验与当前Luna重新对照：原保留组两者均45/48=93.75%，新保留题均24/24=100%；全部504次结构成功，仍未达到原题95%门槛。旧95.83%仅为历史结果，线上不切DeepSeek；继续稳定性修正。
- v19规则r14已完整604 Go/37 Python通过并冻结，Auto复用v16；隔离12条已有正反例通过，但未经字段污染的原生动作审核合成请求仍被Qwen判Jailbreak/medium。该失败真实保留，v19尚未部署，未加入白名单或放宽strict。已确认凭据边界修复有效，不能据此宣称整个动作审核链路修复。
- 美西实际Codex旧compact及续接、取消、恢复补验5/5，配线上v18 Guard/Auto v16。P02旧版两地证据齐备；其他原生高级失败继续逐项复核。
- 新增tools/reconcile_acceptance.py：按真实响应UUID及数字Key只读核账。取消后部分计量仅在同区域/身份/请求/上游ID、唯一终止审计且response_complete=false时单列；只接受已观测token不大于账单，保留原值及最终账单，不把零前缀当最终零用量。缺审计、跨Key、完整响应、超额计量和未知用量均不能借此消除差异。3项针对性测试通过；不改数据库schema、线上计量或历史事件。
- 启动任务五模型第二轮仍等待明确数据外发审批，未重新调用。当前线上仍candidate-20260917-r13-native-v18；本轮工具/文档无线上回退影响，4000/4001未切流。

### 2026-09-17 07:35 / 两地v18采用；动作审核授权字段误判修复中

- v18规则r13完整604 Go/37 Python、vet及六构建通过；三条原生误拦输入在隔离Guard各三次允许且正文不变，加Kotlin正反例共12/12。两地仅升级Guard，入口各17/17；Auto继续复用v16，密钥、状态及受保护服务未变。
- P16两地多用户检查全通过并恢复临时Key设置；P05自然Astra xhigh两地均完整完成，发送/回报一致，精确账单继续。两地四端重跑仍发现部分模型安全误判，不能把已修三条样本等同于原生全矩阵通过。
- 原生动作审核实际先被凭据硬规则阻断：匹配器把user_authorization字段内的authorization后缀当成认证字段，污染公开审核策略。核对公开Codex0.154源码后定位；先前Qwen对照使用的是已脱敏审计，不能据此认定未经污染的审核策略也失败。v19修复ASCII标识符边界，与JSON完整键名规则一致；真实Authorization和中文凭据继续保护，规则升r14使旧缓存失效。固定公开策略指纹复用仅为失败实验，没有进入代码或线上规则。
- 启动项目第二轮五份相同历史副本已准备；执行被自动审批以私有源码具体外发目的地未获明确授权拒绝，已向用户请求该轮数据发送授权，保持暂停。其余工作继续，禁止间接重试该批处理。

### 2026-09-17 07:20 / 原生实测与Guard分块上下文修复中

- v16两地OpenCode和Claude原生高级流程各7/7；东京Codex工具高级6/6、旧compact5/5。东京Hermes长历史、美西Codex子任务和旧compact遭Qwen Jailbreak/medium误拦；美西Hermes首回合超时，原因仍查。原生动作审核允许样本也被Guard阻断，未冒充通过。
- 同机既有脱敏审计对照发现：长用户输入使旧分块器完全丢失上下文；另有无害的短子任务被旧说明文字放大成Jailbreak。简洁API上下文加有界头尾片段令三条原生样本所有分块均Safe；原文每个字符仍单独完整检查。中英风险样本在用户、工具及instructions三个位置共18/18仍Unsafe；阈值、失败关闭和硬规则不放宽。v18规则r13候选增加覆盖长输入中部、小分块及Unicode的回归；尚未部署，动作审核仍待单独修复。
- v17 Kotlin修复完整603 Go/37 Python通过并独立冻结，复用Auto v16。真实Qwen的两个合成成员引用与一个凭据反例3/3符合预期；完整本地开发转录导出被自动审批拒绝，已改用合成样本，未绕过该拒绝。v17未上线，修复并入下一候选。
- 两地v16图片SSE/Responses工具/multipart共6/6，已检查东京编辑保持红蓝且中间变黄、美西生成白底绿圆。收到业务输出后的取消5/6正确；美西Responses先耗尽4096输出额度、未收到可见文本，属于未完成取消样本，单列复验。东京自然Auto在客户端none时选Astra xhigh，发送和回报均xhigh，账单待核对。

### 2026-09-17 07:00 / v16两地隔离通过；Kotlin凭据误判修复中

- v16完整603 Go/37 Python、vet和六构建通过；东京、美西真实隔离各6/6，覆盖原生压缩、加密续接、三协议本地工具历史及账单关联ID。东京4004已仅升级Auto，保留密钥/状态，受保护服务未变；美西仍旧版，当前入口完整验收继续。
- 首次发布执行因Python自行生成未列入清单的pycache被制品校验拒绝，未激活；将缓存移出冻结包后使用python3 -B重试成功，没有修改已冻结制品内容或放宽校验。
- P04启动任务的DeepSeek和Luna均遇403。定位为工具返回Kotlin表达式accessToken = data.accessToken ?: current?.accessToken被当作真实凭据；不是样本含密钥，也不是模型能力失败。增加正反对照回归，仅扩展已有成员引用+空值回退识别到Kotlin ?:；引号内和普通点号凭据仍保护。规则版本升preflight-r12使旧安全决策失效，脱敏接口与原始附件保持。完整验证、真实复验及部署待完成；旧失败保留，模型输出不由评测方修补。

### 2026-09-17 06:45 / 开窗真实结果与本地工具历史兼容修复中

- 两地固定最高effort共20次均HTTP200。Responses中GPT四候选max、Luna/Terra/Sol的xhigh、Astra的xhigh及DeepSeek max均回报发送值；DeepSeek Chat无回报字段，仍未知。美西请求gpt-5.6-sol的两次实际回报gpt-6-sol，原样记录。账单字段核对和自然Auto最高档另行验收，不能由固定参数探测代替。
- v14与v15东京隔离均5/6：旧compact与加密续接、Chat/Messages本地工具记录及账单ID通过，Responses本地工具记录400。最小对照确认：末尾为本地工具调用/结果时DeepSeek要求reasoning_text；原Key、无映射测试Key和显式关闭thinking均失败，追加新用户回合或普通输入成功。四GPT候选相同历史均200。仅记录合成测试正文的元数据，不存模型推理。
- 新增回归在v15旧实现复现6项失败，覆盖同步/流式规划的新会话、粘性会话和仅有不兼容候选。v16候选增加local_tool_history硬能力，只对无供应商绑定的Responses本地工具记录应用；保留原始调用/结果、现有绑定和其他协议，克隆分类能力切片后加约束，避免污染分类缓存。完整验证和真实复验待完成，线上仍东京v10.2/美西v9.2。
- DeepSeek low独立程序启动因宿主glibc过旧失败、样本数0；同一源码静态重建后可在主机启动，SHA75637d5bb3869074b397e47d36bf03f676d78446125b0783468c9a46fdb1a7b8，新目录重新开始冻结题集对照。旧失败保留；线上Luna不变，不能把运行中样本计作质量通过。
- P04第一批DeepSeek与Luna的小源任务各自完成、HAP和五项行为检查通过；其余任务及统一产物评审仍在进行。未改原项目或正式入口。

### 2026-09-17 06:16 / 收到业务输出后的取消验收驱动

- 原生客户端固定等待后取消可能发生在分类阶段，因此新增独立HTTP SSE驱动：Chat/Responses/Messages先收到可见业务文本，再断开连接；按响应网关ID等待终止审计和业务attempt，后续另查供应商账单。该驱动不替代四端原生覆盖。
- v15本机三协议各6项通过，均request_canceled且没有upstream_failed；Anthropic保留已收输入用量，其余未报告用量仍未知。初次驱动在审计先写入、用量尚未写入时误判，已修正为同时等待两类记录；非网关代码修改。
- 工具与两地配置已放入GPU私有测试目录，真实6次取消等待v15部署后执行；cancel-after-visible-output-fixture.json仅为合成后端证据。冻结制品、线上服务、缓存及夜间策略不变。

### 2026-09-17 03:53 / v15原生动作审核允许、拒绝及故障闭环

- 本机实际Codex0.154连接v15，正常允许/拒绝各4项通过，只有明确允许时产生预定私有文件；Guard503、审核模型503、审核结果非JSON三种故障各4项通过，原生事件均denied、文件未创建。Guard失败时审核上游调用为0。
- 审核模型503原生客户端进行了4次调用，格式错误进行了3次；按实际attempt记录，不把客户端重试合并成一次网关调用。格式错误HTTP响应本身完整，网关完成与Codex审核拒绝分别记录；不在网关重写原生schema。
- 全部为本机合成鉴权/Guard/模型，脱敏证据native-codex-review-v15-fixture.json；P03真实区域业务+审核和账单仍待完成。仅增加私有验收工具和证据，不改v15冻结制品、线上配置或缓存，无部署回退影响。

### 2026-09-17 03:49 / 固定effort探测对账ID修正

- 核对Sub2API中间件确认其自行生成响应X-Client-Request-ID，不采用请求传入值。两份私有固定effort脚本原先把自设UUID记成上游ID，合成20次均复现错配；修复后20次全部取响应ID，请求UUID单列，缺失响应ID时不能记成功对账。
- GPU待执行脚本在实际模型调用前备份并替换，保留原版本SHA及修订清单；06:33的一次性排队任务继续原计划，不重复启动。追加探测同样修正，尚未调用模型；fixed-effort-correlation-fixture.json仅证明驱动关联。
- 同步总计划三处历史状态：专用分组已创建、最新本机候选为v15、v11.1故障修复已随新候选验证。未改线上二进制、schema、缓存或夜间策略，无线上回滚影响。

### 2026-09-17 03:16 / 验收脚本请求关联修正；失败计量候选补齐

- 缓存、多人和自然最高effort驱动误将自设X-Request-ID当成网关request_id；现改读响应X-Gateway-Request-ID，客户端探测ID单列，缺失时明确失败。多人审计改按字段精确匹配并覆盖轮转文件。三个v2工具已上传受限测试目录，尚未真实执行。
- 实际v14本机网关配合成鉴权/Guard/模型验证通过：缓存6次均精确找到业务attempt及上游缓存；多人成功与身份错误两分支正确取ID；自然路由8次均关联终止审计，低档合成分类未被误记最高档。证据为acceptance-driver-correlation-fixture.json，不作为真实区域或模型能力通过。
- 复核发现原始JSON上游模式未捕获X-Client-Request-ID，读取失败/超限也丢失已收的响应头。新候选在读正文前保存两种独立关联ID、状态及传输元数据；读失败时仅保留完整JSON中已报告usage，不转发部分正文、不把未知用量补零、不标完成。新增三协议六种结果回归：旧版18项均复现，修复后针对性race/vet及完整557 Go/37 Python、六构建通过。
- 同轮继续补齐失败计量：SSE聚合失败保留已收到的usage和模型/响应ID，取消保留context.Canceled；分类HTTP/读取失败保留有效JSON中的已知usage但仍拒绝分类；动作审核非2xx保留独立账单关联。旧实现17项失败已复现，新针对性race/vet及完整578 Go/37 Python、vet、六构建通过，source efc73780c1b690f3ea7199b9da2d4013f514192dfd727ab44845ee3f49798e89；合并候选已冻结为candidate-20260917-r11-native-v15，Auto SHA365ca2903eb506225abc9e2acc476bb7d44ca038055a5918815860b0b4e4420e；Guard复用v8。失败时收到的计量可能只是部分值，不冒充供应商最终账单。
- v15本机原生高级回归完成：Claude/OpenCode/Hermes各7项、Codex6项通过，四端主动取消各1条request_canceled、完整响应upstream_failed为0；使用合成鉴权/Guard/模型，证据native-*-gateway-v15-fixture.json。独立真实v15区域验收脚本已准备但未执行。
- 线上默认SSE聚合路径已有账单关联，该修正不改变路由、Guard、schema或缓存语义；v14冻结目录不改，v15已独立冻结，原生客户端及真实两地复验继续。4004/4005仍保持既有运行版本，原入口不切流。


### 2026-09-17 02:22 / 当前能力文档对账，业务制品不变

- 总计划、Auto/Guard说明、部署说明和维护指南改正旧待办：同机共享状态、分类缓存、区域usage导入、WebSocket及运维发布已完成各自当前范围；Guard历史摘要仍仅进程内，不与P10混淆。
- 明确模型列表包含auto及专用codex-auto-review，业务候选不公开；补v14原生会话头、取消/交付归因和固定候选评测工具状态。统一验证现构建六个程序，开发模型产物、真实两地高级流程、最高effort、精确账单和缓存评测仍未关闭。
- 仅更新维护文档，不修改服务代码、线上配置或冻结v14制品。源码清单含README，因此当前工作区文档与冻结候选的文档快照有差异；冻结目录及验证SHA保持，后续不得据此声称重新构建或部署。
- 补齐独立验收准备：自然最高effort脚本使用既有四道保留题，预先限定每地首次完整xhigh即结束，客户端none不参与选择；已上传但未执行。美西新增五个固定候选回环评测进程与独立状态/用量，均健康且模型调用0，缓存对照及账单配置已纳入两地，仍须等待上游开放。测试进程完成后按PID及命令核对退役。


### 2026-09-17 01:38 / v14冻结与四端原生候选回归通过，真实区域矩阵继续

- v14完整检查通过：538项Go测试/子测试、37项Python、vet及六命令Linux构建；source `4f2845d750f36cf654222d20ec2871b97e42e7b8f3a21acb40af286b7681880a`，冻结为candidate-20260917-r11-native-v14，尚未部署。Guard复用v8，状态/缓存schema不变；原先版本记录保留为历史。
- 同一v14源码交叉构建Mac候选后，原生Claude/OpenCode/Hermes各7项、Codex工具高级流程6项通过。四端主动取消各产生一条request_canceled，完整响应被误记upstream_failed为0；取消后均能恢复。鉴权、Guard、分类和业务回复为本地合成服务，不能计入两地真实模型通过；脱敏证据为native-*-gateway-v14-fixture.json。
- 两地专用测试资源只读预检各4项通过。美西Redis容器环境存在过时密码变量，而实际服务未启用密码；验收脚本先前因此失败，已按真实PING结果修正连接方式，未修改Redis配置、缓存数据或生产账户。撤销/到期/额度矩阵仍未执行。
- P04补查现有JVM单元测试：Android启动项目基线通过；提醒项目60项中既有StationRouteBuilderTest一项失败，须作为基线缺陷单列，不能归因于候选模型或擅自改题。缓存评测驱动准备按分类缓存、响应缓存和上游缓存分别记量；不把重复请求耗时直接宣称为缓存因果收益。
- 两地夜间策略保持，真实模型/账单验收等待开放；线上仍东京v10.2/Auto v10、美西v9.2/Auto v9，4000/4001/4003、原Guard及GPU0不变。
- 后续补齐评测前置：固定模型驱动通过真实SSH合成任务4回合的读/改/检查/最终复验；5个仅回环评测端点已启动，15任务单次批处理等待06:31。Android启动基线21项JVM通过，提醒基线59/60通过且既有路由测试差异已单列；产物评审口径在实际候选执行前冻结。
- DeepSeek独立low推理实验针对性race和构建通过，只改变主分类effort与实验版本，提示词、复核和阈值保持；原题和新题均与最新Luna基线重新对照，等待上游开放，不改线上主分类。准备精确client ID/数字Key的失败及图片账单对照，未知gateway token保留未知，不能用0掩盖取消后的供应商账单。
- 核验入口监听发现验收驱动误写127.0.0.1:4004/4005，已修正为既有VPN地址192.168.64.16；线上监听未变。两地有效专用Key的GET /v1/models当前均503 identity_check_unavailable，证明夜间也阻挡真实身份校验；没有以直连内部端口绕过该限制。

### 2026-09-17 01:10 / 原生取消审计与图片完成/交付口径候选

- OpenCode v13原生七项现已全部通过；旧版绑定503已由实际客户端重跑确认修复，仅本地合成Guard/模型。v13.2完整检查通过（37 Python、Go race/vet、六命令构建；530 Go测试/子测试，source fcbe69c701ca064c0fc07c3c3bff8f44f915b441ae43ae5ee99b802fa1742207），仍未部署。
- 四客户端原生取消日志显示主动取消仍记upstream_failed，虽然现有模型健康度已经忽略context.Canceled。候选新增request_canceled阶段，保留具体SSE终止原因；客户端写出失败仍为client_delivery_failed，真实超时/异常EOF仍为上游失败。
- 图片SSE此前未参与终止事件已交付的识别，图片JSON/SSE还把生成完成和客户端写出成功合并。候选统一终止事件接口，分别记录ResponseComplete和成功交付，完整生成后取消不误报；写出失败仍记录失败attempt且保留已知usage/独立账单ID。新增模型及图片端到端HTTP回归，旧版真实失败、修复后针对性race/vet通过，完整验证中，不覆盖上一候选证据。
- P16/P05专用资源已创建：东京group729/users2,3/keys142,143；美西group704/users3,4/keys88,89；每区3个原账户关联、2个成员，每Key额度3美元、24小时到期、交互登录关闭。源分组预像保存在GPU私有目录，原共享分组未修改；没有绕过夜间策略。尚未进行真实多用户模型验收，完成后只停用这些专用资源，保留账单。

### 2026-09-17 00:50 / OpenCode 用户原生工具记录兼容修复中

- 实际v12.1网关连接本地模型夹具：Claude Code与Hermes高级流程各7项通过；Codex另补原生双exec、namespace子任务、fork、取消和恢复6项通过；OpenCode前三项通过，原生子任务返回后持续503。捕获确认客户端会生成带明确用户命令标记、ULID call_id、完整调用/结果的本地task记录，这类调用没有上游模型绑定。与OpenCode v1.18.4官方源码及实测结构一致，失败留存，不计完整通过。
- 候选仅识别OpenCode原生完整本地task/bash单对记录；先查现有同区域/Key/协议工具绑定，有绑定仍固定原模型，无绑定才作为用户提供的完整历史继续。UA与文字标记是兼容提示，不授予执行权限或访问别的Key状态；鉴权、完整Guard与原始传输保留。其他工具、缺结果、ID不匹配、未知客户端及正常跨Key模型工具结果继续拒绝。
- 新增三协议完整记录、边界、Guard阻止、已有绑定优先、跨Key与损坏状态回归，针对性race通过；首次测试类型缺指针接收器导致编译失败，修正后通过。全量首轮37项Python通过，vet检出新测试LoadChecked按值复制互斥锁；已改指针接收器，重新验证中。未部署、不改缓存键或状态schema；回退恢复原先的503行为。

### 2026-09-17 00:38 / 固定候选能力评测工具开发中

- 为P04同一真实项目任务的模型对照新增独立 `model-profile` 命令：只监听显式回环端口，限定目录中的单个候选及原生effort，客户端字段不能改变比较档位；每次新建0700评测目录及0600身份材料。正式Auto入口不新增固定模型选项，也不改共享分组。
- 比较链路仍先向同区域Sub2API逐次鉴权，再执行真实Guard正文契约，复用Auto协议、工具回合与usage；关闭响应缓存和自动失败换模型，分类来源明确标注fixed-model-capability-profile-v1。固定候选对照不充当自然Auto选型或模型质量通过的证据，实际产物必须另做构建和行为评审。
- 新增监听/候选边界、客户端effort忽略、文本范围和身份/Guard失败前零业务调用回归；验证脚本扩为6个命令构建。针对性3项race已通过；新增受限文件工具驱动，禁止任意shell/路径越界/链接逃逸，只能跑操作者指定的离线构建，模型最多3次检查并由驱动最终再查一次。2项Python边界回归通过。两个Android历史副本离线debug基线通过；脱敏初次误改XML输入类型，已还原语法并重建通过。尚未完整回归/部署或开展真实模型任务，不覆盖v12.1已冻结的506 Go/35 Python历史结果。

### 2026-09-17 00:05 / 原生会话头补齐；分类评测遵守 Guard 脱敏

- 四个真实客户端连接本地确定性模型夹具，确认 OpenCode 1.18.4 使用 x-session-id、Codex 0.154 使用 session-id、Claude 2.1.233 使用专用会话头及结构化 metadata；Hermes 0.21 Chat 请求没有会话信号。旧代码漏识别前两个原生字段，导致 HTTP 请求级降级及 WebSocket 重连丢失路由会话。候选统一识别 HTTP/WebSocket 原生头，亲和与父线程字段不作为自身身份，显式集成覆盖和 child-agent 作用域保持。
- 新增原生头/父子隔离/优先级及 WebSocket 两次真实连接回归；旧代码会话测试失败，修复后针对性 race 通过。连接内历史仍随断开销毁，不能将路由会话稳定误称为跨连接响应历史持久化。Hermes 无显式信号仍按请求隔离，不按用户或提示前缀拼造会话。
- semantic-eval 原先 Guard 放行后仍分类原输入；新增回归复现脱敏遗漏，修复为仅使用摘要契约验证后的 SanitizedRequest，拒绝 block/unavailable/缺契约。评测身份改为显式 region/key-id，不再硬编码东京 141；校验并发范围，保留真实评测 UA。针对性测试通过，未改变线上主链路、分类提示词及策略版本。
- 继续原生子 agent 捕获确认 Claude 正常模式的主子请求共用 session 头，子请求新增 x-claude-code-agent-id；旧代码隔离测试实际失败。候选将该原生 agent ID 纳入现有作用域，优先保留 X-Gateway-Agent-ID，忽略 parent-agent-id；不要求客户端追加自定义头。OpenCode/Claude 本地原生高级流程各7项通过，仅协议夹具，不替代两地实际模型。首次Claude --bare未开放Agent、夹具误读末尾system和task-notification导致失败，修正后留存最终证据。
- Hermes原生PTY在长历史后完成压缩、双read_file、真实子会话、Ctrl+C与恢复7项；终端明确报告API等待被中断，夹具观察到断连。短历史不足/摘要会增长时客户端拒绝压缩，首次长文本PTY部分写入也已修正，失败不算通过。OpenCode原生Anthropic模式同样发送x-session-id，识别扩展到三协议。最新Linux完整回归通过（506 Go测试/子测试、35 Python、vet和5构建）；仍未部署。
- 本轮仍是未部署候选，待完整 Linux 回归及两地真实验收；线上版本保持顶部既有记录。会话命名空间变化会自然产生新状态/分类缓存，不批量清旧数据；回退恢复旧识别行为。四客户端头证据仅保存 SHA/字段名，不含原始会话或凭据。后续继续压缩、多工具、子会话和取消实跑。

### 2026-09-16 23:41 / 两地运维闭环；业务候选仍受真实验收门槛约束

- 东京已激活release v10.2、Auto仍v10；美西已激活release v9.2、Auto仍v9；Guard均v8。两次changed_services为空，密钥/状态/正式服务及容器保持。美西先完成新版工具注入失败回退，配置/指针/状态/容器均与演练前一致。
- 两地实际公开health均200，/_gateway/status、/_gateway/drain、/metrics、/logs均精确404，/v1/preflight未认证401；内部五个监听均回环，受限日志/状态0600/0700，health/usage任务最后执行成功、无告警、待补传0。保留期仍null，仅归档无删除。
- 发布工具矩阵按真实/离线范围归档：实际部署/排空/回退、并发锁、制品拒绝、Guard连接故障；磁盘/缺密钥/回退自身失败/Guard单组件回退为离线故障注入。P13/P14据各自范围关闭，业务模型和客户端放行要求不变；剩余9个P项与F01不能因此算完成。
- v11.1业务候选494项Go race、35项Python、vet和5构建通过，source 2f128a4ffb055e48a0d96d6d521b73d2d1e6314d9ca4430ec7fb054e5d17f68b；复用v11 Auto和v8 Guard，未安装或部署该业务候选。既有夜间策略继续保留；没有用离线结果替代两地真实模型验收。
- 补记过程失败：制品构建首次用了工具不支持的allow-dirty参数，解析即退出2，没有产物/服务变更；改用工具实际支持的candidate参数后冻结。私有资源脚本修改第一次字符串生成语法失败且未写回，修正后语法通过；资源仍未创建。后补README是文档变化，不重写已冻结制品清单。

### 2026-09-16 23:33 / Guard单组件回退修复；运维独立性验证

- 发布故障矩阵补出具体缺陷：仅Guard升级失败时，旧Auto仍处于drain，回退代码先检查ready再解除drain，会误报回退失败并使入口保持关闭。新增回归先复现该路径；修正为恢复容器后、入口仍关闭时先解除Auto drain，再检查就绪。35项Python当前通过，完整冻结待完成。
- v11 Files/SSE修复已完整Linux494项Go race、34项Python、vet和5构建通过，source 29291267116628633175a424c1904cc1d1df3f2490e2a89c54805809db1abe3e；已冻结但未部署。上述发布工具后续变更将使用新制品，不改v11冻结清单。
- 两地通过两个真实OS进程、独立文件锁与当前回环健康端点验证：模拟导出进程阻塞时健康检查14ms/10ms完成，导出恢复后告警恢复；测试仅用临时运维目录，没有阻塞线上导出。首轮夹具错误假设status有ready字段，修正后通过，未把首轮记成功。
- 美西入口404规则准备独立运维制品，只复用Auto v9、Guard v8，不推广待实际模型验收的v11；当前尚未更新，发布结果另记。现有夜间策略保持。

### 2026-09-16 23:24 / Files存储错误及客户端写出归因候选，尚未部署

- Files真实文件系统故障回归复现旧行为：锁文件不可用时误报404/413并包含本地路径，损坏元数据/内容被列表静默省略。候选统一返回脱敏503，列表明确失败；正常缺失/到期404、大小/配额413，Responses及图片编辑的file_id展开沿用相同错误口径。文件ID、原始字节、状态布局和缓存语义不变。
- 针对回归在旧代码失败、修复后通过，含锁/损坏元数据/损坏内容/128文件配额及禁止上游调用；初始图片夹具缺NativeAPI已修正，未把接口未配置误当文件检查。完整Linux验证待跑，线上尚未采用。
- 客户端写出错误与上游读取错误已有类型区分，但前者仍计入模型熔断并统一记upstream_failed；候选改为client_delivery_failed并从供应商健康采样排除，失败usage仍保留，不把未送达响应改记成功。待旧行为复现与完整验证。
- 重新核对全部轮转审计，东京旧版complete=true失败共12条、美西0条；旧记录无终止原因，不能追认具体根因或覆盖历史。证据保留历史范围，当前修复需原生复验。
- 实际Guard端口连接失败在20.087秒后拒绝发布，实例目录未改、尚未排空或备份。已归档并发锁、7项制品拒绝及该故障结果；没有绕过23:00–06:30上游策略，没有新建临时测试用户或改共享分组。

### 2026-09-16 23:10 / 两地上游夜间停用窗口，真实验收暂停于该边界

- 东京v10已仅Auto升级，Guard/密钥/状态/保护服务保持。部署后的17项实际入口仅5项通过，Codex旧compact客户端首轮也失败；均保留原始失败证据，不计为协议验收通过。
- 两地直连9881/9880的models与东京账单接口返回同一明确503：API unavailable from 23:00 to 06:30 (Asia/Shanghai)。东京Sub2API及PostgreSQL/Redis容器健康且无本轮重启，网关Auto/Guard正常。这是现有上游时段策略，未擅自取消或绕过；已询问按时间窗继续或先准备独立测试通道方案。
- 旧compact的两地隔离真实验证是在窗口前通过；完整原生客户端验收仍待窗口开放。美西继续v9.1/Auto v9，未把东京受阻的上线基线算作通过后推广。
- v10.1完整Linux486项Go、34项Python、vet及5构建通过，复用v10 Auto和v8 Guard；东京零组件重建激活，新增受限路径404和版本化Nginx模板更新。两地实际竞争安装被升级锁拒绝；真实制品私有副本的CPU/契约/状态/哈希/额外文件/路径逃逸共7项拒绝验证通过，原制品未变。


### 2026-09-16 22:58 / v10隔离验收；入口边界及发布校验候选

- v10首轮vet发现通用JSON解码值未断言object，修正后486项Go race、30项Python、vet和5构建通过，source fd5f23aaf4b33f672f839049a0187991e8db3afada0ceb9d213342e89beedea1；东京真实Guard/上游的旧compact及加密续接通过，美西继续验证。
- v9.1两地零组件重建已激活，health/usage独立service实际成功，故障和恢复验证通过。两地当前各14条成功调用与Sub2API精确匹配，含DeepSeek/Luna、分类/业务、JSON/SSE和缓存token。
- 只读边界检查确认8001/8013/8014/8093/8094均为回环，日志与ops为0600/0700。内部路径经网站兜底返回HTML，不是泄露内部API/日志；候选将/_gateway和/logs明确404，并通过发布模板随升级/回退一起更新。校验旧模板无漂移及区域端口，兼容最初东京endpoint缺少内部端口字段。
- 发布验证新增Guard契约/兼容状态schema拒绝，补磁盘不足、缺密钥及旧入口元数据回归。以上发布/入口改动尚未验证部署；不改变业务缓存、正式4000/4001或原8011。

### 2026-09-16 22:48 / 两地Codex远端压缩通过；旧compact适配候选

- v9两地各17/17；原生Codex0.154临时OpenAI provider实际执行远端v2压缩，300条历史续接、取消、恢复两地各5/5。原生contextCompaction完成事件与后续标记/阶段回答均核验，原始日志仅保留在私有临时目录。
- 旧/v1/responses/compact新增候选适配：调用真实原生compaction_trigger，返回原始加密摘要和经Guard检查的用户/指令消息，不本地伪造摘要；非法输入、Guard拒绝/故障和普通回答冒充压缩均拒绝。计量复用实际一次调用，压缩不进入响应缓存。依据官方Responses compact返回契约及Codex0.154源码，待Linux/实际客户端旧流程验证和部署。
- 运维拆分候选v9.1已完整Linux通过483项Go race、30项Python、vet和5构建，开始冻结发布；当前两地仍v9，Guard保留v8。
- 同一候选补齐Files跨进程锁：保存/读取/列举/删除共享稳定锁，使同机多个Auto的全局和单Key配额检查与写入成为一个事务；4进程160次上传仅接受128次的真实文件回归已新增，待验证。文件ID、存储布局和客户端请求方式不变。

### 2026-09-16 22:38 / 运维任务解耦候选；v9东京及两地账单验证通过

- 东京v9入口17/17；两地隔离原生压缩各3次真实调用全部通过Sub2API独立client请求ID匹配，输入总量、输出及缓存token逐条一致。美西开始仅Auto升级，保护端口、Guard、密钥及状态保持。
- 发现原分钟任务先同步区域数据库，再检查健康，SSH/数据库长超时会拖住告警。候选改为独立usage service/timer与health service/timer，分别加锁；健康仅读取原子状态，180秒未成功导出告警，导出失败保留可重放outbox。新启动器在回退旧制品时关闭独立导出，旧组合任务继续运行。
- 候选不改变Go二进制、业务缓存或状态schema；保留期继续为null，无自动删除。已新增阻塞导出时健康立即返回、失败/过期/恢复回归，待验证和发布，当前线上尚未采用任务拆分。

### 2026-09-16 22:23 / 原生压缩能力候选及真实轮转演练

- 新候选识别Responses compaction_trigger/compaction状态，仅从支持该能力的OpenAI模型选型。压缩操作在Guard之后按协议需求选择，不当成普通聊天；加密状态保留原字节，用摘要/元数据标明文本Guard不可读，其他明文元数据继续扫描；分类器通过原生Responses读取压缩上下文，整段响应缓存排除压缩状态。
- 分类能力语义升至semantic-v8-native-compaction，业务修订guard-r11-semantic-v8-native-v9；旧分类缓存失效、会话schema不变。新用量字段及压缩候选回归中，未部署；旧/v1/responses/compact适配仍未完成。
- 两地真实日志轮转各6个文件，5种业务/Guard日志旧inode归档字节SHA全部保持，新文件0600、owner10001；没有删除任何日志。继续验证新请求写入新文件、定时重放和告警恢复。

### 2026-09-16 22:20 / v8.1两地零重建通过，补充账单关联字段

- v8.1已在两地激活，changed_services均为空，原Auto/Guard容器、密钥和状态保持。两地分钟任务已实际成功，Python路径固定/data/miniforge/bin/python3，usage无待补传、轮转维护开启、默认不删除，受限status/alerts已落盘。
- v8逐请求账单对账首次未匹配：Sub2API用独立X-Client-Request-ID生成client:UUID，网关X-Request-ID和供应商response ID不是该账单ID。依当前Sub2API提交源码与本人记录核对，候选新增upstream_client_request_id，独立捕获分类、JSON/SSE、原生Messages桥接、动作审核及图片响应头，保留原request_id；不会伪造或覆盖用户UA。候选未验证/部署，历史缺失字段无法补造。
- Codex原生本地压缩/续接/取消/恢复两地v8均5/5；远端v2东京直连通过，但Auto选DeepSeek导致输出普通文本，候选将按明确协议能力约束模型并保留加密状态。不把本地压缩通过替代远端协议通过。

### 2026-09-16 22:11 / v8两地基线通过，修复systemd解释器差异

- v8已在4004/4005激活，各17/17；两组件升级，密钥/状态/保护服务校验通过。东京Codex原生压缩/续接/取消/恢复在v8再次5/5。
- 两地分钟任务已安装，但实际运行失败：systemd硬编码/usr/bin/python3是3.6.8，交互python3是/data/miniforge/bin/python3。启动器is_relative_to要求3.9+；不能把脚本命令行通过等同于定时运行通过。
- 安装器现在验证Python>=3.9，并把实际解释器绝对路径写入生成的systemd单元，避免shell PATH假设；新增对应回归，27项Python通过。候选只改运维/模板，将复用v8二进制，以v8.1发布并同时实测无重启升级；当前任务尚未修复生效、没有自动清理日志。

### 2026-09-16 22:06 / v8完整验证通过；Codex原生本地压缩通过

- 最终冻结480项Go race、26项Python、vet及5构建通过，source SHA 84fcfb43a9c787486705fcf62194c9b05bb8745871e01e6baa21dde1db44d655；v8同制品已安装，4004升级开始，4005尚v7。两组件均有变更，Guard语义/脱敏版本不变。
- 真实Codex0.154.0 app-server在v7东京完成300条历史说明→原生compact→标记与三个阶段正确续接→原生取消→恢复继续，共5/5。官方对应tag源码确认自定义provider采用本地摘要，OpenAI/Azure采用远端v2；本次初次连接回退HTTP，不能作为WebSocket通过，远端compact仍单独未关闭。启动脚本不支持全局ignore-user-config的首轮失败保留，修正为独立临时配置后通过。

### 2026-09-16 22:02 / v8回归首轮失败已定位，未部署

- 26项Python通过，Go race新增图片对账断言失败：fixture只提供input_tokens，未提供cached_tokens，却断言全部为cache miss。当前Normalizer正确保留缓存状态unknown，不能凭空当成miss；fixture改为明确cached_tokens=0再验证cache miss透传。其余线上仍v7，重新冻结完整回归。
- 东京/美西独立库、每日桶与GPU ledger的事件/token总数均一致，未映射桶均0；这只是网关持久化一致性，Sub2API实际账单逐请求核对仍待新版本请求完成。

### 2026-09-16 21:56 / 两地独立用量库初始导入成功，v8重新冻结

- 东京559条、美西172条历史usage已导入各自独立gateway_usage库；数字Key映射分别141/87，待补传均0。东京第二次导入0/导出0，证明现有spool重复扫描不重复记账；历史Guard无token事件不能补造，新的安全用量从v8开始。
- 补齐图片用量上游request ID/cache miss及写入失败元数据回退日志；count_tokens读取损坏共享状态时503，避免静默重新选型。内部Guard诊断缺身份时明确归unattributed，不计为用户Key；区域配置提供默认region，避免坏身份事件阻断整个spool。
- 新增受限systemd安装器：验证当前不可变release、备份原工具、每区独立timer，未确认的留存策略默认null。26项Python已通过，最新Go/部署模板重新冻结验证；线上仍v7，无日志自动删除。

### 2026-09-16 21:49 / 区域运维兼容修复；未误认落库成功

- 实际持久导入在Key映射阶段失败，尚未进入数据库创建/导出。根因是区域Python3.6不支持subprocess的text/capture_output参数；跳板机把远端Traceback作为stdout并返回0。修改为兼容参数，并要求远端状态尾标记且为0，缺失/异常时拒绝确认导出。新增“跳板机0码仍判失败”回归；26项Python通过。
- v8原候选已完整Linux通过25项Python和Go/vet/5构建，但以上运维修复随后新增，须再冻结验证；未部署v8、未启动轮转/定时导入。现有4004/4005仍是已验收v7。

### 2026-09-16 21:41 / 两地SQL隔离验收通过，分钟运维候选完成

- 两地真实PostgreSQL会话临时表验证通过：同一batch提交两次仍5事件；业务2请求/2attempt/20输入/4输出、1失败、1缓存命中；数字Key映射777仅合成数据，security unavailable=1。未创建持久库、未改业务表。
- `gateway_ops.py`候选每分钟导入/监测：Guard p95/排队/不可用、按模型及Key失败比例、磁盘阈值；告警存受限JSON和systemd日志，不默认发送外部消息。新增systemd实例化service/timer，跟随当前不可变release选脚本，旧发布缺少轮转契约时拒绝执行轮转。
- 日志32MiB轮转、0600/目录0700、稳定锁和Nginx reopen；用量日志只有完全导入且远端全部确认后才允许按配置过期清理。保留期已询问，未获答案前retention=null，不自动删除。25项Python通过，候选未部署。

### 2026-09-16 21:33 / 用量候选重放及区域导出测试通过，尚未落库

- GPU现有东京/美西SSH管理通道均验证可用；新增`usage_sync.py`和独立`gateway_usage`数据库schema，不写Sub2API账单库。区域数字Key映射在各自服务器内读取并计算HMAC，原Key不离开区域、不进入参数或日志；GPU只接收数字ID与HMAC映射。
- 本地ledger保留断点和未提交尾行；区域导出按event_id去重、事务重算受影响每日桶，远端提交但ACK丢失时可重复发送。安全模型与分类/业务/动作审核分表/用途；未知usage单列，不把未报告token冒充真实0。
- 补齐安全检查结果事件及每日unavailable、排队等待/延迟指标；Qwen分块attempt单独记，规则/缓存判断不虚增模型调用。
- 22项Python通过，安全usage/Guard/Auto/轮转追加器针对性Go race通过；之后新增usage_reported字段待最终回归。区域数据库尚未创建、定时导入和对账未部署，P11/P13保持未关闭。

### 2026-09-16 21:23 / 4004只替换Auto通过，P11/P13候选补齐计量与轮转边界

- v7冻结475项Go/15项Python；东京实际升级`changed_services=[auto]`，Guard复用v6二进制/原容器，密钥、状态、正式服务保持。东京17/17与区域故障矩阵通过后，美西也仅Auto升级并17/17。两地Guard均保持v6原二进制和容器。
- P11候选新增Qwen每次分块实际请求token/结果元数据，安全模型独立purpose；分类缓存命中计数独立、attempt/token为0。P13共用JSONL追加器采用跨进程稳定锁、每次重新打开、0600及fsync，配合后续轮转，避免Guard长期持有旧日志文件。
- 检查发现同版本重复激活或只更新Guard时，未重建Auto仍在drain状态，原readiness会失败。候选在入口仍关闭时解除未变Auto的drain，再检查就绪；新增Python回归，尚未真实重复激活演练。以上新修改尚未完成Go全量验证，不计已部署。

### 2026-09-16 21:19 / 熔断恢复真实通过，用量重放候选开发中

- P09最终候选全量Linux检查通过；真实Guard与美西Sub2API经临时故障代理：三次DeepSeek503→Luna成功、第四次避开熔断模型、31秒后恢复DeepSeek。逐失败请求2 attempts/1 failure，恢复请求1 attempt/0 failure。故障只注入临时代理，现有上游未改；v7制品冻结中，尚未部署。
- 补充响应缓存与失败切换交互：允许切换时不合并并发miss，避免等待者沿用原模型身份、遗留原cache key的在途记录；区域业务响应缓存继续关闭。
- P11新增候选`usage_ledger.py`：0600 SQLite WAL/FULL同步，按原JSONL行摘要去重、inode/offset及首行摘要跟踪、防截断、保留未完成尾行；重启/轮转重放、失败已知token、数字Key映射及字段白名单。3项本地测试通过，区域PostgreSQL导入与Guard模型用量尚未完成，未部署。

### 2026-09-16 21:08 / v6两地已部署，运行时健康与有限失败切换开发中

- v6同制品已激活东京4004和美西4005，备份/密钥/状态/其他容器校验通过；共享事务和持久分类缓存已采用。东京17/17及原生Codex图片通过，模型目录解析警告消失且无HTTP回退；美西也17/17、Codex原生图片正确且目录警告消失，无HTTP回退。SSE在途排空复验完整结束，active=0，新增503。
- P09候选新增区域/Key/协议/模型隔离的真实调用健康采样、连续故障熔断、冷却后的单请求恢复检测；普通请求仅对未生成的明确429/502/503/504最多切换一次。Guard失败、鉴权失败、模糊传输超时、已有输出及当前工具回合不重放；逐次尝试独立审计和计量。当前代码开发中，未验证/部署。
- 业务缓存与会话schema不变；下一次仅Auto变更将同时验证组件最小替换，保留现有Guard进程。正式切流和第二故障域仍排除。

### 2026-09-16 21:00 / 共享状态实测通过，组件升级与连接内存边界补齐中

- P10两个真实Auto实例共享本地卷：跨实例分类缓存命中、实例重启后命中、A发出工具调用/B保持Terra续接且内容正确；候选尚未部署。P07当前冻结Guard的满队列和审计写失败均正确503且无业务调用，已归档。
- 发布候选新增逐组件源码摘要与二进制版本，支持复用未变化的Guard/Auto；部署只重建有二进制或配置变化的组件，并校验未变化容器ID。失败时连激活前改写的配置也恢复。Python15项通过，完整Linux验证中，尚未演练线上最小替换。
- WebSocket候选保持显式会话头跨重连、隔离命名lane；输入内存预算包含排队及执行中正文，多行SSE事件限制总大小。该补丁正在验证。
- 长SSE排空首轮已拒绝新请求503、原流保持活跃，但90秒观察窗口未等到终止；记为未完成并继续复验，不计通过。两地专用动作审核只读样本返回原生allow/schema/low；危险提议由Guard403终止，不声称审核模型返回deny。
- 范围继续仅4004/4005；无第二故障域，无正式切流。原版状态schema保持v1，需完成两地采用及回退验证。

### 2026-09-16 20:45 / 两地WebSocket真实通过，目录格式继续补齐

- native-v5已在4005/4004激活，密钥、状态和正式容器不变；美西额外注入激活失败并成功自动回退后再升级。459项Go/13项Python/vet/5构建的同一制品。
- 两地真实WebSocket预热零业务usage、增量记忆、双工具、Terra绑定续接和跨lane拒绝通过；原生Codex图片均回答正确，Nginx101且无HTTP回退。首轮网络连接超时导致HTTP回退保留为失败，不计WebSocket通过。
- 原生Codex暴露模型目录需`models`包装。候选同时保留标准`data`与Codex描述符，默认指令使用0.154.0公开原版，避免用空指令替换客户端行为；不覆盖本地/管理员Guardian策略。该补丁待回归/部署。
- P10共享状态完整463项Go、13项Python/vet/5构建通过；两临时实例的真实共享缓存/工具回合正在补验。P07当前30分钟18000次和96次60K输入均通过，队列及审计故障继续补齐。


### 2026-09-16 20:29 / 原生适配完整回归通过；共享状态开发中

- WebSocket/专用审核/排空针对性race通过，冻结候选完整Linux回归通过（结果目录1oLoOD，具体计数随发布元数据归档）；尚未激活。专项回归发现专用审核的model/effort被普通规范化删除，已保持其独立原生契约并复验。
- P06美西同矩阵六例通过：1.598s→0.567s、追加历史复用22条，身份/历史/envelope变化重检、凭据动作拦截。两地P06功能验收关闭；历史摘要仍单Guard内存，不冒充跨机器容灾。
- P10候选新增本地共享卷文件事务、fsync/原子替换、有界锁等待、每次读盘；会话路由和同回合多工具绑定纳入同一事务。分类缓存持久共享、有版本/身份摘要、TTL/容量，区域整段响应缓存继续关闭以交Sub2API计费。适用于同机多Auto进程，不宣称跨主机分布式存储。正在补回归，未部署。


### 2026-09-16 20:20 / WebSocket客户端桥接开发中

- 新增Responses WebSocket入口，逐回合重验身份并进入相同Guard→Auto链路；连接内保存原生历史、限制内存及并发、隔离stream_id，支持generate:false预热、previous_response_id续接和response.cancel。上游仍使用现有SSE，不能宣称上游原生WebSocket的延迟收益。
- 发布排空信号通知WebSocket：完成已接纳回合再关闭连接，空闲连接立即关闭；保留原生客户端重连语义。添加固定Gorilla WebSocket v1.5.3及校验后的vendor，离线发布清单同步。
- 当前仍在实现/回归，未部署；compact上游404、审核实际回报Luna继续核查。P08依用户确认排除，不修改现有GPU服务。


### 2026-09-16 20:11 / r11两地通过，专用审核与WebSocket/compact继续开发

- r11版本化发布已完成东京4004、美西4005，内部密钥、状态、其他容器均保持；各17/17入口回归通过。东京增量Guard真实矩阵6项符合预期。30分钟10rps及长输入测试正在执行。
- 补验Hermes Chat在4005真实小源开发及原生复核：首轮多改推送逻辑被拒收，复核后仅Navigation8+/5-，HAP构建3s108ms，5项行为检查通过。真实音频排除响应两地三协议中的Chat/Responses四例400明确码。
- 本地新增`auto/action_review.go`及回归，按原生Codex专用请求保留model、策略、schema、effort及工具，Guard先行，不进普通Auto分类；单列action_review usage。当前未验证/未部署，不假称已可用。
- 上游核查：两地Responses WebSocket完成；compact直接502，东京实际上游404；专用审核请求返回allow但response.model=Luna，账号显式review映射为原名，继续查Sub2API/供应商解释。官方Codex审核输出与模型选择源码、Sub2API准确revision19149ca已查阅。


### 2026-09-16 19:54 / r11-native-v4候选452项通过，4004升级中

- P06新增`guard/history.go`：完整Go凭据扫描后，用身份、区域、会话、规则、模型、envelope与有序消息链摘要验证已允许的历史前缀；复检新增内容、全部用户/系统意图和两条边界消息。仅内存保存有上限/TTL的摘要，重启、修改或压缩后重检；最终业务原生正文保持完整。Guard规则升为r11。
- P15新增SSE终止阶段和错误类型，区分客户端写失败、上游读失败及已成功写出terminal后的context取消；只有最后一类正常关闭不记失败，未知末尾错误仍保留。当前尚待真实Codex复现证明。
- P14新增`service`生命周期与版本信息、两个进程SIGTERM排空、私有运维查询/排空；新增`tools/source_manifest.py`、`release.py`、`deploy-release.py`，从准确验证清单构建一次，校验ELF/配置/状态/哈希，支持候选install/upgrade/status/rollback及失败关闭回退。保持现有实例运行数据目录，制品进入独立releases目录，内部密钥不重建。
- Linux452项Go、Python13、vet及5构建通过；已冻结candidate-20260916-r11-native-v4，源码摘要de7d593f4ba5ad8a1def345e490fc5793a5eed3c2e937efee24b75f8b3c8b9ed。4004升级中，4005仍native-v3；不能提前记为两地完成。
- 已知未关闭：长SSE/激活故障/回退演练、多实例事务/用量DB/监控告警和其他原计划验收。第二故障域P08按用户决定取消。正式4000/4001不切换。


### 2026-09-16 19:43 / native-v3已部署，r11增量历史候选开发中

- 两地native-v3 Auto已部署；Guard维持r10。TXT/CSV/DOCX/XLSX及Messages DOCX共10/10内容正确，上传/删除均成功；Linux433项Go和Python10、vet、5构建通过。证据位于native-media目录，包含两地部署回退路径。
- 用户明确取消第二Guard故障域（P08）；原要求其余P01–P16继续完成，正式4000/4001切流P17排除。音频与附件语义审核仍按已确认范围排除。
- Guard r11正在实现版本化、身份隔离的已检查历史摘要。完整凭据扫描和最终原生正文不缩减；历史改写/压缩/规则模型变更失效，新增片段连同全部用户意图和边界上下文复检。此候选尚未验证或部署，不能代替P06完成证据。


### 2026-09-16 19:30 / 范围确认：除正式切流，其余原计划能力全部完成
- 用户指出前期矩阵和计划闭环不足。重新归并17个未关闭工作包，区分旧版已验证、当前未验、未实现及上游/资源受限；不计算虚假的百分比。
- 用户授权P01–P16均须在4004/4005完成；F01 DeepSeek优化继续，P17正式4000/4001切流排除。已明确排除音频、后续增加附件语义审核。需要第二GPU故障域等真实条件时先查现有资源，不能用同机副本冒充。

### 2026-09-16 19:22 / MIME候选修复；r10已部署
- 19:10两地r10同包部署，426项Go/Python10/vet/5构建通过，源码138文件。备份分别native-media-r10-20260916-191007/191047，原有容器、内部密钥及排空后状态保持。
- 两地各17/17，PDF三协议内容及文件生命周期通过；Messages流式工具调用/结果续接4次内容正确。美西Guard故障聊天/图片/文件全部503、无分类/业务usage，恢复后真实请求200。图片流式/工具/multipart两地6/6有效PNG，目视检查符合要求。
- 新增TXT/CSV/Word/Excel内容检查发现网关MIME封装不兼容（文本charset、OOXML被识别为ZIP）。两地正确MIME直连8/8读出内容。候选按原扩展名传递标准MIME，保留Messages展开文件名和原始字节；不做内容抽取。仅更新Auto，Guard r10不变；待完整回归、部署和10项文件内容复验。
- OpenCode小源首轮构建/行为通过但加入额外stopPush，已交回原生客户端按与其他端相同的评审范围修正，未将该首轮记为最终质量通过。

### 2026-09-16 18:59 / 开发中：Messages 文件桥接与代码搜索凭据误报
- 双区域原生 Files 生命周期和 Chat/Responses file_id 内容读取通过，但 Messages 返回200却忽略文档。新增仅文件请求的 Messages→Chat 原生附件桥接，保留文本、工具身份和增量流；尚待回归及部署，不能用HTTP200代替内容正确性。
- Guard r10限定源码搜索结果中行号标注的 `accessToken: string` 为类型声明；真实字符串凭据、配置文件仍扫描。修复OpenCode小源任务后续回合误报，等待完整重跑。
- 用户已将 gpt-4o-audio-preview 引入两地；18:54实测两地测试Key均返回400，明确不支持通过Codex的ChatGPT账号调用。Luna原生音频文件也返回unsupported MIME audio/wav。只读分组确认新增映射仍绑定OAuth/Codex账号。用户随后确认本期不支持音频：显式音频块、音频文件与音频API返回400 audio_not_supported，不加入路由候选。

### 2026-09-16 18:42 / 候选验证：原生附件、文件兼容层、Guard r9
- 原生媒体分类在文本 Guard 通过后使用 Luna/Sol Responses 附件能力，业务仍收到原生内容；附件内容不属于 Guard 检查范围。新增 `/v1/files` 本地兼容层：区域/Key 隔离、0600 文件、标准上传/列表/元数据/下载/删除、file_id 自动内联；默认30天，支持明确 expires_after（1小时至30天），单文件50MiB、合并请求64MiB、每Key512MiB/128文件、实例4GiB限额。
- `/v1/images/generations`/`edits` JSON、multipart、流式与 Responses image_generation 工具支持 Flare/Sunburst；默认生成Flare、编辑Sunburst，不传文本effort。较大图片 SSE 事件单独放宽至128MiB。UA继续透传。
- 两地上游视觉/PDF验证通过；Flare生成、Sunburst编辑共4次200返回PNG，并目视确认编辑色块。Files原生上传404，网关兼容层修复此边界；gpt-audio-1.5两地均404 model_not_found，未假称音频可用。
- 原生适配阶段全量Go/Python/vet/构建已通过，最新候选增加了生成图片历史和Guard r9。r9明确待审核API角色/工具数据语境；隔离已知误报转Safe，三条恶意对照仍Unsafe，严格阈值和失败关闭不变。最新候选仍需冻结、全量回归、实际部署及四端复验。

### 2026-09-16 18:30 / 仅开发：原生附件与 SDK 接口兼容
- 用户确认附件内容原生透传、只检查普通文本，附件语义审核后续增加；用户操作不变。新增原生内容视图，以摘要绑定恢复图片、文件、音频数据，普通文本仍经 Guard 脱敏；审计明确 `text_and_attachment_metadata_only`，不宣称完整媒体审核。分类采用 `semantic-v7-native-media`，媒体预算为估算；文件与 Responses 图片工具按能力筛选 OpenAI，Guard 故障仍关闭。
- 新增图片生成/编辑、音频接口和文件上传生命周期适配；仅两种 GPT Image 模型。两地上游真实图片、内联 PDF 均200且内容可读，Files 上传均404，因此补区域/Key 隔离的临时文件兼容层，file_id 自动展开成相同内容的原生内联附件。当前目录无音频模型，音频实际可用性仍需上游核验。
- 初轮新增回归有6项 JSON 字段顺序比较失败，正在修正为保持附件内容语义不变的检查并增加上传隔离/过期验证。尚未部署这些附件代码；原生四端及两地图片生成仍待真实验收。

### 2026-09-16 18:12 / 执行中：原生附件范围纠正、Guard r8
- 用户明确附件应直接交模型；撤销尚未部署的 CPU OCR/文档提取前置方案。图片、音频、文件按原生协议保留，能力差异与 Guard 文本检查范围分别说明。附件适配代码及区域验收尚未完成，不能称已支持。
- r8 给 Qwen 全文分块补充当前用户任务（仍为不可信数据），不删减任何原文；126 文件冻结清单 SHA `71da0dc91f18218ae13c5d4a70f70bdf34d9207e15fcca029e2bf8ca566eea2c`。406 Go/Python10/vet/5构建通过。18:06 已仅更新 4004/4005 Guard；Auto SHA 不变、内部密钥及排空后状态不变，原有容器基线一致。回退备份分别为两地 acceptance 根目录下 `backups/r8-20260916-180604`、`backups/r8-20260916-180607`。
- OpenCode r8 重跑初始请求通过，后续会话仍被隔离；退出码1、无 App 文件改动，继续查实际首次拒绝原因。不能用孤立 Qwen 探测通过替代全任务验收。

### 2026-09-16 17:58 — 原生UA已确认；OpenCode分块语境修复开发中

- 双区域17/17入口通过；真实OpenCode问候的完整工具上下文约5.8万预算字节均simple/none、Luna分类且DeepSeek业务响应完整200。Sub2API本人Key与精确时间窗口记录显示分类/业务UA均为`opencode/1.18.4 ai-sdk/provider-utils/4.0.23 runtime/bun/1.3.14`；使用窗口和Key关联，Sub2API内部client UUID与网关request ID不同，未伪称ID逐项相同。
- 美西4005新增Guard故障演练返回503 preflight_unavailable、分类/业务usage为0，恢复ready。只停止新增Guard8014，原服务保持。
- 同一真实开发任务重跑，Claude/Codex已完成HAP和行为检查，Hermes由原生客户端移除额外推送改动后重建。OpenCode首请求因Qwen第一块判Controversial/Jailbreak被拒绝，不记录为成功。
- 原输入每20000字符拆块，首块含工具/代理配置且没有当前用户任务语境。隔离验证给每块重复实际任务（明确为不可信数据），保留完整输入，四块Safe；三条恶意对照仍Unsafe。实现preflight-r8上下文分块，长任务无法装入上下文前缀时保留原全量分块；仍聚合全部块、strict/fail-closed不变。
- 当前r8仅开发中，等待完整回归与新一轮原生OpenCode验证；多模态仍未实现。

### 2026-09-16 17:47 — 双区域部署完成；新增图片与文件必做范围

- 东京4004仅Auto程序/模型配置更新，Guard二进制及容器保持；备份`/data/ai-gateway/acceptance-tokyo/backups/v6-context-ua-20260916-174512`。原服务容器、内部密钥、重开前状态和恢复后的Nginx配置保持。
- 新建美西4005（Auto8094/Guard8014），上游9880，与4004复制同一份403项Go/Python10验证制品；独立内部密钥、区域认证/状态/日志，不存用户Key，不修改分组。
- 当前模型配置Luna主分类/Sol复核，恢复0.8低置信复核门槛；容量与原UA透传上线。真实入口及四原生客户端新一轮验收继续执行，未完成项不能写成通过。
- 用户追加图片/文件输入必须适配，生成/编辑限定Flare和Sunburst。先核对官方协议及现有能力，保持Guard前置，不将目录中的vision标签写成已支持。当前尚无多模态实现或上线声明。

### 2026-09-16 17:49 — 用户确定保留95%门槛，选Luna分类/Sol复核

- Flash扩大复核后原题回归仍44/48一致（91.67%），179/180成功，其中Sol复核有一次60秒超时；新冻结24组三语72次全成功、100%一致，不能覆盖原题未达标事实。
- 用户明确选择先用Luna、继续优化DeepSeek。采用已完成180/180调用、46/48保留组一致（95.83%）的Luna/Sol，未出现高难变simple；不是完整业务质量最优证明。
- 撤回没有改善原题一致率的0.9复核门槛尝试，恢复已验证0.8规则。407项Go及新样本证据保留为未采用实验；实际部署采用403项Go、Python10已通过的上下文/UA版本，不混用实验结果。
- 分类器选择与业务回答分开：简单任务仍可选DeepSeek Flash。当前仅推进4004 Auto更新和独立美西4005；原服务保持。

### 2026-09-16 17:30 — DeepSeek分类速度确认；质量门槛仍未通过

- 同一60组×三语输入，Flash/Sol和Luna/Sol均180次成功；分类链路中位耗时1248ms与8362ms。Flash保留样本44/48一致（91.67%），Luna46/48（95.83%）；均无高难误判simple。不能因更快就称Flash达到95%门槛。
- 首轮使用r7已冻结semantic-eval；分类提示和复核逻辑与初版v6相同，但不是新容量代码端到端证据。旧结果冻结保留。
- 扩大主分类低置信度复核区间：小于0.9交独立Sol，复杂/极复杂继续必复核；复核不足0.8仍失败关闭，禁止复核悄悄降低主分类难度。高置信度简单请求仍一次分类。自报置信度不是校准概率。
- 原保留集已用于诊断，此后结果只能算回归；另冻结未运行的新样本作新质量检查。配置与部署继续等待验证，不改原服务。

### 2026-09-16 17:10 — 上下文预算、真实UA透传与美西4005（开发验证中）

- 用户授权修改简单任务过度选型，并新增美西4005接Auto+Guard、保留原始User-Agent。原有4000/4001/4003和Guard8011/vLLM8001不变；4004后续只更新已验证Auto与配置。
- 原8000字节硬门槛在旧代码三协议针对性测试均复现；改为按各模型输入/输出/总窗口检查完整请求的保守预算，预留输出与封装空间，不截断上下文。semantic-v6使旧分类缓存失效；审计增加预算方法与数值，工具回合超容量拒绝跨模型切换。
- 加入原UA透传，覆盖鉴权、分类、计数、同步及SSE；缺失UA保持缺失，不再自动生成Go UA。对应旧代码失败/新代码通过证据待验证。
- 部署模板支持固定东京4004/8093/8013/9881与美西4005/8094/8014/9880；新区域有独立目录、容器、密钥、状态及日志，各自Sub2API鉴权/额度不混用。按最新要求改验DeepSeek主分类/Sol复核，Luna作对照；当前没有改变线上Sol/Astra配置。
- 本条仅记录开发中状态，尚未部署新版本或4005；全套回归、真实客户端、UA下游日志和双区域验收完成后追加结果。

### 2026-09-16 16:49 — OpenCode简单问候出现Sol与Terra：只读定位完成

- 用户刚测试的两次问候对应16:44:34、16:45:18，OpenCode本地SQLite记录均正常stop、无工具调用；与4004两次业务用量逐项匹配，HTTP200且响应完整。
- Sol是固定主分类模型，分别输入15120/15151、输出82token；两次都判simple、5分、0.99置信、none，无Astra复核，分类缓存未命中。Terra是回答模型，分别输入10945/10964、输出12/6token；第二次业务Prompt Cache命中10752token。
- 根因定位于`auto/semantic_classifier.go`完整请求8000字节的long_context阈值及`auto/models.go`能力目录。系统/工具背景也计入预算，Flash/Luna没有long_context标签，因此简单任务仍被能力过滤到Terra起步；不是难度误判或Sol回答后又降级。
- 本次只读诊断并记录[脱敏证据](evidence/2026-09-16-client-compatibility/opencode-greeting-diagnosis.json)，未改代码、模型配置、缓存策略或部署，没有新增测试通过声明。待按核实后的各模型容量修正预算匹配，分类器开销另行评估；Guard完整输入保护保持。

### 2026-09-16 16:34 — 四原生客户端小源开发流程与文档完成

- **最终结果**：OpenCode1.18.4、Hermes0.21.0、Claude Code2.1.233、Codex CLI0.154.0分别完成同一个真实历史退出登录任务。四份隔离副本均只改Navigation，未签名HAP构建成功，转译实际导航源码的5项行为检查全部通过；修复前基线失败。原小源项目仍干净，验收方未代客户端改App代码，未提交/推送/安装。
- **评审闭环**：Claude Code移除新增推送stop/resume，Login恢复基线；Codex恢复HTTP原有独立未授权清理、移除额外推送变更。修正由各自原生CLI执行，并分别于16:24、16:25完成重新构建。首版不接受的原因和SHA保留，最终产物以`xiaoyuan-final-results.json`为准。
- **实流量证据**：会话关联记录表明四端实际经过4004，已完成响应的有效模型均为Terra；这不是四家模型横评。Hermes曾遇到旧3秒Guard失败后恢复；Codex改用30/35秒预算后完成，但两轮合计5条审计为HTTP200、response_complete=true且upstream_failed。现有日志未细分具体传输错误，不把客户端成功写成零失败，也未将该问题写成已修复。
- **版本与验证范围**：OpenCode/Hermes全任务在r7旧预算下，Claude评审/Codex在r7-budget；不是四端全部在最终版本从头重跑。当前冻结部署120文件、Linux382项Go（含子测试）、Python8项、vet与5静态构建通过；最终入口6项正反检查通过。此次收尾仅修改文档及元数据，不改变已验证代码/配置。
- **文档**：同步唯一总计划、CHANGELOG、项目/Auto/Guard/部署README、维护指南及证据索引，写明真实任务来源、首版失败、修正、产物SHA和待验项目；冻结源码清单不重写。
- **部署/回退/限制**：仍只使用独立4004，4000/4001/4003及原Guard/vLLM未修改。16:15备份保持，不新增部署或回退演练。无真机UI验证；更多客户端能力、Hermes Chat、多用户/负载、最高effort和生产切流仍待验收，通用版本发布/回滚工具仍待实施。

### 2026-09-16 16:23 — 四客户端已走完首次改码与构建；产物评审修正中

- Codex在30/35秒预算下完成真实小源改码与HAP构建，Guard没有再次阻断该次运行。四客户端均已实际执行读代码、修改、构建，不能再归类为合成工具探针。
- 独立产物复核发现Codex删除HTTP层原有独立未授权清理；导航栈未注册时原清理能力会丢失。另新增停止推送而未配套恢复。首版产物构建成功但质量暂不接受；保留摘要与SHA。
- 对Codex和Claude Code给出同一范围反馈，由各自原生CLI在自己的隔离副本继续改码：恢复HTTP原行为，移除本轮额外推送生命周期变更，保留Navigation统一持久化清理，并重新构建。没有由验收脚本替它们直接修App代码。OpenCode/Hermes产物保持。
- 最终文档将分别记录网关/工具流程可用、HAP构建与产物复核，不用构建成功替代代码质量或真机UI验收。

### 2026-09-16 16:15 — 4004完整输入检查预算已更新

- 冻结120文件；Linux 382项Go（含子测试）、Python8项、vet及5程序构建通过。只更新4004 Auto程序和两服务超时配置：Guard30秒、Auto35秒；Guard r7二进制保持不变，排队1秒/上限128、strict及fail-closed保持。Auto默认仍5秒，配置必须为1至120秒，用户取消仍立即终止并返回不可用。
- 16:15更新前排空并备份，原服务、内部密钥、状态及恢复后的入口配置一致；备份`/data/ai-gateway/acceptance-tokyo/backups/r7-budget-20260916-161459`。这次修改支持多块输入的有限检查时间，不宣称正常负载SLO或长历史增量优化已经通过。
- 小源实测：OpenCode/Claude Code/Hermes已各自产生修改并构建未签名HAP，行为检查通过；Hermes运行中曾遇到一次3秒Guard不可用，客户端恢复后完成，不能省略该失败。Codex正在新预算下从同一未修复版本重跑。原始工具正文保留私有临时目录，仓库仅保存来源/版本/结果元数据。

### 2026-09-16 16:08 — r7真实开发重跑；长上下文预算修正中

- r7在16:02仅更新4004 Guard；380项Go、Python8项、vet及5程序构建通过。Auto/内部密钥/重开前状态和其他服务保持；源码119文件清单校验。
- OpenCode、Claude Code和Hermes已实际完成小源退出登录修复。独立执行其导航源码的状态行为检查通过；各自构建及最终结果继续核验。Hermes遇到ArkTS重载声明顺序错误后自行修正，失败日志保留；Claude附带推送生命周期改动，不能等同于原历史最小diff。
- Codex在真实工具结果后遭遇Guard不可用：请求9e089070cd00af2d9ea611d3496effd4，入口3.103秒503，对应classifier_error；原预算3秒。该脱敏输入131942字符、7块，单独暖缓存重放992ms且均Safe；并发/冷前缀条件下3秒不足是当前证据支持的原因，不把单独暖缓存速度当负载能力。
- 修改目标：仅4004整份输入Guard预算30秒，Auto传输35秒；队列仍1秒、上限128，不截断或跳过任何块，用户取消仍生效、超时仍fail closed。增加可配置有界Auto超时与取消回归；默认其他部署继续5秒传输，既有Guard8011不改。长历史增量检查/容量优化仍待完成，本改动不代表SLO通过。

### 2026-09-16 16:00 — 四客户端小源真实任务首轮失败；r7修复中

- 按用户要求用历史任务“修复退出登录状态未清除”（019fcb9a-55e9-70b1-99d6-60912a1c9bab），修复提交5b3a9d1的父版本2e394cb6作为四份相同起点。保留真实ArkTS源码、Android导航参考与依赖；排除签名/发布凭据，仅构建未签名HAP。原项目未修改；修复前基线构建通过。
- OpenCode、Claude Code、Codex、Hermes四原生客户端均已开始真实工具读取；首轮均被Guard凭据硬拦，尚无代码修改。Hermes在临时目录使用官方固定提交29112bef、0.21.0，与既有部署无关；不能把退出码0当流程通过。
- 已关联日志：OpenCode的Bearer模板字符串和accessToken运行时成员引用、Codex的interface内accessToken:string被当作凭据；Claude/Hermes另命中验收准备的普通占位值。验收副本改用既有脱敏契约占位符；该数据调整不算网关修复。
- r7修复只识别可见类型容器中的原始类型、带可选链/显式fallback的成员引用、完整反引号认证模板；不豁免引号内真实值、混合模板或普通配置的password:string。针对性用例已在旧逻辑上失败；新逻辑验证中，尚未部署，不放宽strict/fail-closed。

### 2026-09-16 15:46 — r6与SSE工具绑定已部署4004；Codex工具回合实测通过

- r5已修正Basic普通词误报、短Basic凭据漏识别及三协议未知模型502；r6补全工具说明内Bearer/Basic模板识别。真实凭据仍脱敏/拦截，strict、fail-closed及完整输入边界不变。
- Codex失败根因是SSE完成事件先到客户端、工具绑定却等到Relay EOF才保存。改为完成批次交给客户端前保存绑定；旧代码针对性用例失败、新代码通过，同时验证不完整流不得绑定。三协议共用该路径；已通过完整回归。
- 15:42只更新4004 Auto，Guard r6二进制不变。118文件冻结清单逐项核验；GPU Linux 378项Go（含子测试）、Python8项、vet及5程序构建通过。更新时只给4004临时503并排空请求，备份配置/二进制/源码/状态；内部密钥与重开入口前状态不变，所有其他容器基线不变。没有修改4000/4001。
- 15:42:54真实Codex CLI0.154.0完成读取合成文件、工具回传、最终答案；Claude Code2.1.233此前已通过同一工具流程。OpenCode1.18.4问候通过，但该工具请求仍被Qwen3Guard判为Controversial/Jailbreak；Hermes主机SSH关闭。四客户端整体兼容仍未验收完成。
- 本轮未改变状态schema、脱敏契约credential-redaction-v1或分类策略semantic-v5；无业务整段响应缓存。回退备份与二进制SHA见[本轮证据](evidence/2026-09-16-client-compatibility/README.md)。一次性有锁升级脚本不等于通用发布/回滚工具已完成；新版源码冻结后的README/文档差异单列校验。

### 2026-09-16 15:34 — 原生工具回合继续验收（未完成）

- r6在15:26仅更新4004 Guard；377项Go、Python8项、vet及5程序构建通过，Auto二进制不变。Codex真实问候通过，凭据模板误报修复。
- 三个原生客户端读取同一合成临时文件：Claude Code成功完成工具→结果→回答；Codex已执行只读工具，但收到SSE完成事件后关闭连接，首轮审计显示complete=true但失败，工具绑定计数0，下一轮503 `tool_context_unavailable`。正在补完成事件对客户端可见前持久化绑定的时序回归；不能用重试或关闭工具隔离掩盖。
- OpenCode该请求由Qwen3Guard判为medium/Jailbreak，不是Go凭据硬拦。保留strict和fail-closed；本轮尚未修改模型判定策略，普通工具任务质量验收未通过。
- 真实客户端汇总保存在[本轮证据](evidence/2026-09-16-client-compatibility/README.md)，失败不覆盖成成功；Hermes SSH仍关闭。


### 2026-09-16 15:21 — Codex工具认证模板误报（r6修复中）

- r5已在15:16更新4004；真实OpenCode1.18.4和Claude Code2.1.233问候通过，6项真实入口正反验证通过。Codex CLI0.154.0仍403，新增实测结果没有并入r5“通过”。
- 精确原因：Sites工具说明包含`OAI-Sites-Authorization: Bearer {siwc_bypass_bearer_token}.`，Header脱敏将完整文档占位符当认证值，Assignment随后也会截取Bearer。与Basic误报是两个匹配分支；不删除客户端工具、不放宽真实凭据保护。
- 修复目标：只识别完整Bearer/Basic变量引用或脱敏占位符；模板后混有真实文字/凭据仍保护，任意password值不会因括号而放行。补嵌套namespace工具回归并提升规则版本r6；契约和状态schema保持。
- Hermes本机未安装，CTYun SSH连接关闭，尚无真实Hermes进程验收；现有三协议结构测试不能代替该项。Codex模型目录元数据仍有fallback警告，完整压缩/工具/取消及高级能力继续待验收。


### 2026-09-16 15:12 — 新任务接续；四客户端兼容与r5修复（隔离验证中）

- 接续旧任务最后要求：OpenCode、Hermes、Codex、Claude Code均需支持，按真实客户端、三协议请求结构及高级能力分别验收；不因接口200宣称客户端全功能通过。已重跑旧源码失败用例，并完成第一轮修复针对性通过；扩展测试及整体验证待完成。
- 代码：`guard/redaction.go`按RFC7617校验Basic的Base64与冒号结构，识别短凭据；`guard/types.go`更新r5使旧安全决策失效；`auto/http.go`提前返回SDK可解析的404与无正文审计。测试在原`guard/redaction_test.go`、`auto/http_test.go`中维护。
- 正文契约仍为`credential-redaction-v1`，分类策略仍为`semantic-v5`；配置/内部密钥/用户Key权限和持久状态格式不变。暂未更新4004，4000/4001不切换。

### 2026-09-16 14:58 — OpenCode“你好”拒绝排查（历史定位）

- 关联本机OpenCode1.18.4错误与4004日志：auto两次403均为`secret_input_not_allowed`，用户消息为“你好”；脱敏标记仅出现在task工具description，原短语为`basic exploration`。Go的`authSecret`把Basic后任意8字符词当凭据，再与全文动作词组合硬拦截，Qwen和业务模型均未调用。
- 另有4次502来自客户端请求已移除的deepseek-v4-pro；不支持模型应返回404并记录请求，不应让客户端当网关故障重试。
- 修复范围：Basic使用base64解码并验证user:password结构，保留真实凭据保护；模型名提前校验；补正反用例并提升Guard规则版本使旧决策失效。保持fail-closed及各人Key策略，不修改OpenCode个人配置或4000/4001。
- 当前仅定位，修复/回归/4004更新待完成；将针对实际客户端请求结构验证，不再用只含裸“你好”的简化请求替代。

### 2026-09-16 14:45 — 4004已部署并通过入口验证；补齐升级维护方案

- **部署范围**：14:33:34启动GPU独立4004，东京前置9881；配套Auto8093、Guard8013，复用既有vLLM8001。使用regional模式和各人Sub2API Key；没有固定用户Key文件。服务器117份清单文件与冻结归档一致。
- **验证**：本地和Linux357项Go（含子测试）、Python8项、vet及构建通过。14:40真实入口17/17；Guard故障503审计与Nginx关联成功，分类/业务usage为0，自动恢复ready，14:43恢复后真实请求200。只验证一把授权真实Key，多Key隔离为模拟回归。
- **原服务保护**：4000/4001/4003、原Guard8011与vLLM8001的容器/镜像/启动时间及Nginx配置SHA保持不变；没有切流、修改Sub2API组策略或升级模型权重。[本轮证据](evidence/2026-09-16-4004/README.md)。
- **日志与回退**：独立根目录`/data/ai-gateway/acceptance-tokyo/logs`下分别保留Nginx、Auto审计/usage与Guard安全日志。停新增Compose三个服务即可撤销4004，不删除数据。此为撤销首次部署，不等于支持版本升级回滚。
- **维护方案**：按用户要求在[维护指南](维护指南.md#后续修改发布和回滚方案)明确版本制品、源码唯一来源、同包按区域推广、代码与密钥/状态/日志分离、流式排空、兼容性检查及回滚限制。发布工具、在线版本输出、优雅退出、部署锁和自动回退尚未实现；本条没有把规划写成已有能力。
- **未完成项**：完整客户端、多用户实流量、业务产物质量和最高effort、图片/WebSocket/compact、超长输入、多实例/数据库用量接入与生产切流。4004是正式替换候选，当前不能宣称所有能力与4000/4001完全等价。
- **文档收尾**：修正Auto README仍写仅pilot的旧说明；冻结版本不重写，后续文档差异单独校验。无Git提交/标签/推送，当前制品以源码SHA追溯。

### 2026-09-16 14:33 — GPU源码校验和Linux回归通过（开始部署4004）

- GPU独立目录`/tmp/ai-gateway-4004-20260916-1430/source`已逐文件核验117份源码；归档SHA见本轮evidence/package.json。
- Linux回归、vet、五程序静态构建及Python8项通过，输出`/tmp/gateway-linux-verify.GrfeQV`；接下来只创建独立`/data/ai-gateway/acceptance-tokyo`和三个服务。原服务基线已保存。

### 2026-09-16 14:30 — 4004区域入口本地验证通过（待GPU部署）

- 新增`auto/sub2api_identity.go`及回归：复用Sub2API、双Key隔离、撤销/过期/额度/限流/故障、伪造身份、冲突认证头、可信客户端IP、失败审计。没有另建用户或Key库。
- `cmd/auto-server`新增regional模式及启动约束；`http.go`可信身份覆盖静态ID、鉴权失败关联审计；区域业务请求不使用整段答案缓存，分类及Prompt Cache保留。
- 联合部署移除固定Key挂载；Nginx保留网页/账号/用量/计费、API别名、原认证头和3600秒超时。新增`tools/verify-entrypoint-live.py`只留元数据，用于已部署入口验证。
- 本轮357项Go（含子测试）、Python8项、vet、五个Linux构建通过；冻结源码及后续证据见`evidence/2026-09-16-4004`。GPU尚未启动新服务；4000/4001/4003保持。
- 替换前未完成项：图片/视频/Gemini、Responses WebSocket/compact、超过8MiB文本输入、完整客户端及多人实流量验收，详见联合部署说明。不能把入口上线当作这些能力通过。

### 2026-09-16 14:24 — 明确4004为正式替换候选（实施中）

- 用户要求4004按4000/4001同样的日常入口建设，验收后复用此代码/配置替换既有端口；不做固定测试Key或精简API专用服务。
- 14:22读取现有两份Nginx配置：全路径反代，东京9881/美西9880；认证头透传，支持Upgrade，超时3600秒，`proxy_cache off`，入口body上限200m。4004草稿缺少网页、管理和用量路径，正在补齐；生成类接口必须经过Guard，不能借兜底反代绕过。
- 整段业务响应缓存保持关闭作为与现有Sub2API权限/计费一致的运行策略，不是临时验收降级；Prompt Cache及Key隔离的分类缓存保留。开启业务响应缓存是后续独立策略变更。
- 14:21本地第一轮回归通过：356项Go（含子测试）、8项Python、vet、5个Linux构建。之后入口兼容调整仍需重新验证，不拿此结果代表最终部署版本。

### 2026-09-16 14:18 — 东京4004部署进行中（尚未启用）

- **授权与范围**：按用户要求部署独立4004；4000/4001/4003、现有Guard8011和vLLM8001保持。复用东京Sub2API管理的各人Key、额度和权限，不新建账号或Key系统。
- **待改代码**：移除验收服务的固定测试Key限制；请求先复用东京`/v1/models`校验，再按Key隔离会话、分类缓存和用量。自身API不返回数据库Key ID，网关使用带`key-hmac-v1:`前缀的本地HMAC标识，不能冒充Sub2API数字ID；真实计费用量仍由东京记录。
- **缓存边界**：4004先关闭网关整段业务响应缓存，让每次业务请求都经过Sub2API的最终模型权限与计费检查；分类缓存保留隔离，供应商Prompt Cache不受此开关影响。整段响应缓存重新启用须补全模型权限变更验收。
- **当前核对**：14:12 GPU确认4004/8013/8093空闲，新部署目录不存在；现有服务仍在运行。源码、回归、Linux构建和线上验收待完成，不能把本条当成已上线记录。
- **回退**：仅停止新增的独立Compose服务，保留独立日志与状态；不改区域Sub2API、共享分组或现有入口。

### 2026-09-16 14:10 — 文档补齐（仅文档，未部署）

本条时间是核对和补录时间，不倒填此前代码完成时刻。上轮虽有项目整理概述，但缺少具体迁移文件、统一命令及最后核对结果；部分组件说明仍沿用“两模块”和旧构建路径。

- **补齐记录**：下条项目整理补充旧/新位置及具体文件；记录 `.gitignore`、`.dockerignore`、统一验证脚本和4004部署草稿的改动。项目最终名称为 `ai-gateway`，`auto-guard` 只在整理过程中短暂使用，没有发布或部署。
- **修正文档**：Auto/Guard说明统一为一个module、两个服务程序；构建和验收脚本均从项目根目录使用 `cmd/`、`bin/`、`tools/`。总计划区分14:03本地回归、11:59旧GPU隔离验收和4004待实现；已完成的本地整理不再写成下一步待办。
- **补齐最终核对**：此前完成的223个本地文件链接检查、97条历史记录完整保留、112份清单文件匹配，原始结果见[14:04整理核对记录](evidence/2026-09-16-maintenance/document-verification.json)。本轮只改文档，另存[补录核对结果](evidence/2026-09-16-maintenance/documentation-followup.json)，不覆盖上次源码清单或测试结果。
- **验证与影响**：复核现有Go/脚本/配置是否仍匹配14:03回归清单；检查链接、表格和记录顺序。没有修改业务逻辑、策略/契约/缓存版本；本轮不重跑模型请求或部署，不把旧测试时间改成本轮时间。
- **待办**：4004的各人Sub2API Key、缓存前身份/额度校验尚未实现；新增六个部署文件仍是pilot草稿。生产4000/4001继续等待完整验收。

### 2026-09-16 14:04 — 项目整理（本地验证通过，未部署）

- **原因**：Auto、Guard 经过多轮修改后，源码、联合部署、文档和版本记录分散；后续供应商/模型/Sub2API 升级需要统一维护入口。
- **结构**：合并为 `script/gateway/ai-gateway` 和一个 Go module `local/ai-gateway`。Auto 位于 `auto/`，Guard 位于 `guard/`，入口在 `cmd/`；模型部署与联合部署在 `deploy/`，验收工具在 `tools/`，全部项目文档与 evidence 在 `docs/`。旧组件目录、旧 module 和分散的总计划/evidence 目录移除。
- **代码**：拆分 Auto SSE 聚合的公共、Chat、Messages 和增量处理；拆分 Guard 的类型/配置、规则、脱敏和判定主流程。Go 函数、类型、规则与现有测试逻辑保持，调整 import 路径、构建上下文和脚本路径。
- **部署草稿修正**：Compose 的 tmpfs 参数改成一个完整字符串；准备脚本校验联合部署文件也属于源码清单；Guard 配置样例不再指向旧版生产接口。这里只调整本地部署工具，没有运行部署。
- **维护约定**：按用户要求将“开发前读总计划/维护指南、修改完成同轮维护CHANGELOG、未验证或仅本地状态如实记录”写入项目AGENTS.md；文档未同步不算本轮完成。
- **文档与证据**：总计划保留当前决策和待办，历史执行表迁入本文件；增加上游升级/缓存版本/回归矩阵及证据索引。保留原有112个非空证据文件，删除3个空日志及1个空目录，未删真实调用、失败或源码哈希；旧诊断 `probe_test.go` 保留原文并改为 `.go.txt`，避免被统一Go测试误收录。
- **兼容与缓存**：HTTP 路径、环境变量、策略版本 `semantic-v5`、安全规则 `preflight-r4` 和正文契约 `credential-redaction-v1` 不变；代码目录/import 和构建命令改变，需要整包重新构建。没有因为目录重排清缓存或修改账号映射。
- **验证**：2026-09-16 14:03统一回归通过：Auto303、Guard31、Guard模型客户端2项（共336项含子测试），vet、五个Linux amd64静态构建、Python8项通过；498个Go非import声明迁移前后相同，Markdown文件链接及三份YAML检查通过。[本轮验证](evidence/2026-09-16-maintenance/README.md)。
- **部署/回退**：本轮未连接服务器、未部署；4000/4001、既有 Guard/vLLM 不变。下一次部署必须使用统一项目的新源码清单，不能沿用旧99文件清单或混合新旧二进制。
- **仍未完成**：4004已确认为东京独立多人测试入口，但程序仍是固定单 Key pilot。复用各自 Sub2API Key、缓存命中前鉴权/额度检查、完整客户端/业务产物、持久化与最高 effort 等验收继续按总计划推进，不能因为整理完成而标成生产可用。


#### 本轮具体文件与迁移对应

旧目录以仓库根为基准；新位置以 `script/gateway/ai-gateway/` 为基准。这里只记录最终目录，不把中间临时名称当成第二套项目。

| 最近改动 | 当前对应文件/目录 | 完成情况 |
| --- | --- | --- |
| `script/gateway/auto-gateway` 合并 | `auto/`，命令入口移到 `cmd/auto-server`、`cmd/auto-preview`、`cmd/semantic-eval`、`cmd/effort-probe` | import统一为 `local/ai-gateway/auto`，保留原有测试与冻结题集 |
| `script/gateway/guard-gateway` 合并 | `guard/`、`guard/model/`、`cmd/preflight-api` | 删除重复go.mod，两个服务从项目根go.mod构建 |
| Auto流式职责拆分 | `auto/buffered_sse.go`、`buffered_sse_chat.go`、`buffered_sse_messages.go`、`buffered_sse_delta.go` | 分开公共帧处理、Chat、Messages和增量合并；Responses结束事件仍在公共分发中，协议行为不变 |
| Guard职责拆分 | `guard/preflight.go`、`types.go`、`rules.go`、`redaction.go` | 主流程、类型/配置、规则、脱敏分开；模型客户端移至 `guard/model/client.go` |
| 六个联合部署文件 | `deploy/acceptance/{compose.yaml,nginx.conf.template,run-auto.sh,prepare.py,deploy.sh,README.md}` | 原Auto下acceptance-deployment整体移入公共deploy；固定Key pilot限制未消除，不能当作多人4004实现 |
| 部署构建与准备修复 | `deploy/guard/Dockerfile`、`docker-compose.yaml`；`deploy/acceptance/prepare.py`、`deploy.sh`、`compose.yaml` | Guard构建上下文改为项目根，两个程序使用cmd入口；部署文件也验证SHA；tmpfs参数修成单字符串 |
| 统一验证与产物位置 | `verify.sh`、`verify-on-linux.sh`、`tools/run-pilot.sh`、`tools/verify-redaction-live.py` | 全项目race/vet/build，Python与Shell检查；真实验收使用 `bin/auto-server`、`bin/preflight-api`，辅助工具在tools |
| 配置与构建排除 | `config/auto.example.yaml`、`.gitignore`、`.dockerignore` | 配置样例明确配套8013 Guard契约；忽略本地产物/运行目录和Python缓存；Docker排除证据目录及匹配的私密运行文件 |
| 文档和证据迁移 | 原 `doc/gateway/` 总计划/evidence → 本项目 `docs/`；新增CHANGELOG、维护指南与证据索引 | 保留97条历史执行表和112个非空历史文件；空日志/目录清理，旧探针改为文本快照 |
| 开发维护规则 | `AGENTS.md`，根/组件/部署README | 按最新文档开发，同轮维护CHANGELOG，真实部署与本地完成分开记录 |

[14:03统一回归](evidence/2026-09-16-maintenance/summary.json)为336项Go（含子测试）、8项Python、vet及5个Linux静态程序；[498个声明对比](evidence/2026-09-16-maintenance/declaration-comparison.json)通过。只做过三份YAML解析和路径检查，未执行本轮Docker Compose运行或GPU部署；目录整理不代表服务器源码已同步。

## 2026-09-16 — 输入契约与传输修复（隔离验收）

- Guard r3/r4 与 Auto 共用经过检查的脱敏正文；摘要或契约不匹配时503。修复随机签名前缀误拦，保留凭据使用拦截。
- 同步请求可显式启用一次 SSE 聚合；保留终止/取消/大小/工具/签名/usage，避免因超时重复生成；保持 JSON 大整数及已有 stream_options。
- DeepSeek count_tokens 的404与正常生成区分，明确预算估算来源，不把估算记作账单。
- r4本地和GPU Auto303/Guard33项race、vet/build、Python8项通过；真实17/17检查，44次外发未发现合成明文残留。限定样本，不等于全量安全或生产验收。[详细证据](evidence/2026-09-16-input-redaction/README.md)。
- 历史任务语义策略更新到v5；48次开发工具流程、15次三语SSE递增及Messages effort修复见[对应版本证据](evidence/2026-09-16-history-live/README.md)。东京共享组仍将xhigh/max映射high；不同轮次结果不混记。

## 2026-09-15 — Auto 协议、分类与多轮能力（分阶段验证）

- 增加三协议规范化、能力/effort映射、分类复核、会话/工具隔离、响应缓存、审计和分类/业务用量分账。
- 修复中文/英文/混合任务不一致、Astra不接受none、SSE认证遗漏、工具续接及失败状态处理；加入Claude Code兼容入口。
- 保留直连参数探测、语义评估、mock与真实业务的不同边界。[三语参数与分类](evidence/2026-09-15-auto-semantic/README.md)、[历史回放](evidence/2026-09-15-history-replay/README.md)。

## 2026-09-14 — Guard 初始部署与输入保护

- 部署Qwen3Guard-Gen-8B与Go Preflight API；补齐安全缓存、完整输入扫描、队列、分块、超时和异常审计。
- 完成当时版本的30分钟/18,000请求稳定性、长输入、过载与审计故障检查；GPU配置和确切时刻见下表。
- 这是初始部署历史；后续r4源码通过隔离验证不代表原生产实例已替换。

## 历史执行记录（迁移前，按时间正序）

| 时间（UTC+8） | 步骤 | 结果 |
| --- | --- | --- |
| 2026-09-14 14:05 | 部署前检查 | qiyuan-gpu 两张 H100：GPU0 运行现有 Qwen3.6-35B-A3B，GPU1 约使用 3.9GB；未发现 8001/8011 监听或 Guard 容器。东京、美西现有 Sub2API 服务正常。本步骤只读，未修改服务器。 |
| 2026-09-14 14:12 | 模型接口核对 | qiyuan-gpu 可访问 ModelScope 的 `Qwen/Qwen3Guard-Gen-8B` 元数据，权重约 16.4GB；实际输出是 `Safety: Safe\|Unsafe\|Controversial` 与 `Categories:` 两行标签。本地客户端已改为按该格式解析，未执行下载。 |
| 2026-09-14 14:18 | 构建环境核对 | qiyuan-gpu 无法在 30 秒内访问 `gcr.io`，本地 Dockerfile 已同步改用服务器已有的 `golang:1.27.1-bookworm` 和 `ubuntu:resolute-20260108` 镜像，并以非 root 用户运行。 |
| 2026-09-14 14:46 | Guard 权重下载与校验 | `Qwen3Guard-Gen-8B` 已下载到 `/data/vllm/models/Qwen/Qwen3Guard-Gen-8B`，目录约 16GB，5 个 safetensors 分片和配置文件齐全。5 个分片 SHA256 与 ModelScope 元数据一致。未启动容器。 |
| 2026-09-14 14:51 | vLLM 参数核对 | vLLM 镜像入口点本身是 `vllm serve`，本地 Compose 已按该入口点配置；直接执行 `vllm serve --help` 会重复入口，已在服务器用 `--entrypoint vllm ... serve --help` 核对参数。 |
| 2026-09-14 15:01 | Guard vLLM 启动 | `qwen3guard-vllm` 已运行，`GET http://127.0.0.1:8001/v1/models` 返回 `qwen3guard-gen-8b`，`/health` 为 200。GPU1 使用约 58.6GB/80GB，GPU0 业务容器未重启。 |
| 2026-09-14 15:17 | Preflight API 传输 | 本地源码归档已准备，但跳板机 `jumpserver.spicrhdk.com` 当前持续 DNS 解析失败，无法建立新的 SSH/SCP 连接。未在服务器启动 API，vLLM 保持运行。连接恢复后继续传输和构建。 |
| 2026-09-14 15:27 | Preflight API 启动与冒烟 | `guard-preflight-api` 已用 `guard-preflight-api:local` 启动，监听 `127.0.0.1:8011`。`/healthz` 和 `/readyz` 均返回 200；正常请求由 Qwen3Guard 判定 `allow`，延迟约 770ms；使用合成凭据测试样本的请求由 Go 规则立即 `block`，未调用 Guard，未调用上游。未使用真实 API key。 |
| 2026-09-14 15:29 | Guard 不可用故障演练 | 临时停止 `qwen3guard-vllm` 后，请求返回 HTTP 503、`code=preflight_unavailable`、`upstream_called=false`、`business_usage_written=false`，并写入 `/data/ai-gateway/logs/preflight-security.jsonl`；恢复 vLLM 后 `/readyz` 再次返回 200。当前累计指标：allow 1、block 1、unavailable 1。 |
| 2026-09-14 15:31 | Guard 并发基线压测 | 服务器本地对 8011 发送唯一输入：并发 1/5/10/20/40/64 的吞吐分别为 6.27/40.98/61.75/92.09/84.70/124.05 req/s；p95 分别为 100/131/161/212/461/498ms，全部 0 错误。当前 `max-num-seqs=32` 在并发 40、64 未达到 p95<300ms，但吞吐超过 10 req/s，需调参后复测。 |
| 2026-09-14 15:33 | `max-num-seqs=64` 复测 | 重启后模型正常，GPU1 使用约 60.3GB。并发 1/5/10/20/40/64 吞吐为 6.32/39.02/62.48/93.49/88.10/132.01 req/s，p95 为 90/129/158/210/445/473ms，全部 0 错误。高并发延迟略有改善但仍未达到 300ms，暂不宣称验收通过。 |
| 2026-09-14 15:38 | 显存参数复测与回退 | `gpu-memory-utilization=0.90` 在并发 20/40/64 的 p95 为 712/347/448ms，GPU1 使用约 76.5GB，收益不稳定；已回退到 `0.70`、`max-num-seqs=64`，按新的 10% 显存余量标准继续验证。 |
| 2026-09-14 15:44 | 长输入与缓存核对 | 使用重复 `x` 的 8K/32K **字符**样本请求耗时约 824/316ms，均返回 `allow`；这不是 8K/32K token 验收。随后已补上安全决策缓存，但长上下文分块和 token 计数仍待补齐，阶段 B 尚未完成。 |
| 2026-09-14 15:50 | 安全决策缓存验证 | 更新镜像并重启 `guard-preflight-api` 后，同一会话、同一输入连续请求两次：第一次约 207ms，第二次约 0ms；vLLM 日志只出现 1 次 `/v1/chat/completions`，证明缓存命中未重复调用模型。 |
| 2026-09-14 15:52 | Go 测试 | 使用服务器已有 `golang:1.27.1-bookworm` 镜像执行 `go test ./...`：`go-check` 和 `guard-deployment` 全部通过，`preflight-api` 编译包通过（无测试文件）。 |
| 2026-09-14 15:55 | 30 分钟稳定性测试启动 | qiyuan-gpu 后台启动 `guard-soak.py`，目标 10 req/s、唯一脱敏输入，日志为 `/data/ai-gateway/logs/guard-soak.jsonl`。启动约 105 秒时累计 1,060 请求、0 错误、滚动 p95 约 152ms；两容器运行正常，GPU1 约使用 72.8GB。 |
| 2026-09-14 15:56 | 监听与防火墙核对 | 8001 和 8011 均只监听 `127.0.0.1`；主机 `INPUT` 默认 ACCEPT，若改成 `0.0.0.0` 会产生暴露风险，因此在明确 Gateway 私网地址和 ACL 前不开放远程访问。 |
| 2026-09-14 16:03 | 显存余量标准调整 | 按确认后的运行目标，将 Guard 验收显存保留从 20% 调整为至少 10%。当前 `0.70/64` 配置 GPU1 使用 60,339/81,559 MiB，剩余约 26%，满足余量要求；此前 `0.85` 测试最低约 10.5%，仅作为压力数据，不标记为稳定配置。 |
| 2026-09-14 16:05 | 并发目标调整 | GPU1 确认为 Guard 专用，不与 GPU0 业务模型争用资源。后续以可持续并发和吞吐最大化为优先，`max-num-seqs=64` 只是当前基线，不是上限；将在 `64/96/128/192/256` 等档位逐级压测，选择满足至少 10% 显存余量、无 OOM、队列等待小于 1 秒的最高稳定档位。 |
| 2026-09-14 16:20 | 并发档位验证 | 依次启动 `max-num-seqs=96`、`128`、`192`（均为 `gpu-memory-utilization=0.70`）。在同一轮 64/96/128/192/256 并发扫描中，`192` 达到 147.54 req/s、0 错误；`256` 约 159.81 req/s 但 126/256 请求超过 1.5 秒超时，暂不选用。`192` 配置再运行 180 秒、10 req/s（1,800 请求）0 错误，滚动 p95 150.8ms，GPU1 峰值约 63,295/81,559 MiB，剩余约 22.4%。当前服务器运行 `192`，尚未宣称 Guard 阶段整体验收通过。 |
| 2026-09-14 16:56 | 30 分钟稳定性验收 | `max-num-seqs=192`、`gpu-memory-utilization=0.70` 持续运行 1,800 秒，10 req/s 共 18,000 请求，0 错误；最终滚动 p95 152.0ms。vLLM 与 Preflight 容器无重启、无 OOM，`/readyz` 返回 ready；GPU1 保持 63,295/81,559 MiB，剩余约 22.4%。30 分钟稳定性项通过，长 token 分块、队列满载和审计故障项仍待验收。 |
| 2026-09-14 17:22 | Guard 代码修复与重建 | 修复缓存先于 Go 规则、审计写失败被忽略和队列参数未生效的问题；Guard 客户端增加 20,000 字符保守分块并聚合全部分块 verdict。服务器 `go test ./...` 全部通过，镜像 `guard-preflight-api:local` 重建并重启。 |
| 2026-09-14 17:25 | 长输入分块验收 | 45,000 字符唯一输入实际触发 3 次 `/v1/chat/completions`，返回聚合 verdict，HTTP 200；没有静默截断。当前分块按保守字符预算实现，精确 token 计数仍待接入 tokenizer 后再做 32K token 验收。 |
| 2026-09-14 17:25 | 队列满载验收 | 1,000 并发请求中 257 个完成、743 个返回 HTTP 503，原因码均为 `queue_timeout`；`preflight_decisions_total{decision="unavailable"}` 增加 743。证明 `PREFLIGHT_MAX_QUEUE=128`、`PREFLIGHT_QUEUE_TIMEOUT=1s` 已生效，过载不会无限堆积。 |
| 2026-09-14 17:25 | 审计故障验收 | 临时测试容器将审计 sink 指向不可写 `/dev/full`，凭据拦截请求返回 HTTP 503、`decision=unavailable`，原因码包含 `audit_write_failed`，`upstream_called=false`、`business_usage_written=false`；测试容器已删除，生产容器未改动。 |
| 2026-09-14 17:30 | 精确 token 与超时边界 | vLLM `/tokenize` 对 32K 样本返回约 32,007 tokens。生产 1.5 秒超时下按设计返回 503 `classifier_error`，无截断；临时 10 秒实例完成 32K 样本约 1,576ms 并返回 allow。为完成长输入验收，生产请求超时调整为 3 秒，队列超时仍为 1 秒。 |
| 2026-09-14 17:36 | 生产回归验收 | 新镜像和 3 秒请求超时已部署。32,007 token 请求生产实例 HTTP 200、约 1,559ms，实际调用 10 个分块；1,000 并发中 256 个完成、744 个因 `queue_timeout` 返回 503，`/readyz` 仍为 ready。Guard 阶段的稳定性、长输入、队列满载和审计故障验收均已有证据，进入 Auto 前仍需整理发布包和第二故障域方案。 |
| 2026-09-14 17:39 | 阶段范围冻结 | 按确认先完成 Guard，Guard 全部验收、发布包和回滚演练完成后暂停；Auto 的生产接入、模型切换和业务流量灰度暂不执行，后续单独启动。 |
| 2026-09-14 17:45 | 完整输入扫描回归 | 修复并部署后，metadata、工具 schema、附件 source 中的 JSON/文本凭据均由 Go 规则直接拦截，返回 `secret_input_not_allowed`、`upstream_called=false`，未调用 Guard。服务器 Go 测试全部通过，Guard 阶段代码和核心故障回归完成；剩余工作仅为发布包归档、回滚演练和第二故障域规划。 |
| 2026-09-14 17:53 | VPN 网络访问开放 | 按另一台 GPU 的方式，将 Preflight API 从 `127.0.0.1:8011` 改为 `0.0.0.0:8011`，GPU 上本机访问 `192.168.64.16:8011/healthz` 已返回 200；Qwen3Guard vLLM 的 8001 仍保持回环监听。当前可通过公司 VPN 访问 `http://192.168.64.16:8011`。重启前容器配置已备份到 `/data/ai-gateway/backup/`。 |
| 2026-09-14 17:57 | 解除阶段冻结 | 按最新指示解除 Auto 冻结。Guard 保持当前 `192/0.70` 配置和 VPN 访问方式不变；开始 Auto 本地核心测试与独立路由验证，暂不连接东京、美西生产入口。 |
| 2026-09-14 18:19 | Auto GPU 独立回归 | 通过 JumpServer GPU 终端，在 `/tmp/auto-gateway-test` 使用 `golang:1.27.1-bookworm` 运行 `go test -v ./...`，全部通过（分类、逐级升级/重置、工具循环、缓存隔离、自动 reasoning、provider 映射、usage 和 `auto` 公共模型约束）。本地三份源码与 GPU 临时目录 SHA-256 一致；未修改 Guard 生产容器，未接入东京或美西真实流量。 |
| 2026-09-14 18:32 | Auto 协议归一化回归 | 新增 Chat Completions、Responses、Anthropic Messages 的协议中立输入归一化及异常输入测试；首次测试发现并修正“工具+图片+翻译任务应为 medium”的错误断言，修正后 GPU 上 `go test -v ./...` 全部通过。客户端 `reasoning_effort` 不参与分类；尚未接入上游或生产监听器。 |
| 2026-09-15 08:33 | Auto HTTP 本地预览回归 | 新增 `/healthz`、`/v1/models` 和 `/v1/route` 本地适配器：只公开 `auto`，将三种协议请求统一归一化后执行分类和路由，返回 `upstream_called=false`。测试覆盖模型白名单、Responses 客户端 reasoning 忽略、非 auto 拒绝；GPU 上全量 `go test -v ./...` 通过。发现并修正 Responses `reasoning` 被误判为结构化输出的问题；未部署监听器。 |
| 2026-09-15 08:39 | Auto mock Guard 链路回归 | HTTP 预览支持可注入的 `PreflightChecker`。mock `block` 返回 403、mock `unavailable` 返回 503，二者均在路由前终止且 `upstream_called=false`；mock `allow` 才进入 Auto。GPU 上全量 `go test -v ./...` 通过；真实 Guard 仍未接入。 |
| 2026-09-15 08:41 | Auto 每日 usage 聚合回归 | 新增内存 `DailyUsageRecorder`，按 UTC 日期、区域、`effective_model`、`effective_reasoning_effort` 和 `api_key_id` 分桶，分别记录 request/attempt、输入、缓存命中/未命中、输出、成功失败及升级降级。GPU 上全量 `go test -v ./...` 通过；尚未接入业务数据库。 |
| 2026-09-15 08:42 | Auto 响应缓存键回归 | 新增 HMAC 响应缓存键和请求摘要，模型、有效 reasoning、版本、区域、Key ID、客户端 cache key 或请求变化都会产生不同键；同一输入保持稳定。GPU 上全量 `go test -v ./...` 通过；未接入现有业务缓存。 |
| 2026-09-15 08:44 | Auto 本地预览程序构建 | 新增 `cmd/auto-preview`，默认监听 `127.0.0.1:8091`，可启动 `/healthz`、`/v1/models`、`/v1/route` 预览；GPU 上 `go test ./...` 和 `go build ./cmd/auto-preview` 均通过。该程序仍不发送上游请求。 |
| 2026-09-15 08:45 | Auto HTTP GPU 冒烟 | GPU 临时目录使用 `CGO_ENABLED=0` 静态构建后启动预览程序，实际 curl 验证 `/healthz` 返回 `{"status":"ok"}`、`/v1/models` 仅返回 `auto`、架构+调试请求选择 `gpt-5.6-terra` 并生成 `low`，且 `upstream_called=false`。首次动态构建因主机 glibc 较旧退出，已记录静态构建要求；临时进程已停止。 |
| 2026-09-15 08:46 | Auto 缓存仿真回归 | 运行既有 `auto-routing-cache-sim.mjs`：逐级升级策略命中率 72.7%、质量 100%、35,000 miss tokens；单次升级命中率 81.8% 但 8 轮低于目标档位；多次升级命中率 77.3%、质量 100%。结果支持保留多次升级、会话粘性和缓存按实际模型隔离，暂不接入生产缓存。 |
| 2026-09-15 08:51 | Auto 流式/长连接边界回归 | 新增 `DecideRouteAtTurnBoundary`：SSE 或 WebSocket turn 进行中强制保持当前模型，只有 turn 完成后才重新分类并允许升级；协议 `stream=true` 同时进入 `stream` 能力门，缺少流式能力的模型会被过滤。该项已写入本地测试，GPU 临时目录已清理，尚未做服务器复测。 |
| 2026-09-15 08:52 | Auto 上游参数映射回归 | 新增 `BuildProviderParameters`，按真实模型能力矩阵生成 OpenAI Responses 的 `reasoning.effort` 或兼容协议的 `reasoning_effort`；不支持目标强度时只使用适配器映射/默认行为，不透传客户端字段。测试已写入本地，尚未接入真实上游。 |
| 2026-09-15 08:53 | Auto HTTP 流式边界接入 | `/v1/route` 增加 `stream_active` 预览字段并调用统一 turn-boundary 路由入口，验证进行中的流式响应不会因为后续高难度特征切换模型；SSE/WebSocket 真实转发仍留在阶段 D。 |
| 2026-09-15 08:55 | Auto 硬失败故障切换回归 | `hard_capability_failure=true` 时在 turn 边界排除当前失败模型，按相同能力门重新排序并返回 `failover/provider_failure_failover`；若没有备用模型则拒绝，不等待健康度刷新，也不在流式响应中途切换。已写入本地测试，尚未接入真实上游。 |
| 2026-09-15 08:56 | Auto Linux 验证脚本 | 新增 `verify-on-linux.sh`，在目标 Linux 主机通过固定 `golang:1.27.1-bookworm` 容器执行 `gofmt`、`go test -v ./...` 和 `CGO_ENABLED=0` 静态构建；脚本不上传、不启动生产服务。最新流式和 failover 代码尚未在 GPU 临时目录复测。 |
| 2026-09-15 08:57 | Auto 本地源码检查 | 完成 Bash 语法、Go 源码控制字符和文件清单检查；本机无 `go`/`docker`，因此没有把静态检查当作编译通过。下一步使用 `verify-on-linux.sh` 在 GPU 临时目录复测后，再进入真实上游适配设计；东京、美西仍未接入。 |
| 2026-09-15 08:58 | Auto 业务缓存写入策略 | 新增本地 `ResponseCachePolicy`：只有 Guard `allow`、非流式、无工具/图片且响应不超过 4 MiB 才允许业务响应缓存写入，默认 TTL 24 小时；`block/unavailable` 一律不写。Prompt Cache 与路由决策缓存保持独立，测试已写入本地。 |
| 2026-09-15 08:58 | Auto 路由审计字段 | 新增 `BuildRouteAudit`，记录请求/实际模型、自动 reasoning、供应商参数映射状态、动作、原因、能力门、流式标记和分类分数；客户端 reasoning 不进入审计的请求输入。测试已写入本地，尚未接入业务日志 sink。 |
| 2026-09-15 09:14 | Auto GPU 全量回归复测 | 最新归档解压到 `/tmp/auto-gateway-verify-20260915` 后，首次发现流式测试样本断言过严；调整为明确高难度样本后再次执行 `./verify-on-linux.sh`，`verify_exit=0`，`gofmt`、全量 `go test -v ./...` 和静态构建均通过。验证只在临时目录进行。 |
| 2026-09-15 09:15 | Auto GPU HTTP 冒烟复测 | 临时启动静态 `auto-preview` 后，`/healthz` HTTP 200、`/v1/models` 仅返回 `auto`、`/v1/route` HTTP 200 并选择 `deepseek-flash/none`；随后用高难度请求配合 `stream_active=true` 返回 `action=keep`、`reason=stream_in_flight`。预览进程已停止，未触碰 Guard 或生产服务。 |
| 2026-09-15 09:16 | Auto GPU 故障切换冒烟 | 临时预览先路由高难度请求，再发送 `hard_capability_failure=true`；第一次保持 `deepseek-flash`，第二次返回 `action=failover`、`reason=provider_failure_failover`，切换到 `gpt-5.6-terra`，`upstream_called=false`。临时进程已停止。 |
| 2026-09-15 09:17 | Auto GPU 验证收尾 | 已停止全部 Auto 临时预览进程；当前 GPU 仅保留 Guard 生产服务。Auto 本地核心阶段的协议、能力门、自动 reasoning、缓存策略、流式边界、故障切换、审计和 usage 回归均有 GPU 证据，下一阶段是本地上游适配/端到端模拟，仍不直接接入东京或美西。 |
| 2026-09-15 09:18 | Auto 临时进程清理确认 | GPU 上 `pgrep -af auto-preview` 无残留进程；验证目录和归档均位于 `/tmp`/`/root` 临时位置，Guard 生产容器、8011 监听和现有业务模型未修改。 |
| 2026-09-15 09:19 | Auto 端到端模拟管线 | 新增本地 `Pipeline` seam，固定执行 preflight → route → provider 参数映射 → 缓存资格 → mock upstream；测试覆盖 block/unavailable 不调用上游、allow 只调用一次、参数映射和上游失败不重试。代码尚未上传 GPU，下一轮继续服务器复测。 |
| 2026-09-15 09:32 | Auto GPU 管线归档校验 | GPU 上已确认上传归档包含 `pipeline.go` 与管线测试；首次回归只失败于旧断言仍读取顶层 `reasoning_effort`，而 `responses` 的实现按文档生成嵌套 `reasoning.effort`。在 GPU 临时目录修正该断言后 `verify_exit=0`；本地已修正测试并重新打包，待用新归档复测，未修改生产服务。 |
| 2026-09-15 09:49 | Auto GPU 修正版正式回归 | 新归档时间戳为 09:43:31、大小 30,720 bytes，已确认测试包含 `reasoning, ok := ...["reasoning"]` 修正版断言。全新临时目录直接执行 `./verify-on-linux.sh`，`verify_exit=0`；`gofmt`、全量 Go 测试和静态构建均通过，未做临时代码修改，未启动或修改生产服务。 |
| 2026-09-15 09:56 | Auto 本地缓存端到端模拟 | 新增线程安全 `MemoryResponseCache` 与 `PipelineMeta`：缓存键由区域、Key ID、模型、有效 reasoning、版本和规范化请求摘要隔离；allow 且可复用时先查缓存，命中跳过上游，成功响应才写入 TTL 缓存，上游失败不写入；同时按实际模型/有效 reasoning/API Key 写入每日 usage。新增命中、过期和失败不落缓存测试；本机无 Go/Docker，已完成 `git diff --check`，待上传 GPU 做 Linux 回归。 |
| 2026-09-15 10:15 | Auto GPU 缓存完整回归及修复 | 上传包 31 个普通文件与本地逐文件 SHA-256 一致，原有测试通过。补充用例复现响应缓存命中重复计 token、HTTP 503/429/302 被缓存、跨协议复用、无效 Guard verdict 未返回 unavailable、缓存嵌套 map 共用问题。修复后在 `/tmp/auto-cache-fixed-20260915.y6g6aG` 执行全量测试和静态构建，`verify_exit=0`；`go vet` 与 race 检测均通过，38 个顶层测试、含子用例共 66 个通过，0 失败。29 份格式化 Go 源码已同步回本地。证据见 [本次缓存回归](evidence/2026-09-15-auto-cache)，未接真实上游或区域流量。 |
| 2026-09-15 10:21 | Auto 缓存验收归档 | SSH 已恢复可用，缓存修复版归档与 GPU 测试目录已逐字节核对。当时的缓存专用归档 SHA-256 为 `527945540280f6b5e5e274db05f8def7d98bef6f1e418e85d36e262374d19a1f`，29 份 Go 源码一致；日志已保存至文档证据目录。10:18 只读检查 Guard 两容器仍运行，`/readyz` 为 ready；Auto 未启动生产监听器。 |
| 2026-09-15 11:00 | Auto 最终源码归档 | 协议 payload 回归后的 31 份 Go 源码已从 GPU 格式化测试目录同步回本地，最终归档 `/tmp/auto-gateway-latest.tgz` SHA-256 为 `733717cde4e90c9ae6fe2c96dcef192a523d195d96d1b476e6fd3e565aa28fb8`。该归档仅供后续临时验证，未部署到生产。 |
| 2026-09-15 11:15 | Auto 会话状态隔离 GPU 回归 | 新增带 TTL 的 `MemorySessionStateStore` 和 `PipelineMeta.SessionID`，按已鉴权会话保存路由状态；不同会话独立选择，同一会话保持粘性，过期后重新选择，缺少会话 ID 时不启用隔离存储。GPU 临时目录全量测试、静态构建、`go vet` 和 race 检测均通过，race 记录 74 个测试/子测试通过、0 失败；33 份 Go 源码已同步回本地。证据见 [会话隔离回归](evidence/2026-09-15-auto-session)，未接生产流量。 |
| 2026-09-15 11:20 | Auto 会话版最终归档 | 本地最终归档 `/tmp/auto-gateway-latest.tgz` 已包含会话隔离和协议 payload，SHA-256 为 `7fc4d8030766a896f7a2493baa56bac64c8f91824d282e39caf5ef25b3fff63c`，同一归档已保存到 GPU `/tmp/auto-gateway-latest-final.tgz`。仅作为后续临时验证包，不启动生产 Auto。 |
| 2026-09-15 13:33 | Auto 共享缓存与传输 seam | `MemoryResponseCache` 增加容量上限/LRU；同一完整缓存键的并发 miss 合并为一次上游调用，避免突发放大；新增 `HTTPUpstreamClient` 非流式 JSON 转发、token 内存注入、响应大小限制和 usage 提取，以及独立 `OpenStream`/`RelaySSE` 与 WebSocket 双向 relay 生命周期 seam。新增文件会话状态持久化、0600 fsync JSONL usage spool 和路由审计 spool；均未接入东京、美西或生产数据库，待 GPU Linux 全量回归。 |
| 2026-09-15 13:40 | 阶段 D 客户端 seam | 新增 `HTTPPreflightChecker`，向 Guard 只发送脱敏文本/工具 schema，Guard 非 2xx、无效决策和不可用统一 fail-closed；`HTTPServer` 增加非流式 `/v1/chat/completions`、`/v1/responses`、`/v1/messages` 入口和 `/metrics`，provider token 只从内存配置注入。流式/真实区域监听器仍待独立接入与验收，未改变生产服务。 |
| 2026-09-15 14:21 | Auto 阶段 2 GPU 全量回归 | stage6 归档 SHA-256 为 `460fa781bd2b7c2a8225b72c56525a91cbd4c18b4de5e569a408df92721b2508`。GPU Docker 内 `gofmt`、`go test -v ./...`、`go vet ./...`、`go test -race -count=1 ./...` 和 `CGO_ENABLED=0 go build` 全部通过；新增传输、Guard 客户端、SSE/WebSocket、文件状态、usage/审计 spool、LRU 和 miss 合并测试均通过。证据见 [Auto 阶段 2](evidence/2026-09-15-auto-stage2)。仅临时目录验证，未启动 Auto 生产监听器。 |
| 2026-09-15 14:28 | 阶段 D 固定顺序端到端回归 | stage7 归档 SHA-256 为 `fcc3f7f9301dfae688e25332b5e8f28b6bc51b6792a9172182cb7a8b01d16940`。Guard mock 的 allow → Auto → provider、block → 403 且不增加上游调用、unavailable → 503 且不增加上游调用均通过；同一归档在 GPU Docker 内的 gofmt、全量测试、vet、race 和静态构建通过。证据见 [阶段 D 回归](evidence/2026-09-15-phase-d-integration)。仍未使用东京/美西真实测试 Key。 |
| 2026-09-15 14:35 | 区域到 GPU Guard 网络边界确认 | 东京和美西服务器访问 GPU `192.168.64.16:8011/healthz` 均超时；GPU 入站只按 VPN 入口开放。GPU 上的 `tokyo-sub2api-proxy:4000`、`us-sub2api-proxy:4001` 分别反代到 `106.14.254.110:9881`（东京）和 `:9880`（美西），供公司 VPN 用户访问；两地 Sub2API 本身不直接对外。GPU 主机主动访问两个前置 `/health` 均返回 200。Auto/Guard 接入点应位于 GPU 的 4000/4001 VPN 入口链路内，不能把 8011 暴露公网。 |
| 2026-09-15 15:05 | 拓扑更正与 stage9 上传 | 按用户确认，统一修正架构、Guard 完成条件和阶段 D：公司 VPN 用户 → GPU Nginx → GPU Auto/本机 Guard → 两地前置 → Sub2API，不再把私有 relay 或区域 Guard 副本作为网络接入前置。SCP/SFTP 写入 `/tmp` 失败，但 SSH 检查 GPU 路径和空间正常；通过 SSH 标准输入上传至 `/tmp/auto-gateway-stage9.VMeN7R.tgz`，SHA-256 为 `9b43e3a4f001b589d620c2d4e225046a25a288a8a92629f5e8090acf1b3422c9`，与本地一致。 |
| 2026-09-15 15:13 | stage9 GPU 临时真实链路 | GPU 临时 Auto 仅监听 `127.0.0.1:8092`，本机 Guard `8011`，东京前置 `9881`。健康检查通过；安全样本经 Guard allow 后到达真实东京前置，脱敏假 Token 返回 401，证明链路和认证透传边界生效；凭据样本由 Guard 返回 403 且未调用上游。审计/usage 各写入一条脱敏事件；随后停止临时进程并确认 8092 已释放。未修改 4000/4001 或生产配置。 |
| 2026-09-15 15:18 | preflight 安全审计修复与 stage10 | 发现 block/unavailable 在旧实现中只返回 HTTP 状态而没有写安全审计；新增 `preflight_blocked`/`preflight_unavailable` 审计事件，allow 路由也记录 preflight 决策和原因码，并新增不含提示词的回归测试。stage10 本地归档 SHA-256 为 `d51b321a1a9c33f69fe51e10a9fd8a61ea0d53d9fa9868b394621ab00457cf73`；GPU 全量 gofmt/test/vet/race/静态构建通过。真实 Guard 凭据阻断返回 403 且写入 `secret_input_not_allowed`，Guard 不可达返回 503 且写入 `guard_transport_error`，均未调用上游；临时进程已停止。证据见 [stage10 runtime](evidence/2026-09-15-auto-stage3/stage10-runtime.md)。 |
| 2026-09-15 15:43 | stage11 流式认证透传回归 | 发现 SSE 转发路径遗漏客户端认证头；已补齐 `Authorization`、`X-API-Key` 等 allowlist 透传并增加回归测试。stage11 归档 SHA-256 为 `e8da088920cccc2c70116238146ffeffedfe317b8780f9f51549731445974c8d`；GPU 全量 gofmt/test/vet/race/静态构建通过。东京 `test` Key 已完成真实请求验证，未改 4000/4001。 |
| 2026-09-15 16:00 | stage12 真实会话切换 | 修复 HTTP 入口固定 `turn=1` 导致会话冷却不前进的问题，按会话状态递增 turn。东京 `test` Key 真实低→高→高→低请求：前三轮保持 `deepseek-flash`，第四轮按 `quality_upgrade` 升级到 `gpt-5.6-luna`，生成 effort `low`；首次升级因测试 Key 尚无 GPT 权限返回 503；用户更新权限后复测 HTTP 200、`gpt-5.6-luna`、`finish=stop` 且 usage 完整，确认是 Key 权限而非路由或 Guard 问题。临时进程已停止，未改 4000/4001。证据见 [stage12 runtime](evidence/2026-09-15-auto-stage3/stage12-runtime.md) 和 [stage12 retest](evidence/2026-09-15-auto-stage3/stage12-retest.md)。 |
| 2026-09-15 16:04 | stage12 真实缓存记账 | 东京 test Key 同一会话同一请求连续两次均 HTTP 200、`deepseek-flash`；第一次 `attempt=true`、输入/输出 14/43，第二次 `attempt=false`、`response_cache_hit=true`、输入/输出 0，且两次均保留 usage 与审计事件。临时进程已停止，未改 4000/4001。证据见 [stage12 cache runtime](evidence/2026-09-15-auto-stage3/stage12-cache-runtime.md)。 |
| 2026-09-15 17:07 | 小源重建任务路由诊断（结论已更正） | 6 类脱敏重建任务在 GPU 临时链路测试，未携带认证凭据，allow 后均 HTTP 401，没有成功业务用量。flash→terra 为一个会话，astra/xhigh 是另一个新会话的初始选择，不能算连续跨模型成功。长探针含追加英文术语，不能计入分类质量验收；正常开发任务的 403 列为疑似误拦。原“完整真实回放/最高档升级完成”结论撤回。见 [更正证据](evidence/2026-09-15-auto-xiaoyuan-replay/runtime.md)。 |
| 2026-09-15 18:00 | 多语言分类基线与报告纠正 | 36 条中/英/混合对照在 GPU 原 stage12 分类器执行：flash 22、luna 5、terra 9，无高档；2/12 同义组的 intent/model/effort 不一致，另有三语共同误判。临时词表补丁已撤回，新增多语言语义分类、日志对账及独立质量验收要求，均标记待实现。东京截至 17:55 测试 Key 业务记录只有 DeepSeek 16、Luna 4，证实此前 astra 成功说法不成立。随后带 Key 补测 Luna 200，astra 仍为失败、0 token，东京未见对应业务记录；未完成最高档验收。见 [诊断基线](evidence/2026-09-15-multilingual-classifier/README.md)。 |
| 2026-09-15 18:37 | 语义分类本地实现与真实供应商评估 | 本地已实现三语语义 schema、独立复核、分类 HMAC 缓存、客户端 effort 忽略、分类/业务用量分账及请求结果审计；修复工具调用与完整 provider payload 丢失、跨 Key 会话状态、无效 200 响应和 SSE 结果日志。当前全量 Go 测试通过，Linux 同步和完整验收尚未完成。东京真实分类开发集：flash/none→Sol 与 Luna/none→Sol 均为 36/36 合法输出、12/12 同义组难度一致；flash 保留集 142/144 成功、44/48 同义组一致（91.67%），未达 95% 门槛，继续候选比较。Astra 直连 Chat/Responses 均 200、有真实模型和 usage，但不是 Guard/Auto 全链路验收。GPU SSH、堡垒机和 VPN Guard API 均不可达，待网络恢复；4000/4001 未变。 |
| 2026-09-15 18:49 | 三语保留集结果与 Astra effort 修复 | 48 个保留任务三语一致率：flash→Sol 44/48（91.67%），Luna→Sol 46/48（95.83%），Sol→Astra 47/48（97.92%）。这是组合分类器的语义评估，不是业务模型最优性或 Guard/Auto 接入验收。另一个 Astra 主分类批次误发 `none`，已停止：164 条已保存样本中 Astra 成功为 0，最终输出来自 Sol/low 复核；该批次排除出模型比较。东京最近 20 条 Sol/Low 成功记录由此产生。已按官方文档及服务器 400 错误修正 Astra 支持表为 low/medium/high/xhigh/max，none/minimal 映射 low；GPT-5.6 三款支持 none/low/medium/high/xhigh/max。本地测试通过，开始修复后小批真实复测。 |
| 2026-09-15 19:10 | 全模型 effort 与新增客户端范围 | 162 条真实参数探测全部传输完整，但高档响应回报 high，最高档执行仍未验收。本地修复 DeepSeek 原生映射、Responses 参数位置、客户端 output_config/configuration_update 控制入口，并统一审计/usage/缓存使用实际发送 effort。新增 Claude Code 别名和工具续接兼容；图片模型/专用 review 模型进入待验证能力分支。SSH 仍被堡垒机关闭，GPU 与生产 4000/4001 未改。 |
| 2026-09-15 19:33 | 三语、全协议和客户端兼容验证归档 | 三语分类 Sol→Astra、Astra（low）→Sol 各 180/180 合法结果，保留集一致率均 47/48。五模型三协议 243 次参数探测及四个 Claude 兼容名的 Messages/count_tokens 8 次探测均 HTTP 200；属于直连对照。只读确认 codex-E 将 xhigh/max 映射 high，最高档未通过；共享配置未改。修复计数路径分类认证头遗漏，本地格式/test/vet/race/Linux 静态构建通过，194 个测试/子测试无失败。相对 stage12 新增 17、修改 28 个文件，见 [证据及源码清单](evidence/2026-09-15-auto-semantic/README.md)。同时修复执行表格断裂和单元格竖线转义，GPU 尚未同步。 |
| 2026-09-15 19:58 | 历史任务回放与第二轮修改验证 | 从 4 个其他项目的历史任务重建 16 个检查点、48 条三语输入。修复补充要求丢失、工具回合重置/串模型、嵌套图片误当文本、未配置 Guard 默认放行、状态写盘错误被忽略、畸形 SSE 被记成功和工具历史进入响应缓存；新增客户端会话识别、工具 ID 绑定、Guard 请求关联。Auto 本地 test/vet/race 通过，258 个测试/子测试无失败；Guard 16 个无失败，两模块 Linux 静态构建通过。历史样本外发被自动审批拒绝，具体授权问题待答复，未发送；GPU 连接仍失败。见 [历史回放证据](evidence/2026-09-15-history-replay/README.md)。本轮未上传代码，未修改 4000/4001。 |
| 2026-09-16 09:07 | 历史题集真实修订回归与网络恢复 | 用户确认 48 条脱敏样本外发后，v4 Sol→Astra 三语一致率 15/16、Astra→Sol 13/16；细化通用难度定义后 v5 Sol→Astra 16/16，均 48/48 有效分类，但不含 Guard 或业务模型。修复历史题集漏统计、计数异常审计遗漏和分类失败 effort 缺失；Auto 本地 race 262、Guard 16 个通过，Python 5 个通过。用户修正 Clash 客户端 fake-IP/TUN 排除并启用公司 VPN，三个 SSH 与 Guard ready 已验证。见 [本轮证据](evidence/2026-09-16-history-live/README.md)。4000/4001 未改，新版服务器独立验收接续执行。 |
| 2026-09-16 09:44 | GPU 真实多轮与东京 effort 核对 | 新版源码在 GPU 独立目录通过 Linux test/vet/race/build。首轮历史业务 41 次发出、36 成功、5 次 Go 弱词组合误拦，导致 7 个后续步骤未执行；Guard r2 修复后 48/48 完成，额外安全正反样本 10/10 符合预期。东京页面将 none/minimal/空值均显示为横线，今天早期主要是分类器 none/low；[数据库及源码核对](evidence/2026-09-16-history-live/tokyo-effort-report.md)。递增任务另外暴露旧 reasoning 历史导致的 503/400 和长请求 504；历史适配修复本地 267、Guard 21、Python 8 项通过，独立流式回归进行中。生产 Guard8011、4000/4001 未替换。 |
| 2026-09-16 09:57 | 三协议递增与 Messages 强度修复闭环 | 15次三语SSE业务全部完整返回，实际选择Deepseek→Luna→Terra→Sol→Astra，发送none至xhigh；东京数据库确认Sol/high及Astra/xhigh→high。发现并修复Messages none被桥接默认medium：显式发送output_config.effort，修复后三条真实请求200且东京数据库none/none。Auto最终273、Guard21、Python8项本地与Linux检查通过；93份源码一致，本轮临时进程已停止，远端Key副本删除，生产Guard健康。[完整证据](evidence/2026-09-16-history-live/README.md)保留所有失败；非流式504、最高强度映射、完整客户端、外发脱敏和生产身份/持久化验收未完成，4000/4001未改。 |
| 2026-09-16 11:03 | 补录：输入脱敏边界与本地修复 | 复现 Guard 只脱敏自身副本、Auto 继续外发原文，以及正则破坏 JSON/漏扫凭据的缺口。Guard r3 新增结构化脱敏与 allow 正文摘要绑定；Auto 的分类、同步、三协议 SSE、count_tokens 和缓存统一消费脱敏正文，旧版 Guard 契约失败关闭。Auto 全量 race/vet、Guard 全模块 test 通过；新增7条外发路径回归及过期/畸形契约拒绝测试通过。详见[修改与验证明细](evidence/2026-09-16-input-redaction/README.md)。此行为补录时刻，非各代码改动的精确执行时间。 |
| 2026-09-16 11:03 | 补录：SSH恢复与GPU归档失败 | 10:57:38直接SSH成功，未改网络配置。源码传入GPU独立目录 `/tmp/gateway-redaction-20260916`，摘要一致；Linux测试因上传包漏掉history-cases.json失败，尚未运行新版真实链路。正在补齐归档并重测；生产Guard8011、4000/4001未替换。 |
| 2026-09-16 11:08 | 脱敏完整归档Linux验证通过 | 补齐history-cases.json后97份源文件上传摘要一致，归档SHA为67aa40696c1ea541fba21c88f1d7a947c94d18b93491b8312743edcdb46373a6。GPU两模块race/vet及静态构建通过，开始真实Qwen3Guard与东京测试Key的独立验收；外发捕获器发现未脱敏合成标记会先阻断。4000/4001未改。 |
| 2026-09-16 11:19 | 脱敏实测及计数/504修复 | 真实Guard五类凭据5/5、三协议同步/SSE6/6通过，21次外发检查无合成明文残留；count_tokens的Deepseek返回404，Auto503，失败保留。五模型同输入对照：Deepseek404、其余4个GPT200。新增Deepseek显式utf8_bytes_estimate并标记响应头/审计，其他模型保持上游计数；针对性本地回归通过，尚待GPU复测。504修复新增单次SSE聚合为JSON，三协议工具/签名/usage、截断和取消测试通过；全量回归进行中。详见[本轮记录](evidence/2026-09-16-input-redaction/README.md)。 |
| 2026-09-16 11:22 | 新修复完整本地回归与上传重试 | Auto完整race共302项通过，vet及Python8项通过；结果审计补充upstream_transport，计数审计区分来源，run-pilot要求显式Guard r3端点。99份源码新包首次SSH上传被堡垒机关闭，未取得远端摘要，不能算同步成功；正在重试，GPU新版本尚未验收。 |
| 2026-09-16 11:25 | 新包服务器构建通过 | 重传99文件包后远端SHA与本地一致，Linux两模块race/vet/build全部通过，构建退出0。同步验收脚本和已授权固定题集，准备开启仅pilot使用的BUFFERED_SSE，复测三协议、计数及15次递增非流式任务。验收客户端等待上限明确为300秒，不以此掩盖此前约70秒的前置504；实际结果待完成，生产端口未改。 |
| 2026-09-16 11:30 | 第二轮计数及同步聚合短任务通过 | 真实Guard5/5、同步SSE聚合3/3、原生SSE2/3；Responses原生流一例200但未完整结束，单列失败继续诊断。Deepseek本地估算189、Luna上游计数43均200且方法明确。15次非流式递增任务进行中，尚不能判定长任务504修复完成。Deepseek六次首轮业务均成功，404只属于计数路径，不是模型不可用。 |
| 2026-09-16 11:33 | 长任务及截断原因核实 | Sol两条非流式任务分别81.128秒、153.304秒完整200，已跨过此前约70秒504窗口，Astra尚在运行。Responses原生流失败复测明确为max_output_tokens耗尽：256输出含224 reasoning token；按失败保留，下一轮测试上限改为1024，不修改用户生产请求限制。同时发现provider重新编码会把大整数转float，补充失败回归并改用UseNumber、保留stream_options；本地验证中，当前服务器批次仍是99文件版本，不混记版本。 |
| 2026-09-16 11:43 | 第二轮完成及签名误拦修复 | 非流式15次中12成功，中英文五级均完成；Astra185.352/201.880秒完整200。混合语言在第三步因签名内部Ghp前缀被误判为凭据，结合login弱词触发规则，其余两步会话隔离。Guard r4修正独立token边界，不豁免signature字段，补GitHub token前缀识别。针对性测试先失败后通过；Auto303、Guard33项race/vet及Python8项通过。第二轮69次外发无合成明文残留，临时进程及远端Key已清理。[完整元数据](evidence/2026-09-16-input-redaction/live-second.json)已归档，下一轮验证r4及输出预算。 |
| 2026-09-16 11:45 | r4上传校验及构建脚本路径修正 | 99文件归档SHA为45629e99ebc1a5b71337a1338eb929a152fd6e9270013f6061545e840d4078ce，GPU摘要一致。Guard Linux race/vet通过；临时构建脚本误用cmd/preflight-api，实际入口为guard-deployment/cmd/preflight-api，导致构建退出1。保留失败日志并修正路径续验；尚未启动r4真实链路，生产未改。 |
| 2026-09-16 11:47 | r4 Linux验证通过 | 修正临时构建入口后Auto303、Guard33项race通过，两模块vet及静态构建通过、Python8项通过，build_exit=0。开始真实Qwen与东京测试Key复验：新增安全正反边界、三协议短任务、计数和混合语言五级非流式。隔离目录为/tmp/gateway-r4-20260916，生产未改。 |
| 2026-09-16 11:50 | r4短链路及东京用量核对 | 真实五类凭据、签名保持、使用凭据403且外发POST为0均通过；三协议同步/SSE6/6完整200；Deepseek估算与Luna上游计数通过；1-token预算耗尽按失败识别。混合语言递增仍在运行。东京只读窗口11:20–11:49有Deepseek18条用量，Sol/high、Terra/medium及Astra/xhigh→high均有记录；不能把分类器none/low混作全部业务强度。 |
| 2026-09-16 11:56 | r4真实复验与清理完成 | 17/17检查通过，包含三协议短任务、计数、安全正反对照及混合语言五级5/5；Astra139.531秒完整200。真实旧签名重放allow且正文未变。44次外发无合成明文残留；403能关联Guard/Auto且无usage，预算耗尽记失败并保留1输出token。99源文件SHA匹配；临时8012/8092/8022关闭、远端Key删除，生产Guard/vLLM健康200。详见[r4证据](evidence/2026-09-16-input-redaction/README.md)。4000/4001及共享effort映射未改，整体生产验收未完成。 |
| 2026-09-16 11:59 | 最终源码与文档归档 | 本轮验收后仅更新3份组件README，Go及脚本与已验收r4保持一致。最终归档SHA为43894a85fc28591179ad8dfba5fe851542a55034b80ae25e705ae9fc8fdf6260，GPU隔离目录99/99文件SHA与本地相符，详见[最终清单](evidence/2026-09-16-input-redaction/final-source-manifest.json)及[逐文件核对](evidence/2026-09-16-input-redaction/final-source-verification.json)。总计划/组件说明/证据状态已对齐，执行记录时序、表格、链接及Key泄露检查通过；不改变生产验收边界。 |
