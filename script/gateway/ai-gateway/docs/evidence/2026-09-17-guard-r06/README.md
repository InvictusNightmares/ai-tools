# R06：正常安装包分析误判及会话隔离放大

状态：已定位并用原脱敏请求复现，尚未修复或部署。两地仍是 release v31、Guard v29/preflight-r19。本轮没有变更模型、策略、缓存、隔离状态或正式入口。

## 事件与归因

按维护映射关联美西 Key5（付晓镇），2026-09-17 14:43:16 CST 首次拒绝 request ID `e7ff849d8aec4bee925bfa36b645ed69`；14:43–14:45 同一会话另有23次 `session_quarantined`，共24次拒绝。后23次不是23次独立语义风险判定，也不计入R04已完成业务调用的47次统计。

首次为 `block / medium / Politically Sensitive Topics`，附带弱规则原因 `cyber_context_review`；不是凭据正则硬拦截。0.7是解析器对Controversial标签的固定映射值，不是模型校准后的概率。受限审计中，用户目标是正常的安装包分析；工具读取包信息、签名、安装脚本和可执行文件元数据，没有要求实施攻击。仓库不保存原始提问、文件路径、脚本、签名或工具正文。

可确定的代码传播链：`guard/model/client.go` 将任一块Controversial映射为medium/block；`guard/preflight.go` 对所有block调用 `blockSession`，默认24小时。于是一次语义误判阻断整段会话。首个语义误判的最小触发文本尚未确定，不能假定只由某个词或加密状态造成。

## 原请求复现与对照

用v31冻结源码的独立临时副本调用同一台服务器的Qwen3Guard-Gen-8B及实际tokenizer，完整脱敏请求不经过业务上游。六块中第2块判为Controversial/Political，其余五块Safe，与首个拒绝一致。诊断测试PASS表示成功执行复现，**不表示该正常请求通过Guard**。

- 审计RawBody SHA256：`ea5387e47d873f69b48ed7c79b44de778744ec0b61b2420245716dbe632b5f40`。
- 第2块59265字节，SHA256：`0a4974451b2843100fa3fa7809a7c3c50eba157ad47fd8ec60d6c15236a45558`。
- 受限数据：GPU服务器 `/tmp/gateway-r06-private/`，目录700、文件600；未进入仓库或外部模型。
- 元数据日志：GPU服务器 `/tmp/gateway-r06-baseline.log`。临时诊断代码仅在 `/tmp/gateway-r06-work`，不在发布制品中。

| 对照 | 结果与边界 |
| --- | --- |
| 仅替换不透明加密状态为摘要元数据 | 仍Political，不能归因加密字段本身 |
| 第2块前半/后半 | 分别Controversial/PII、Controversial/Political；拆小本身不能证明误判已消失 |
| 用户目标单独检查 | Safe；不意味着可以豁免历史与工具内容 |
| 顶层instructions单独检查 / 移除instructions | 单独Safe，移除后仍Political；不是单一instructions字段足以解释 |
| 保留全部正文，强调分析材料与执行行为的区别 | 仍Political；提示词调整未解决 |
| 保留字段顺序和全文，将109个字符串字段显式JSON引用 | 仍Political，20091输入token；引用格式未解决 |

上述为诊断对照，不是新验收题集，不以删掉疑似内容后的Safe结果代替完整输入的安全校验。[Qwen官方说明](https://github.com/QwenLM/Qwen3Guard/blob/main/README.md)将Controversial定义为依赖上下文和应用场景的中间类别；目前网关将其直接视为拒绝并隔离，与正常开发材料之间存在误判风险。

## 待处理边界

已向用户提出争议结果自动本地复核的方案，用户追问“本地复核怎么做，通知？”，尚未选定该策略。解释为：另一模型结合任务及工具上下文复核，正常放行、危险拒绝并隔离、不可用或不明确则失败关闭；不是等待管理员通知审批，也不是对同一个Guard简单重复采样。

本机8000已有另一模型服务 `qy-coder:latest`，最大上下文52144；它承担GPU0业务，不能由一次0%利用率快照推断有持续空闲资源。未向该服务发送同事正文，未加载新模型、接入依赖或改变其服务配置。若采用此方案，必须先验证真实误判、危险正反例、完整上下文覆盖、复核注入抵抗、超时/解析/审计失败关闭及共享负载，之后才能更新4004/4005。高风险硬规则和上游明确cyber信号不能被争议复核旁路。

当前会话是否仍被隔离需按后续实时状态核查；本轮没有清除或声称恢复。未经完整原请求、负例及两地入口验证，不标记解决。夏斯建CLI超时独立记录在[R05](../2026-09-17-codex-r05/README.md)，不混为同一问题。
